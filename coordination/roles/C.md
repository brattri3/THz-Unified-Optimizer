# Роль C — приложения

**Имя сессии:** `c-app` · **префикс коммита:** `[C]`

## Пишу
`attenuator_app/**`, `docs/attenuator_app/**` · `track_viewer/**`, `docs/track_viewer/**`
(зона расширена санкцией владельца 2026-08-12)

## Не трогаю
`data_pool/**` — read-only. ⚠ Зона C зависит от имён и расположения файлов и **дважды ломалась
молча**: нерекурсивный `glob` вернул ноль файлов 28.07; валидатор тихо возвращал ноль три дня
после переименования 04.08. Оба раза ничего не падало.

## Что открыто и что дальше

1. **СВОДНОЕ ЗАДАНИЕ — `HANDOFFS.md`, `ОТ ORCH К C` от 17.08.** Четыре пункта в порядке владельца,
   собраны из трёх записей зоны A и её вердикта. Коротко:
   (1) **знак `θ₀` на `att-11-16-356`** — A22 даёт `+1.2238`, `_calib_raw.json` `−1.2730`, модуль
   совпал, знак нет; ворота ко всему приборному;
   (2) **паспорт прибивает `eta0 = 0`**, вся утечка идёт в `D_eff`, а A24 показала: перекос
   неподвижного элемента **имитирует утечку**, и паспорт подмену не различает. ⚠ Тест ранга 1 на
   приборе провален — `T⊥`/`T∥` из A22 там эффективные, в паспорт их зона A **не предлагает**;
   (3) **C-6** — отвязать побинный фит от полосы показа (единственная правка кода);
   (4) **C-2** — одна редакционная фраза в `docs/track_viewer/01_FORMATS.md` §6.3.
2. **Разбор твоего кода сессией A ВЫПОЛНЕН 16.08.** Вердикты — `QUESTIONS.md`, раздел «Вердикт
   зоны A по C-1, C-2, C-5, C-6». **Ни один пункт не отклонён, код фита менять не требуется**,
   `blocking` сняты со всех четырёх. `C-1` принят (A взяла встречное обязательство прогонять
   сверку при правке оригинала), `C-5` — режим `noise` не запрещён и статус не поднят, твоё
   собственное опасение про σ в глубоком гашении **подтверждено**. `C-3` закрыт владельцем
   (В-26), `C-4` снят тобой же. Осталось только (3) и (4) из пункта 1 выше.
3. **Два release gate ждут владельца, оба — личный прогон у прибора:**
   `C6_v02_acceptance` (аттенюатор v0.3) и **пункт 5 ТЗ** `track_viewer` (запуск на Win7 вместе с
   проверкой панелей дельты и канала `dark`). До обратной связи владельца C3/C4/C5 не начинать.
   ⚠ `.exe` аттенюатора v0.3 собран под Python 3.14 и **на Win7 не запустится** — целевая доставка
   теперь портативный комплект.
4. Сдано и смержено: `track_viewer` фазы 1–3, приёмка 27/27, комплект Win7 (106 МБ), документация
   `docs/track_viewer/`.

## Находки, переданные другим (не забыть проследить)
- **A/B:** прежняя оценка шумового пола по предымпульсу завышена на **12.4 дБ**
  (`002_bg`: 3.94e-03 против 2.25e-04 по ВЧ-хвосту); умолчание переведено на метод Нафтали.
- **A/B:** `T_time` из `a21_transmission_summary.py` без вычитания DC завышает тёмные углы в 121 раз
  (опубликованные числа A21 не задеты, риск в переиспользовании).
- **A/B:** поиск пика по сырому `argmax(abs(E))` в зоне гашения промахивается на 5 шагов сетки.
- Собственная находка про гармоники 6–8 **отозвана самой C** — считалась не в той метрике.

## Моё
передача `attenuator_app/HANDOFF_C_20260807.md` · состояние `attenuator_app/STATE.md` ·
`coordination/handoffs/REVIEW_C_fit_malus_2026-08-13.md`

⚠ `track_viewer/core/fit_malus.py` — **копия** `research/two_wgp/malus_linearization.py` (зона A),
а не импорт: комплект Win7 каталога `research/` не содержит. Сверка машинная
(`track_viewer/tools/verify_fit_malus.py`, 48 сверок, расхождение 0.000e+00), но при правке
оригинала расхождение появится молча. **Схема согласования принята (`C-1`, вердикт A 16.08):**
зона A при правке оригинала сама прогоняет сверку и отмечает это в коммите; лока и CI заводить
не нужно (В-27). ORCH прогнал сверку 17.08 — 48/48, `0.000e+00`, порт совпадает бит-в-бит.
