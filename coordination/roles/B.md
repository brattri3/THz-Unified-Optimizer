# Роль B — техническая гигиена и численное ядро

**Имя:** `b-core` · **префикс:** `[B]` · по вызову, не постоянной сессией

## Пишу
Ядро где угодно: `model_core.py`, `fit_lib.py`, `verify_model_core.py`, `refactor_ab_check.py`,
рефакторинг `research/experiments/*.py`, `scripts/**`, `unified_optimizer/**` — только по разовой
санкции на дефект (каждый раз заново)

## Не трогаю
Гипотезы и физику поверх ядра (A) · чужие результаты · `data_pool/**`

**Сужена 08.08** (было «ядро+ревьюер»): ноль голоса в физических выводах, допуск ноль, снимок «до».

## Что открыто и что дальше

1. **NameError продакшна (2) — патч на `main`, НЕ через `session/B-prod-fix`** (вторая несвязанная
   история, `GIT_HISTORY_2026-08-23.md`): `optimizer_2d.py:122` (`p_max_um` не определено),
   `optimizer_lmfit.py:92` (`estimate_deff_initial` без импорта), обе `--method 2d|both`. Проверка
   — `HANDOFFS.md` задача 1. ⚠ `--method 1d` не нужен (18 мин впустую).
2. **`noise_floor`=`4/π`, хэндофф A (16.08, открыт).** Ошибка `×4/π≈1.273` в `tnoise` везде —
   задевает пороги масок/SNR/`χ²`. Решить, править ли (`verify_model_core.py` обязателен). Места
   в коде — `HANDOFFS.md`, `ОТ A К B` 16.08.
3. **Вынос `GEOMETRY`** → `geometry.py` (A), со сроком. Числа в `a18_microscopy/SUMMARY.md`:
   `D_pass_um`/`D_meas_um`/`D_eff_um` (знаменатель явно), `sigma_pos_um` (не приор).
4. `build_experiment(angles_limit, mask_angles)` — снимает второй билдер.
5. Инвариант: второй копии Бланко/грида/невязки/билдера не заводить.

⚠ «5→20» копий фильтра не подтвердилось (их 5) — риск в двух билдерах, маска зависит от углов.

## Моё
`HANDOFF_B_20260807.md` (§3.1 — с поправкой выше) · `CHANGELOG_model_core.md`
