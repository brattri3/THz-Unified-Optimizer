# Сессия C — метка намерений / замок

> Обновляй при СТАРТЕ, смене задачи, «пульсе» и остановке. Перед правкой ОБЩЕГО файла — проверь
> чужие sessions/*.md на пересечение. Устаревший heartbeat (>~1ч) = сессия считается неактивной.

- **Статус:** IN_PROGRESS — чат **C2** (ник `c-app`), запущен 2026-08-12 20:38.
  **➜ ЧИТАТЬ ПЕРВЫМ при следующем холодном старте: `attenuator_app/HANDOFF_C_20260807.md`**
- **Специализация:** приложения-прототипы (CHARTER §3 / OWNERSHIP.md). Зона расширена
  2026-08-12 санкцией владельца: `track_viewer/**` + `docs/track_viewer/**` (CHARTER §8).
- **Текущая задача:** **C7_track_viewer** — новый инструмент `tds_track_viewer`, интерактивный
  контроль углового прогона THz-TDS у прибора. ТЗ: `docs/artifacts/C_TZ_tds_track_viewer.md`.
  Старая C6_v02_acceptance по-прежнему **open** и ждёт прогона владельца — не двигается.
- **План:** этап 1 просмотрщик (ядро → selftest → GUI), этап 2 генератор трека, этап 3 комплект
  Win7. Работа на `main` в основном каталоге: worktree `../TUO-C` отстал на 57+ коммитов, а
  `track_viewer/` — новый каталог, ни с кем не пересекается.
- **Файлы, что трогаю (замок):** `track_viewer/**`, `docs/track_viewer/**` — новые, пересечений
  нет. Читаю (не правлю): `data_pool/**`, `research/two_wgp/prepare_road_test_grid_33_11.py`,
  `research/results/a18_microscopy/a21_transmission.json`, `unified_optimizer/data_manager.py`.
- **Заявлено до (время):** до конца чата C2
- **Heartbeat:** 2026-08-13 (фаза 2 C7 сдана: этапы 4–6, приёмка 27/27, комплект Win7
  пересобран и проверен изнутри; пункт 5 приёмки по-прежнему ждёт прогона владельца)
- **Предыдущий heartbeat:** 2026-08-12 (C7_track_viewer сдан: три этапа, приёмка 19/19,
  комплект Win7 собран и проверен изнутри)
- **Предыдущий heartbeat в этом чате:** 2026-08-12 20:38 (старт C2, лок занят кодом 0)
- **Предыдущий heartbeat:** 2026-08-07 (закрытие чата C1; валидатор зоны C починен после
  переименования данных A, числа воспроизведены бит-в-бит; передача дел записана)
- **Предыдущий heartbeat:** 2026-07-28 21:55 (UI переведён на английский — прибор в Китае, решение
  владельца; коммит `713f807` в `session/C`; физика бит-в-бит; чек-лист ред.2 распечатан;
  Win7-комплект `../win7_attenuator_kit` пересобран с EN-интерфейсом и EN-README)

## 🔒 Замок исполнения (см. CHARTER §12)
- **Статус:** IN_PROGRESS
- **Держатель (ник):** `c-app` (чат C2)
- **Взят:** 2026-08-12 20:38 (`preflight.py C --acquire c-app` → код 0, `.lock` создан с нуля)
- **Heartbeat:** 2026-08-13 (фаза 2 задачи C7 сдана; чат продолжает работать, лок за ним)
- **Таймаут простоя:** 15 минут
- **Предыдущий держатель:** C1, chat id `f8241c1b-2e46-4b7f-baf8-2bd6f194ef9d` — `retired`
  2026-07-28 17:57; дальнейшая работа C с 18:30 того же дня шла в **Cursor**, не в Claude Code
  CLI — у неё нет chat id в `~/.claude/projects`; см. `NICKS.md`

## История намерений (append)
- [2026-07-27] seed-инициализация метки.
- [2026-07-28] Сессия C зарегистрирована: прототип v0.2 уже функционален (CLI, API, selftest 13/13,
  validate 69/69); создан `attenuator_app/STATE.md`; зависимость от A4 (attenuation_calc) — open, ждём A.
- [2026-07-28 15:31] Атрибуция постфактум + закрытие C1. Уточняю запись выше, не редактируя её:
  прототип v0.2 и все 7 документов `docs/attenuator_app/` написаны рабочей сессией C **до** появления
  каркаса `coordination/` (как у A и B); регистрация была оформлена снаружи и истории кода не знала.
  **Поправка по существу:** «ждём A4» неверно — C не заблокирована, `attenuator_app/core/` считает
  сама и валидирована на 69 точках (100 % покрытие 95 % интервала). Задача переформулирована:
  `C2_a4_integration` (blocked) → `C2_a4_crosscheck` (open), сверка двух калькуляторов вместо
  интеграции; запрос к A — в `HANDOFFS.md`.
  **Долг вне зоны, задекларирован:** этой же сессией по заданию владельца созданы `WORKSPACE_MAP.md`,
  четыре `CLAUDE.md` и строка `runs/` в `.gitignore` — по нынешней карте зона ORCH. Задание было до
  карты; впредь C туда не пишет.
  Подробности состояния, допущения (6 шт.) и два непотеряемых решения — в `attenuator_app/STATE.md`.
- [2026-07-28 18:30] Перезапуск сессии C в Cursor. План итерации: (1) selftest+validate до доверия
  числам, (2) коммит незакоммиченной зоны C на main per-zone — по рекомендации ORCH из прохода 17:16,
  с анонсом в ACTIVITY, (3) worktree `../TUO-C` + ветка `session/C` для дальнейшей работы (CHARTER §2).
- [2026-07-28 18:45] Итерация C-restart закрыта. Приёмка бит-в-бит (selftest 13/13, validate
  69/69 = 100 %, RMSE 1.23 дБ); зона C закоммичена на main (`3e4cd24`) — C-часть флага ORCH снята;
  создан worktree `../TUO-C` (`session/C`), там же закоммичен обновлённый STATE.md (`2d84261`).
  **Для ORCH:** ветка `session/C` готова к мержу в любой момент; дальнейшие правки C — только в worktree.
- [2026-08-07] **Закрытие чата C1, штатное освобождение лока.** Передача роли —
  `attenuator_app/HANDOFF_C_20260807.md`. Итог последнего прохода: **валидатор зоны C был сломан
  с 04.08 и молчал об этом.** Переименование данных Сессией A (`eda293e`) прошло по `ROTATES`
  внутри `tools/validate.py`, но не по умолчанию `--datasets` и не по префиксам групп ⇒ 0 найденных
  файлов, текст «no data for validation», код возврата 0. Починено; числа воспроизвелись бит-в-бит
  (13/13; 69/69 = 100 %, +0.35 дБ, RMSE 1.23, разница групп +0.15 дБ, запас ×1.67) ⇒ **переименование
  физику не затронуло, запрос A к C закрыт.** Добавлена защита: «файлов не найдено» отделено от
  «мало углов», `main()` возвращает 1. Ветка `session/C` **полностью влита в main** (`713f807` —
  предок main), worktree `../TUO-C` чист, но отстал на 57 коммитов — работать в нём без обновления
  нельзя, иначе повторится тот же дефект. Задача C6 (приёмка v0.2 владельцем) не двигалась.
- [2026-08-12 20:38] **Старт чата C2, ник `c-app`.** Новая задача **C7_track_viewer** по ТЗ
  `docs/artifacts/C_TZ_tds_track_viewer.md` (составил ORCH, согласовано с владельцем 2026-08-12).
  Владелец в этой сессии закрыл O-1…O-7 §8 ТЗ: код в `track_viewer/` в корне (зона C расширена);
  стек Tkinter + matplotlib; усреднение референсов в спектре **по мощности** ⟨|FFT|²⟩ (не
  комплексное — измерено: комплексное завышает T в 8.6 раза на 2.76 ТГц); третья колонка
  игнорируется; блочный порядок обхода по умолчанию; DC по умолчанию — предымпульс 15 %;
  доставка — портативный комплект Win7, `.exe` отложен; очерёдность — просмотрщик первым.
  **Число приёмки воспроизведено до кода:** T = 98.909 % для `003_a0.txt`/`002_bg.txt` без
  вычитания DC. Расхождение с `a21_transmission.json` (99.08 %) объяснено: A21 идёт через
  `DataManager`, который **когерентно усредняет трассы** rep1+rep2 и лишь потом считает T
  (`unified_optimizer/data_manager.py:84`); порознь rep1 = 98.909 %, rep2 = 98.461 %.
- [2026-07-28 19:05] Указание владельца: релизов НЕ было, «в эксплуатации» — ошибка записи;
  введён release gate (флаги одобрения по версиям) — `session/C` коммит `4da44b3`, задача
  C6_v02_acceptance. Отревьюирован операторский гайд незарегистрированной сессии: команды/файлы
  сверены с кодом, 2 фактические ошибки (зоны red/amber, «84° Specac») — разбор передан владельцу.
- [2026-08-13] **Фаза 2 C7 сдана: этапы 4–6** (`58633d2`, `645db0b`, `a1f69c2`), приёмка
  19 → 27, комплект Win7 пересобран и проверен изнутри. Добавлены пропускание по полосе
  частот рядом с временны́м и панели дельты (Парсеваль проверяется кнопкой прямо на
  экране: полная полоса схлопывает Δ до −3.8e-06), канал `dark` (перекрытый пучок) и три
  источника шумового пола. Умолчание пола переведено с предымпульса на ВЧ-хвост (метод
  Нафтали): **измерено завышение на 12.4 дБ** — предымпульсный участок содержит
  низкочастотный дрейф, а не только белый шум. `dark` **не вычитается из трасс** ни при
  каких настройках. Кнопка «полный пересчёт» переименована в «перечитать все файлы с
  диска». Разбор — `coordination/ACTIVITY.md`, запись `[C] 2026-08-13`.
