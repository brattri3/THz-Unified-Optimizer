# `docs/` эпохи Antigravity — архив (июль 2026)

Заморожено 2026-08-09 по решению владельца **В-2**: «заморозить, живое развести».
**Не исполнять, не пополнять.** 212 файлов, ни один не правился после **2026-07-15**.

## Что осталось живым в `docs/` (сюда НЕ переехало)

- `docs/attenuator_app/` — руководство пользователя GUI, зона **C**, правилось 2026-08-07;
- `docs/literature/` — PDF первоисточников, зона **L** (переезжает к `research/literature/`).

## Что здесь лежит

| Часть | Файлов | Что это |
|---|---|---|
| `artifacts/` | 60 | **Рабочий журнал агентов Antigravity.** Согласованные тройки `plan_*` + `task_*` + `walkthrough_*` (setup, stability, normalize, legacy_2d, ide_setup, drude, fixes, lmfit) — ровно то, что предписывал §3 отменённого `AGENTS.md` (см. `archive/antigravity-2026-07/`). Плюс отчёты, `MANIFEST.md`, презентации, `report.tex`/`report.pdf` |
| `images/` | 139 | Графики тех прогонов: невязки 1D/2D, гриды, стабильность, дрейф, кадры презентации `story_*`. Плоской кучей, без разбивки по теме — найти можно только угадав имя |
| `legacy_scripts/` | 2 | Два старых оптимизатора |
| корень | 11 | Аналитика (`analysis_*`, `precision_analysis`, `optimization_comparison`, `instrument_function_proof`, `modeling_storyline`), **черновик статьи** `article_draft_part{1,2,3}.md`, паспорт прибора `passport.tex` / `passport_ru.md` |

## Про черновик статьи — вопрос закрыт числами

`article_draft_part{1,2,3}.md` — это 47 + 47 + 65 = **159 строк от 14 июля**. Действующий черновик
зоны D — `research/paper/DRAFT.md`, **434 строки, правился 5 августа**. Старый заведомо перекрыт;
сверять построчно смысла нет. Если что-то из него понадобится — брать оттуда как из источника
формулировок, а не как из параллельной версии текста.

## Что было удалено при заморозке, а не перенесено

LaTeX-побочки `report.{aux,log,out,synctex.gz,toc}` — регенерируются из `report.tex`, который
здесь же. Исходник и итоговый `report.pdf` сохранены.

## Если нужен файл отсюда

Читать можно свободно. Возвращать в работу — только через роль-владельца соответствующей зоны
(`coordination/OWNERSHIP.md`), с явной пометкой, откуда взято и почему считается актуальным.
