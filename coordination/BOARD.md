# BOARD — открытые находки, решения и хэндоффы по ролям

> **Кто сейчас активен и в каком статусе — смотреть `claude agents` / `claude agents --json`
> (см. `coordination/MIGRATION.md`), не здесь.** Эта колонка регулярно отставала от факта (см.
> историю проекта 2026-08-03…07) и с 2026-08-07 убрана. Ниже — то, чего `claude agents` не знает:
> научные находки, принятые решения, что делать дальше.

| Роль | Открытые находки / решения / что дальше | Обновлено |
|---|---|---|
| **A** модели/идентифицируемость/гипотезы + софт затухания | Следующая задача — **A3_refit_series** (A0/A6/A4 — DONE, все хэндоффы к A закрыты). **A0**: гармоническая линеаризация Малюса (точный базис 5-мерный, 4-я гармоника несёт фазу утечки); цена `ANGLES_LIMIT_2D=(0,90)`: σ(θ₀) хуже в 9.9 раза, сдвиг θ₀ −0.69…−2.10°. **A6**: асимметрия `U(θ)` НЕ сводится к `angle_offset` (9.3σ / 4.2σ) → **HN13 REJECTED**; M3ref исключён (11.5σ), `M5_eps` выживает (1.6σ). **A4**: калькулятор затухания 9/9, таблица для `C2_a4_crosscheck` готова; при физ. `D=11` эталон даёт 23.45 дБ против ≈37.9 дБ из данных — загадка `D_eff` со стороны прибора. **Хэндоффы:** B→A `blanco_t` **done** (86.5×/27740×; «допуск 0» недостижим при смене единиц — измерено 1.05e-15), B→A билдер закрыт, C→A **done**, D→A/B свод **done**. **Мои запросы:** к B (точка входа по `pol`; `build_experiment` с `angles_limit`+`mask_angles`), к D (методичка), к L (первоисточники по 2ω/4ω). Далее A3 → A5 + переподгонка M5 на полном диапазоне | 2026-07-29 |
| **B** ядро/рефактор | model_core доведён (итер. 1–2), бит-в-бит. Далее по запросу: two_wgp на ядро (нужен хэндофф A), перегенерация устаревших артефактов | 2026-07-28 |
| **C** app | **C6_v02_acceptance** — UI переведён на EN (прибор в Китае, решение владельца, ACTIVITY 21:55), физика бит-в-бит; Win7-комплект + чек-лист ред.2 распечатан. Ждём прогон владельца на Win7-ПК у спектрометра + обратную связь → флаг одобрения v0.2; далее C2 (ждёт A4), C3–C5. Ветка `session/C` (`../TUO-C`, HEAD `713f807`) готова к мержу ORCH | 2026-07-28 21:55 |
| **D** статья + образоват. слой | **D3 — DRAFT v0.1 готов**; **D4 — primer 01 готов** (D1/D2 done). Готово: `RELATED_WORK.md` (D1), запрос `ОТ D К L` 5 пробелов + `ОТ D К A/B` свод чисел (D2), `GLOSSARY.md` v0.1, **`DRAFT.md` v0.1** (Abstract/Methods/Results/Novelty/Limitations), **`primers/01_jones_polarization.md`**. Результаты A0/A6 внесены в DRAFT (§3.2 цена обрезки углов, §4.6 HN13 + нечётность, §3.6 бутстрап по частотам занижает ошибку в 3.5–18.9×; номер гипотезы HN12→HN13 исправлен 2026-08-07). Далее: primer 01b (заказ A, Малюс) → 02 Бланко → 03 Друде. Ждём: P1 от L (библиография ядра), свод чисел от A/B, ⟨?⟩ D/P у test_grid (0.50 vs 0.46) | 2026-08-07 |
| **L** литература/обзор + метапоиск | **L3_supply** — `ОТ A К L` **done**, `ОТ D К L` частично (P1 done, P2/P3 частично, P4/P5 не начаты). **P1 ЗАКРЫТ: ссылка на аналитическое ядро модели была недействительной.** Blanco 1986 = *Infrared Phys.* **26**(6):**357–363**, DOI `10.1016/0020-0891(86)90058-8`; запись «IJIMW 7(11):1611–1629» — фантом. `model_blanco.py` реализует именно её. Границы автора: $d/\lambda<0.2$, $d/p<0.1$, приемлемо до **0.5** — наш $D/P=0.71$ **вне** области. **АУДИТ DOI КОРПУСА: 34 DOI, 1 битый** — Manabe & Murk `…884786` **404** → верный **`…838786`** (был в 6 местах, в т.ч. в запросе D2 — **D, поправить**). **P3 отрицательный:** плотный режим $D/P>0.5$ не разработан. **Запрос A done:** канон — Hauge 1980; связка «когерентность → 4ω → относительная фаза» **не найдена** (заявка на новизну). Артефакты: `papers/litrev/answers/*`, `litrev/tools/doi_audit.py`. Далее: P2-выжимки, P4/P5, 2-й уровень графа, Kaltenecker на гармоники | 2026-07-29 19:40 |
| **ORCH** | **Заморожено** (2026-08-07, решение владельца — см. `ACTIVITY.md`): координационная работа встала на паузу, 7 заходов протокола против 0 в `research/` за 2026-08-03…06. С того же дня — миграция на штатные механизмы Claude Code вместо самодельных (см. `coordination/MIGRATION.md`): реестр `NICKS.md` закрыт, живой статус здесь убран. Отложено (не отменено): `--touch` для `.lock`, Фазы C/D/E `SCHEDULING.md`, режимы ведения, извлечение в шаблон-репозиторий. Открытые хэндоффы: `B→A build_experiment(angles_limit,mask_angles)`, `A→D primer 01b Малюс`, `D→A/B свод чисел Results`, `L→C переименованный корпус`, `D↔L P4/P5` | 2026-08-07 |

## Легенда владения (полностью — OWNERSHIP.md)
A→`research/two_wgp/`+`research/hypotheses/` · B→`research/experiments/model_core.py`+shared-code · C→`attenuator_app/` ·
D→`research/paper/` · L→`research/papers/` (вкл. свёрнутый `research/papers/litrev/`) · ORCH→`coordination/`,`CLAUDE.md`,main `research/state.json`.

## Активные «замки» намерений
См. `coordination/sessions/*.md`. Перед правкой общего файла — проверь их. Каждый файл содержит
блок «🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, имя держателя, таймаут
простоя 15 минут.
