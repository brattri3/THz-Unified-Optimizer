# SCHEDULING — окна работы, роль SEC, автономный режим (ПРОЕКТ, ждёт одобрения владельца)

> **Статус: ЧЕРНОВИК.** Ничего из описанного здесь ещё не введено в действие — ни в
> `CHARTER.md`, ни в виде реального кода/задач Планировщика. Документ сводит воедино
> решения, принятые в разговоре с владельцем 2026-07-31, для вычитки и правки. После
> одобрения: (1) появляется CHARTER §13 со ссылкой сюда, (2) реализуются файлы-механизмы
> ниже, (3) регистрируется сессия SEC по обычной процедуре (NICKS/BOARD/sessions).

## 0. Зачем это вообще

Мультисессионная работа над проектом (сессии A/B/C/D/L + ORCH) может в перспективе идти
автономно, по расписанию, без владельца за клавиатурой. Но владелец хочет: (а) не терять
контроль над скоростью эволюции проекта, (б) успевать вмешаться или прислать сигнал
остановки, (в) не тратить общий 5-часовой лимит подписки Pro впустую из-за того, что
несколько сессий одновременно запустили дорогие расчёты. Отсюда — три независимых, но
переплетённых механизма: **окна** (контроль темпа), **реестр резерваций** (контроль
бюджета), **роль SEC** (клей между ними + канал уведомлений).

## 1. Роль SEC — «секретарь и аналитик»

Уже частично существует как инструмент (`coordination/tools/kpi_stats.py` +
`kpi_report.py`, зона ORCH, коммит `c6dad19`). Проектом предполагается вырастить это в
полноценную сессию-роль:

- **Пишет:** `coordination/reports/**`, свой `sessions/SEC.md`, свои строки в
  `NICKS.md`/`BOARD.md`/`ACTIVITY.md`, (после этого документа) `coordination/schedule.json`
  и `coordination/QUESTIONS.md`.
- **Читает:** всё координационное + `git log`. Не трогает `research/**`, `attenuator_app/**`
  — она про процесс, не про науку.
- **Никогда не делает:** не редактирует чужие локи, не форсит чужие решения — только
  предлагает (запись в `ACTIVITY.md`/`QUESTIONS.md`) или сигналит (`PushNotification`).
- **Обязательный preflight:** по договорённости — запускается ПЕРВОЙ при старте любой
  сессии: собирает свежий KPI (`kpi_stats.py`), даёт сводку «что случилось, пока вас не
  было», проверяет `/usage` (реальный остаток лимита текущего 5-часового окна — команда
  `/usage`/`/status` встроена в Claude Code, это НЕ оценка по токенам, а факт от Anthropic)
  и реестр резерваций (§3), даёт вердикт: можно стартовать / подождите окна / осторожно,
  бюджет на исходе.
- **Событийные правила** (пуш только при срабатывании, не по расписанию — решение
  владельца):
  1. Зависший лок (`IN_PROGRESS`, heartbeat старше 15 мин, CHARTER §12).
  2. Коллизия — два чата одной роли одновременно `IN_PROGRESS` (прецедент L1/L2).
  3. Кандидат на `/compact` (токены/длительность активного чата превысили порог).
  4. Порог бюджета — оценка `$`/токенов по роли или по проекту с прошлой проверки.
- **Стек:** Python stdlib (уже написан) + Planировщик Windows (`schtasks`) для запуска.
  Инструменты сессии: `PushNotification`, `Artifact` (передеплой отчёта), Bash/Read/Grep.

## 2. Окна — темп, не бюджет

Смысл, зафиксированный владельцем: окна нужны **для контроля темпа со стороны владельца**,
а не для экономии токенов (для этого есть реестр, §3). Ошибочно было бы решить, что окна —
это оптимизация; это в первую очередь предохранитель «проект не убежит быстрее, чем я
успеваю следить».

- **Активное окно** (пример: 15 минут) — в это время могут работать зарегистрированные на
  это окно сессии.
- **Окно тишины** (пример: 5 минут) сразу после — доступ только ORCH/SEC. Используется для:
  сбора заявок на следующее окно, вызова `/usage`, публикации расписания следующего окна.
- **Два режима того, что значит «окно закрылось»** — оба нужны, переключаются флагом
  (см. §5), а не архитектурно разные системы:
  - **HARD_STOP** — сессия обязана физически остановиться на границе окна и ждать связи с
    владельцем/следующего разрешения.
  - **PING_CONTINUE** — SEC присылает статус на границе окна, работа продолжается до
    ближайшего естественного атомарного шага (CHARTER §7 уже требует «один атомарный шаг за
    итерацию» — это и есть точка паузы, ничего дополнительно изобретать не нужно).

### 2a. Разбор открытых вопросов §8 пп.1-2 (Фаза B, 2026-08-04, песочница scheduling-dev)

**П.1 — ширина окна ≠ таймаут `.lock` (15 мин). Это два разных масштаба, путать не стоит.**
Довод: у них разное назначение и разная физика отказа.
- Таймаут `.lock` (CHARTER §12) отвечает на вопрос «жив ли этот конкретный чат прямо сейчас»
  — калиброван под то, сколько правдоподобно молчать между heartbeat-ами одного процесса,
  прежде чем считать его упавшим. Короткий по конструкции, это порог ОБНАРУЖЕНИЯ СБОЯ.
  Фаза A сегодня добавила конкретную цифру к этой же оси времени: headless-перезапуск
  `claude -p` (даже тривиальный, `--model haiku`) занял ≈9 с целиком + ≈19-22 с дрейфа
  запуска от Планировщика — то есть даже холодный пуск процесса съедает заметную долю
  15-минутного окна, если бы окно и таймаут были одним числом.
- Ширина окна планирования отвечает на другой вопрос — «сколько проекту позволено уйти
  вперёд, прежде чем владелец должен посмотреть» — порог ЧЕЛОВЕЧЕСКОГО ТЕМПА, не сбоя.
  Он должен вмещать реальный «один атомарный шаг» (CHARTER §7) содержательной работы
  (правка кода, прогон фита, запись в лог) — это на порядок дороже по времени, чем сам факт
  живого процесса; `kpi_data.json` (снимок 2026-07-31) показывает, что содержательные сессии
  ролей длятся часами тратя десятки Bash/Edit-вызовов на один заход, не минутами.
  **Вывод: НЕ переиспользовать 15 минут дословно.** Рекомендуемый стартовый дефолт —
  **активное окно 30 минут, окно тишины 10 минут** (на порядок шире таймаута лока, чтобы
  один реальный атомарный шаг гарантированно помещался, но короче настолько, чтобы владелец
  успевал вмешаться в пределах одного присеста за компьютером). Цифра — начальная, не
  калибрована по данным (это была бы Фаза C/задача §1 плана — нужна отдельная выборка
  длительности «один атомарный шаг» по ролям, сегодня не собиралась); **владелец
  подтверждает или поправляет перед тем, как цифра попадёт в CHARTER §13.**

**П.2 — дефолт по умолчанию: HARD_STOP.** Довод от собственной формулировки документа (§2,
выше): окна — это «предохранитель, не оптимизация», цель явно сформулирована как
«проект не убежит быстрее, чем я успеваю следить». `PING_CONTINUE` этому соответствует
слабее: если владелец не увидел пуш-уведомление сразу (сон, недоступность), проект может
уйти на несколько атомарных шагов вперёд ДО следующего окна тишины — именно тот сценарий,
от которого окна должны страховать. `HARD_STOP` даёт эту гарантию буквально ценой простоя
сессии в ожидании. Рекомендация: **`HARD_STOP` — дефолт**, `PING_CONTINUE` — явный opt-in
флагом для конкретной роли/захода, когда владелец сознательно даёт больше автономии (напр.
известно дешёвая, низкориска задача). Как и п.1 — рекомендация ORCH, не решение: сам
документ (см. шапку) требует одобрения владельца перед тем, как это станет действующим
правилом CHARTER §13.

**П.3 — пороги SEC (§1), калибровка от `kpi_data.json` (Фаза B, 2026-08-05).** Не единое
число на все роли — довод из самих данных: `kpi_data.json` (снимок 2026-07-31) даёт
`cost_usd` **на порядок разный** по ролям (`ORCH ≈ $217` vs `D ≈ $31` совокупно за всё
время). Единый абсолютный порог был бы либо смешон для ORCH, либо никогда не сработал бы для
D. Взята за основу медиана трат ПО ЧАТУ данной роли (не суммарная роль-агрегатная цифра —
она смешивает несколько чатов в одно число, не годится как «типичная одна сессия»), с
коэффициентом запаса ×2 (верхняя граница предложенного диапазона ×1.5-2 — тише «дёргает»
SEC на нормальный разброс, всё ещё ловит реальный перерасход):

| Роль | Чаты в выборке | Медиана `cost_usd`/чат | Порог (медиана × 2) |
|---|---|---|---|
| ORCH | O1=$177.76, O2=$39.40 (n=2) | $108.58 | **≈$217** |
| A | A1=$59.50, A2=$93.90 (n=2) | $76.70 | **≈$153** |
| B | B1=$92.24 (n=1) | $92.24 | **≈$184** |
| C | C1=$292.51 (n=1) | $292.51 | **≈$585** |
| D | D1=$31.14 (n=1) | $31.14 | **≈$62** |
| L | L1=$46.86, L2=$67.36 (n=2) | $57.11 | **≈$114** |

Формула: `порог_роли = медиана(cost_usd по чатам этой роли) × 2`. **Честные оговорки, не
скрываем:** (а) у B/C/D в выборке ровно ОДИН чат — «медиана» там формально вырождается в
единственное значение, не настоящая статистика, это анкерная точка, не устойчивая оценка;
(б) снимок `kpi_data.json` датирован 2026-07-31 — не включает эту неделю (чаты O3/O4/O5,
Фазы лока §12 и SCHEDULING); (в) сама таблица тарифов в `kpi_stats.py` помечена автором
инструмента как «ОРИЕНТИРОВОЧНАЯ» (не сверена с реальным счётом) — эти `$`-пороги того же
качества: для ОТНОСИТЕЛЬНОГО сравнения ролей между собой, не абсолютная истина. Это порог
мягкого сигнала SEC («осторожно, бюджет на исходе» — см. §1, третий из трёх вердиктов), не
блокировка.

Порог «кандидат на `/compact`» — токены истории не годятся мерой (окно контекста разное у
разных моделей: Opus/Sonnet/Haiku размером не совпадают, абсолютное число токенов
несравнимо между ними). В Claude Code есть команда **`/context`**, показывающая ЖИВУЮ
разбивку использования контекстного окна ТЕКУЩЕЙ сессии в процентах — это и есть правильная
единица измерения, не эмулировать её оценкой по накопленным токенам из прошлого. Рекомендация
(не откалибровано, стартовый дефолт): **≥80% контекста → кандидат на `/compact`**. Оговорка
честно: команда `/context` в этом заходе НЕ прогонялась эмпирически (в отличие от `/usage`,
§7, тоже не тестировался live в этой сессии) — не выдаю знание о её существовании за
подтверждённый в этом чате факт.

И п.3, как пп.1-2 — рекомендации ORCH, ждут подтверждения/правки владельцем перед CHARTER §13.

**П.4 — парковка vs пересоздание чата по роли, дефолт для ВСЕХ ролей (Фаза B, 2026-08-05).**
Не абстрактный выбор — есть прецедент, ТРИЖДЫ подтверждённый (реальные `/clear` с сохранением
состояния через файлы: O1→O2 `2026-07-28`, O2→O3 `2026-08-03`, O3→O4 `2026-08-04`), плюс
сегодняшний **контрпример, который довод только усиливает**: переход O4→O5 физически
`/clear` вообще НЕ делал (владелец решил продолжить в той же беседе без сброса, раз состояние
всё равно живёт в файлах) — координация не пострадала ни в том, ни в другом случае, потому что
источник истины НИКОГДА не был памятью чата.

**Рекомендация: дефолт `resume + /clear + новое имя` — для ВСЕХ ролей A/B/C/D/L, не только
ORCH.** Не буквальная «парковка» (держать процесс живым между окнами — хрупко к сну/
перезагрузке, до 7 дней потолок) и не полное пересоздание с нуля (терять `--resume`-ссылку на
историю чата совсем, хотя де-факто состояние и так не в истории, а в файлах). Роль никогда не
"паркуется" в буквальном смысле — она всегда либо продолжается тем же процессом под новым
именем, либо поднимается заново headless-вызовом (Планировщик, §7a) — оба варианта читают
состояние из `coordination/**`/`research/**`, не из памяти чата.

**Что тогда остаётся от `CronCreate`/`ScheduleWakeup` (§4)?** Не конкурирующий механизм
между-сессионного пробуждения — он для этого никогда не подходил (in-memory, гибнет при
`exit`, 7-дневный потолок). Их реальная ниша уже: короткое самопробуждение ВНУТРИ уже
открытого, продолжающегося чата в пределах ОДНОГО активного окна (напр. «подождать 10 минут,
пока досчитается фит, потом проверить результат», не завершая сессию) — там полный headless-
перезапуск (§7a, ≈9-31 с оверхед даже на тривиальный вызов) избыточен для короткого внутреннего
ожидания. §4 переписан ниже под это разделение — не оставляю как будто ещё нужный механизм для
между-окон-пробуждения, которым он не является.

**Отдельное наблюдение, НЕ решение архитектуры:** если для какой-то роли (в первую очередь
кандидат — D, содержательные нюансы интерпретации гипотез при синтезе статьи) кажется, что
для продолжения нужна именно ПАМЯТЬ ЧАТА, а не файлы `coordination/**`/`research/**` — это
сигнал, что дисциплина «писать существенное в файлы сразу» где-то у этой роли проседает
(CHARTER §7 п.5: «числа → RESEARCH_LOG.md», это касается не только чисел, но и интерпретаций).
Законный повод для парковки в буквальном смысле НЕ равно «мне так удобнее» — если файлы
неполны, чинить нужно дисциплину записи, не архитектуру планирования. Зафиксировано как
наблюдение для будущей проверки (напр. когда роль D реально продолжит работу), не как решение.

## 3. Реестр резерваций — бюджет, не темп

Цель — не дать нескольким сессиям одновременно съесть общий 5-часовой лимит Pro.

- **Заявка** — лёгкая запись самой сессии в СВОЁМ `sessions/<ID>.md` (поле
  `next_window_request: {задача, оценка_токенов/действий, приоритет}`), не общий файл —
  чтобы не было гонки записи N чатов в одно время.
- **SEC — единственный автор** `coordination/schedule.json` (машиночитаемый) и его
  человекочитаемого зеркала на KPI-странице (§6). В окне тишины перед открытием окна SEC:
  собирает все `next_window_request` → смотрит `/usage` → распределяет по приоритету →
  публикует авторизованный список на окно (кто допущен, кто ждёт).
- **Один активный чат на роль одновременно** (уже действующий инвариант CHARTER §12) —
  сохраняется и внутри окна: разные РОЛИ могут работать параллельно в одном окне, но не два
  чата одной роли.
- **Дальний слот-«письмоноша»** — сессия может зарезервировать далёкий слот с
  почти нулевой заявленной стоимостью ТОЛЬКО чтобы проверить, не пришло ли ей что-то в
  `HANDOFFS.md`/ответ от другой сессии; если пришло — регистрирует уже полноценную заявку на
  следующее подходящее окно, если нет — снова засыпает.

## 4. Механизм запуска/пробуждения — факты, не предположения

Проверено через `claude --help` и поведение инструментов в этом чате; разбор между-окон
vs внутри-окна дополнен Фазой B п.4 (§2a, 2026-08-05).

- **Дефолт МЕЖДУ окнами/сессиями — для ВСЕХ ролей (A/B/C/D/L, не только ORCH), см. §2a п.4:**
  `resume + /clear + новое имя` (следующий ник в `NICKS.md`). НЕ буквальная «парковка»
  (держать процесс живым — хрупко к сну/перезагрузке, `CronCreate` ограничен 7 днями) и НЕ
  полное пересоздание с нуля. Обоснование — трижды подтверждённый прецедент ротации ORCH
  (`O1→O2`, `O2→O3`, `O3→O4` — реальные `/clear`) плюс контрпример `O4→O5` (продолжение БЕЗ
  `/clear` тоже прошло штатно) — координация не зависит от памяти чата ни в одном случае,
  источник истины всегда `coordination/**`/`research/**`.
- **`CronCreate`/`ScheduleWakeup` — узкая ниша, НЕ механизм между-окон/между-сессий.**
  Работают ТОЛЬКО внутри уже открытой, продолжающейся сессии (само себя будит), in-memory,
  **гибнут при `exit`/крахе чата**, `CronCreate` — жёсткий потолок **7 дней**. Их место —
  короткое самопробуждение ВНУТРИ ОДНОГО активного окна, пока чат физически не закрывался
  (напр. «подождать 10 минут, пока досчитается фит, проверить результат») — там headless-
  перезапуск избыточен (см. ниже, ≈9-31 с только на тривиальный вызов). Ни разу не
  используется для того, что этот документ называет «пробуждением между окнами» — эта задача
  решена пунктом выше, полностью независимо от `CronCreate`/`ScheduleWakeup`.
- **Планировщик Windows (`schtasks`) + headless `claude -p ...`** — единственный способ
  поднять НОВЫЙ процесс для роли, чат которой закрыт (между-окон случай выше, когда роль
  физически не осталась открытой). Переживает перезагрузку/сон, не имеет 7-дневного потолка.
  Заявленная владельцем ненадёжность — **базовый тест сделан, см. §7a, частично закрыт**
  (срабатывание подтверждено, дрейф старта ≈19-22 с + ≈9 с сама сессия; экран/серия/сон —
  ещё нет).

## 5. Режим ATTENDED / UNATTENDED — что делать с вопросами к владельцу

Проблема, которую поднял владелец: ночью (или когда он недоступен) сессия может зависнуть на
`AskUserQuestion`-подобном опросе, ожидая ответа, который не придёт.

- **Флаг** `coordination/MODE.flag`: `ATTENDED` (владелец у клавиатуры, вопросы — как
  обычно) / `UNATTENDED` (владелец недоступен, переключает сам перед тем как оставить
  проект без присмотра, например на ночь).
- **В `UNATTENDED`** сессия применяет уже существующий критерий обратимости действия
  (тот же, что в системных инструкциях по риску — не новое правило):
  - Вопрос про **обратимое/некритичное** (выбор подхода, приоритет, стиль) → берёт разумный
    дефолт, пишет его и причину в `coordination/QUESTIONS.md` (append-only, как
    `HANDOFFS.md`), продолжает работу.
  - Вопрос про **необратимое/дорогое/меняющее направление** → останавливается, SEC шлёт
    `PushNotification` «сессия `<ник>` ждёт решения», ждёт ответа владельца (в т.ч. через
    Remote Control с телефона — уже подтверждено рабочим).
- **Флаг остановки** `coordination/STOP.flag` (наличие файла = сигнал) — про ДРУГОЕ: полная
  приостановка планирования НОВЫХ окон проектом целиком (не ответ конкретной сессии на
  конкретный вопрос — это обычный диалог, отдельного протокола не требует). SEC проверяет
  этот флаг перед публикацией расписания каждого следующего окна.

## 6. Расширение KPI-отчёта — таймлайн с будущими слотами

Продолжение уже существующего `svg_gantt` (`coordination/tools/kpi_report.py`): прошлые
бары — как сейчас, сплошные; плюс запланированные слоты справа — пунктиром/бледнее, с
оценкой бюджета токенов, кратким описанием задачи и кликабельной ссылкой на md-файл
сессии, которой принадлежит слот. Зависит от появления `coordination/schedule.json` (§3) —
делается сразу следующим шагом после реестра.

## 7. Что уже РЕАЛЬНО работает (не проект, а факт на 2026-07-31)

- Remote Control подтверждён (владелец пишет с телефона).
- `PushNotification` — реальный инструмент, шлёт на телефон если Remote Control подключён,
  иначе — desktop-уведомление.
- `/usage`/`/status` — встроенная команда Claude Code, реальный остаток лимита 5-часового
  окна от Anthropic (не оценка).
- `coordination/tools/kpi_stats.py` + `kpi_report.py` — собирают токены/коммиты/длительность
  по нику из локальных транскриптов, отчёт опубликован как Artifact.
- CHARTER §12 (ники чатов + лок исполнения, таймаут 15 мин) — уже действует, независимо от
  того, будет ли одобрен этот документ.

## 7a. Результат эмпирического теста Планировщика Windows (Фаза A, 2026-08-04, песочница scheduling-dev)

**Базовый вопрос («срабатывает ли вообще headless-перезапуск») — ДА, подтверждено реальным
прогоном, не симуляцией.**

- Разовая задача `schtasks /create /sc once`, цель — `.cmd`-обвязка, запускающая
  `claude.exe -p "..." --model haiku --dangerously-skip-permissions` headless и логирующая
  метки времени до/после.
- **Найден реальный баг настройки, не гипотетический:** `schtasks /create ... /sd MM/dd/yyyy`
  с датой в формате US (`08/04/2026`) на этой машине (локаль `ru-RU`, `ShortDatePattern =
  dd.MM.yyyy`) молча разобрался НЕВЕРНО — предупреждение `/ST is earlier than current time` +
  `Next Run Time: N/A`, задача была бы создана, но никогда не сработала. Это ровно тот «тихий
  отказ», которого план и опасался (§0 плана) — только источник оказался не в правах/сессии, а в
  локали формата даты. Исправлено передачей даты в локальном формате (`dd.MM.yyyy` через
  `(Get-Date).ToString('d')` под текущей культурой) — после этого `Next Run Time` показал верное
  время, задача сработала.
- **Замер после исправления (один прогон):**
  - Плановое время: `22:51:00`.
  - `schtasks`-журнал (`Last Run Time`): `22:51:19` → дрейф запуска процесса **≈19 с** от плана.
  - Первая строка лога самого `.cmd` (`FIRED:`): `22:51:22,17` → ещё **≈3 с** на старт `cmd.exe`.
  - `claude -p` (headless, `--model haiku`, тривиальный промт) отработал и вернул ровно `OK`
    (запрошенный формат ответа соблюдён), заняв **≈9 с** до строки `DONE: 22:51:31,24`.
  - Итого от плана до полного завершения: **≈31 с**. `schtasks`-код результата (`Last Result`) —
    `0` (успех).
  - Задача одноразовая, удалена сразу после `schtasks /delete /f` — не оставлена в системе.
- **НЕ проверено этим прогоном (честно фиксируем пробел, не выдаём за покрытое):**
  - Поведение при ЗАБЛОКИРОВАННОМ экране — сессия владельца была активна и разблокирована все
    время теста; специально блокировать экран ради теста означало бы прервать текущую рабочую
    сессию, не сделано. Это именно тот сценарий, который план называет «частым источником тихого
    отказа» — вопрос остаётся открытым.
  - Дрейф на СЕРИИ срабатываний подряд (план предлагал повторный тест с интервалом) — сделан
    только один разовый прогон, а не интервальный.
  - Устойчивость к перезагрузке/сну машины между планированием и срабатыванием.

## 8. Открытые вопросы (нужно решение владельца перед реализацией)

1. ~~Длина активного окна и окна тишины~~ — **разобрано 2026-08-04, см. §2a:** окно и таймаут
   `.lock` — разные масштабы, не путать; рекомендация ORCH — 30 мин активное / 10 мин тишина
   как стартовый дефолт, не откалиброванный по данным. Ждёт подтверждения владельца.
2. ~~HARD_STOP или PING_CONTINUE как режим по умолчанию~~ — **разобрано 2026-08-04, см. §2a:**
   рекомендация ORCH — `HARD_STOP` дефолт, `PING_CONTINUE` explicit opt-in. Ждёт подтверждения
   владельца.
3. ~~Пороговые значения правил SEC~~ — **разобрано 2026-08-05, см. §2a:** порог бюджета —
   медиана `cost_usd`/чат по роли (из `kpi_data.json`) × 2, НЕ единое число на все роли
   (таблица в §2a); порог `/compact` — % контекста через `/context` (не абсолютные токены).
   Оба — стартовые дефолты, B/C/D посчитаны по выборке n=1 (честно оговорено), данные
   устарели (снимок 2026-07-31). Ждёт подтверждения владельца.
4. ~~Для каждой роли (A/B/C/D/L) — «парковать» чат между окнами или закрывать и поднимать
   заново~~ — **разобрано 2026-08-05, см. §2a и обновлённый §4:** дефолт для ВСЕХ ролей —
   `resume + /clear + новое имя`, не буквальная парковка. Опора — прецедент ротации ORCH
   (3 реальных `/clear` + 1 контрпример без `/clear`, оба варианта штатны). `CronCreate`/
   `ScheduleWakeup` сужены до in-session самопробуждения внутри одного окна, не между-окон
   механизм. Ждёт подтверждения владельца.
5. ~~Результат эмпирического теста Планировщика Windows~~ — **частично закрыто 2026-08-04
   (см. §7a):** базовый механизм (разовая задача → headless `claude -p`) работает, дрейф
   старта ≈19-22 с + время самой claude-сессии. **Остаётся открытым:** поведение при
   заблокированном экране, дрейф на серии срабатываний, устойчивость к перезагрузке/сну —
   ни одно из этого не проверено эмпирически, только базовый факт срабатывания.

## 9. Порядок реализации после одобрения

1. ~~Тест надёжности Планировщика Windows (маленькая разовая задача).~~ **Частично выполнено
   2026-08-04, см. §7a:** базовое срабатывание подтверждено реальным прогоном (headless
   `claude -p` через `schtasks`, дрейф ≈19-22 с + время сессии). Остаётся не проверено:
   поведение при заблокированном экране, дрейф на серии срабатываний подряд, устойчивость к
   перезагрузке/сну.
2. `coordination/MODE.flag`, `coordination/STOP.flag`, `coordination/QUESTIONS.md` — простые
   файлы-сигналы, минимальный код.
3. Регистрация сессии SEC (NICKS/BOARD/sessions/SEC.md) по обычной процедуре CHARTER §1.
4. `coordination/schedule.json` + логика сбора заявок/публикации SEC (§3).
5. CHARTER §13 — короткая выжимка + ссылка на этот файл (сам файл документом-источником и
   остаётся, как `OWNERSHIP.md`/`ORCHESTRATOR.md` для своих тем).
6. Расширение KPI-таймлайна (§6) — только когда есть что показывать.
