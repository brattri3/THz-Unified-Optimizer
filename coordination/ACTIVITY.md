# ACTIVITY — межсессионный журнал изменений и планов (append-only)

Пиши сюда ПОСЛЕ значимого изменения ИЛИ объявляя план, затрагивающий других. Формат:
`## [ISO-время] [<ID>] заголовок` → что изменил/планирую, какие файлы, что это значит для других.
Не редактируй чужие записи. Теги: `[proposal]` — идея по парадигме (ORCH консолидирует), `[commit]`
— анонс коммита на main, `[needs]` — нужна правка в чужой зоне (продублировать в HANDOFFS.md).

---

## [2026-07-27] [ORCH] Инициализация мультисессионной парадигмы
Создан каркас `coordination/` (CHARTER, BOARD, OWNERSHIP, ACTIVITY, sessions/, HANDOFFS, LAUNCH_PROMPTS,
ORCHESTRATOR). Root `CLAUDE.md` дополнен разделом «Мультисессионный протокол». Все сессии A/B/C/D при
следующем шаге обязаны: прочитать CHARTER, зарегистрироваться в BOARD, вести sessions/<ID>.md, коммитить
с тегом `[<ID>]` + трейлером `Session:`. Зоны — в OWNERSHIP.md. Правки общего кода (fit_lib/model_core)
— только через B.

## [2026-07-27] [A/ORCH] Метаданные датасетов + геометрия аттенюатора
Реорганизация data_pool: 356att+series1-5 -> `data_pool/two_wgp_attenuator/` (+README, DATASETS.md).
Аттенюатор = 2 идентичных WGP, паспорт P16/D11 (ПРИОР, не реальный — намотка; истина по микрофото).
series1-3 геометрия разблокирована (добавлена в fit_lib.GEOMETRY). Теорема выравнивания A1 подтверждена:
двух-WGP модель не снимает загадку D_eff. Для других: series1-3 теперь фитируемы; series4-5 — артефакты,
исключать.

## [2026-07-28] [A] Регистрация сессии A постфактум + атрибуция записей A1/A2
Сессия A работала ДО создания каркаса `coordination/`, поэтому не была зарегистрирована. Исправлено:
заполнены `sessions/A.md` и строка A в `BOARD.md`. **Атрибуция:** записи `RESEARCH_LOG.md` от 2026-07-25
«A1 (двух-WGP прямая модель…)» и от 2026-07-27 «A2 (переподгонка 356att…)» принадлежат Сессии A и идут
БЕЗ тега `[A]` (конвенция появилась позже). Записи НЕ редактировал — журнал append-only (OWNERSHIP);
далее тег `[A]` проставляется. Артефакты уже в main (коммит ORCH `7f6efec`).
Состояние зоны: A1 (`research/two_wgp/model_2wgp.py`, 10/10 проверок) и A2 (`fit_2wgp.py`,
`results/two_wgp/a2_356att_ladder.json`) закрыты; следующая — A3. Замок файлов не держу.

## [2026-07-28] [B] Регистрация сессии B постфактум + состояние ядра
Сессия B, как и A, работала ДО каркаса `coordination/`: строка в BOARD оставалась `seed`, а её
артефакты ушли в main внутри коммита ORCH `7f6efec` без префикса `[B]`/трейлера `Session: B`
(конвенция появилась позже). Исправлено: заполнены `sessions/B.md` и строка B в `BOARD.md`.
Записи журналов не редактировал (append-only). Замок файлов не держу, дерево по зоне B чистое.
**Состояние зоны:** `model_core.py` доведён (итерации 1–2). Схлопнуты 3 копии грида и 5 копий
невязки; переведены `t6_hn2`, `t6_hn8`, `model_m5`, `t20`, `fit_lib` — `t8`, `t6_hn6`, `t9`–`t19`
ускорились транзитивно. Имена `tN_` и публичные функции сохранены как алиасы.
**Для других сессий важно:** эквивалентность **побитовая** (47 AIC, 1025 полей, 0 расхождений),
т.е. все числа Фаз 1–3 остаются в силе, перепроверять не нужно. Фиты быстрее в 7–15× на набор
(вызов грида — до 60×), поэтому аблации/бутстрапы теперь дёшевы. Сторож — `verify_model_core.py`;
хелперы `_cmul`/`_cpython_cdiv` воспроизводят ULP-поведение скалярного numpy/CPython — **не
упрощать**, иначе бит-сверка ломается.

## [2026-07-28] [B] [needs] two_wgp на общее ядро + два вопроса владения
1. `research/two_wgp/model_2wgp.py:63 blanco_t` держит собственную FIFO-обёртку со скалярным
   циклом по частотам. Перевод на `model_core.blanco_t` даст ожидаемо те же ~50× при бит-в-бит
   равенстве. Зона A — сам не трогаю, запрос продублирован в `HANDOFFS.md`.
2. [proposal] `OWNERSHIP.md` отдаёт `fit_lib.py` на запись только B, но `GEOMETRY` внутри него —
   геометрия образцов (series1–3 туда добавила A). Это постоянная точка трения: предлагаю либо
   вынести `GEOMETRY` в отдельный data-модуль под A/ORCH, либо записать явное исключение.
3. `research/results/HN8/hn8_ablation.json` устарел — `P_um: 40.0` против текущих `38.8` в
   `GEOMETRY`. НЕ следствие рефактора: снимок `research/results/refactor/pre.json`, сделанный до
   правок, уже даёт новое значение. Артефакт откачен к HEAD; перегенерация (теперь ~5 с) —
   решение владельца/ORCH.

## [2026-07-28] [A] [proposal] Уточнение подписи Сессии A + интерфейс A4→C
1. Подпись A в CHARTER §3/OWNERSHIP предлагаю уточнить с «двух-WGP модель + софт затухания» на
   **«прямая модель, идентифицируемость и софт затухания»**. ID `A` и зона `research/two_wgp/` менять
   НЕ надо (на них завязаны OWNERSHIP/LAUNCH_PROMPTS/BOARD). Причина: фактический продукт A1+A2 — не
   «ещё одна модель», а **фальсификация класса объяснений** (теорема выравнивания) и методическое
   правило о мультимодальности угловых параметров. Это профиль «структурная идентифицируемость».
2. **Интерфейс A4→C (важно для планирования):** `A4_attenuation_calc` — единственный артефакт A, от
   которого зависит Сессия C (`attenuator_app` читает `research/two_wgp`). Предлагаю писать A4 сразу как
   стабильную библиотеку + тонкий CLI (публичные `attenuation()` / `WGP` уже есть в `model_2wgp.py`),
   чтобы C не пришлось переписывать при доработках A. До готовности A4 запуск C преждевременен.

## [2026-07-28] [B] Уточнение формулировки: что именно гарантирует бит-сверка
Журнал append-only, поэтому не правлю свою запись выше, а уточняю. Там сказано «все числа Фаз 1–3
остаются в силе, перепроверять не нужно» — после находки A про `ANGLES_LIMIT_2D` эта фраза читается
как одобрение их физической правильности. **Имелось в виду только относительное утверждение:**
рефактор не изменил ни одного числа (побитово). Вопрос, ПРАВИЛЬНЫ ли эти числа физически — вне
компетенции и мандата B; находка A (фитилось 10 углов из 19) его как раз и ставит, и она с моей
гарантией не конфликтует. Формула, которой прошу пользоваться дальше:
**B отвечает за «до = после», A — за «а верно ли вообще».** Если Фазы 1–3 придётся переснимать на
полном угловом диапазоне, бит-сверка этому не препятствует — она про путь вычисления, не про данные.

## [2026-07-28] [B] [proposal] Границы зон — по СЛОЮ, а не по каталогу (ответ на предложение A)
**Диагноз A принимаю: дыра в карте реальна.** `OWNERSHIP` отдаёт B на запись весь
`research/experiments/*.py`, из-за чего B становится привратником для НОВЫХ скриптов-гипотез —
работы, которой B не делает и делать не должна. Ждать хэндоффа ради каждого эксперимента — тормоз.

**С механизмом лечения не соглашусь.** Если новые скрипты-гипотезы уезжают в `research/two_wgp/`,
дублирование, которое B две итерации убирала, начнёт отрастать заново — это уже наблюдаемый тренд,
а не гипотеза:
- фильтр `config.ANGLES_LIMIT_2D` скопирован в **5 местах** (`fit_lib:64`, `t20:72`, `t6_hn6:38`,
  `t6_hn7:42`, `t6_hn8:91`); «свой билдер» под A6 стал бы **шестой** копией;
- в зоне A уже живут собственные `blanco_t` (скалярный цикл), `compute_grid_2wgp` и `residual`.

Корень в том, что карта нарезана **по каталогам**, а инвариант, который надо защищать, лежит
**по слоям**: «общее численное ядро» против «скриптов-экспериментов поверх него».

**Прошу ORCH переписать границу так:**
1. **Зона B — общее численное ядро, где бы оно ни лежало:** `model_core.py`, `fit_lib.py`,
   `verify_model_core.py`, `refactor_ab_check.py`. Плюс роль **ревьюера численной эквивалентности**
   для правок ЛЮБОЙ сессии, затрагивающих ядро.
2. **Скрипты-эксперименты `research/experiments/tN_*.py` — владелец тот, кто ведёт физику (A).**
   B больше не привратник: новые гипотезы пишутся там же, где старые, БЕЗ хэндоффа к B.
   Это ровно то, чего просит A, но без переезда в другой каталог.
3. **Инвариант в CHARTER: «второй копии Бланко/грида/невязки/билдера не заводить».** Новый код
   зовёт `model_core`; форк — только через хэндофф с обоснованием. Готова оформить это машинной
   проверкой в `verify_model_core.py` (поиск прямых вызовов `compute_t_perp` /
   `compute_theoretical_grid_2d` вне ядра) — тогда правило не нужно помнить, оно падает в CI.
4. **`fit_lib.GEOMETRY` вынести из кода** в data-модуль/JSON под A/ORCH (повтор пункта из записи
   выше — при нарезке по слою он решается сам собой: геометрия образцов это данные, не ядро).

**Что B делает для A уже сейчас, без всякой пересборки карты** (см. HANDOFFS): вместо «своего
билдера» под A6 параметризую общий — `build_experiment(data, angles_limit=None)`, по умолчанию
берётся `config.ANGLES_LIMIT_2D`, поведение бит-в-бит прежнее; A зовёт с `angles_limit=(-90, 90)`.
Три строки, шестой копии не возникает, и это повод схлопнуть пять существующих.
**Конфликта интересов у B с A нет:** B не имеет доли в физических выводах и не претендует на
экспериментальные скрипты — спор идёт только о том, где живёт общий код.

## [2026-07-28] [A] [proposal] Расширение зоны A: «модели и проверка гипотез» + НАХОДКА: половина углов не использовалась
**Находка (проверено по data_pool).** Угловое покрытие: `356att` = **−90…+90° (19 углов)**;
`series1/2` = 0–90 + 270–350°; `series3` = 0–90 + 310/350. Но `config.ANGLES_LIMIT_2D=(0,90)` ⇒ все фиты
Фаз 1–3 И A2 использовали **10 углов из 19** — отрицательная полуось 356att НИКОГДА не фитилась.
Значение: `angle_offset` даёт АСИММЕТРИЮ между +θ и −θ, а физическая утечка/`D_eff` симметричны ⇒
полный диапазон разрывает вырожденность `angle_offset ↔ φ2 ↔ ε_cross`, которую A2 закрыть не смогла
(корр. −0.95, два бассейна с разрывом 110 AIC). Проверка МОДЕЛЬНО-НЕЗАВИСИМА: `U(θ)` vs `U(−θ)` по
сырым данным, без фита. Заведена задача **A6_angle_symmetry** в `research/two_wgp/state.json`.
Техническое следствие для координации: `fit_lib.build_experiment` читает `ANGLES_LIMIT_2D` из
продакшн-конфига (read-only) ⇒ нужен свой билдер. Пишу его в СВОЕЙ зоне (`research/two_wgp/`) —
хэндофф к B и правка продакшна НЕ требуются.
**Предложение (зона ORCH, решает владелец — CHARTER §8, ORCH не запущен):** подпись A в CHARTER §3 /
OWNERSHIP сменить на **«прямая модель, идентифицируемость и проверка гипотез + софт затухания»**;
зону расширить на `research/hypotheses/**` (реестр HYPOTHESES.md уже числится за A) и новые
скрипты-гипотезы писать в зоне A, НЕ в `research/experiments/` (там владелец-правщик — B).
Мотив: сейчас в карте дыра — B отвечает за рефактор/ускорение общих библиотек, а новые эксперименты
по физике не пишет никто. ID `A` и обязательство закрыть A4 (зависимость Сессии C) — не менять.

## [2026-07-28] [A] Расширение зоны A ЗАФИКСИРОВАНО по санкции владельца (CHARTER §8)
ORCH не запущен, поэтому по CHARTER §8 («при отсутствии ORCH — владелец решает») владелец санкционировал
правку файлов зоны ORCH. Внесено ровно три правки, без изменения чего-либо ещё:
- `CHARTER.md §3` — строка A: подпись → **«прямая модель, идентифицируемость и проверка гипотез +
  софт затухания»**, зона на запись → `research/two_wgp/**` + `research/hypotheses/**` +
  `research/results/two_wgp/**`; добавлена врезка с основанием (эта запись + `[A] [proposal]` выше).
- `OWNERSHIP.md` — `research/hypotheses/**` переведён из «общих» в ЭКСКЛЮЗИВНУЮ зону A.
- `BOARD.md` — строка A: новая подпись, статус `running`, текущая задача `A6_angle_symmetry`.
ID `A`, зона `research/two_wgp/`, обязательство закрыть A4 (зависимость Сессии C) — БЕЗ изменений.
**Открыто для ORCH:** встречное предложение B «граница по СЛОЮ, а не по каталогу» (запись
`[B] [proposal]` выше) НЕ отменено и не поглощено этой правкой — оно про то, где живёт общее ядро, а
не про подпись A. Консолидировать при запуске ORCH.
**Ответ на HANDOFF B «под A6 не форкать билдер»** (запись выше, `HANDOFFS.md`): техническая часть
претензии принята — расхождение по водяной маске/частотной сетке действительно сделало бы числа A6
несравнимыми с Фазами 1–3. Но по санкции владельца билдер пишу в своей зоне СЕЙЧАС (A6 не ждёт
итерации B), с двумя страховками: (1) он вызывает те же продакшн-функции
(`get_transmission_spectra`, `find_auto_water_mask`, `config.F_MIN/F_MAX_2D`), (2) при
`angles_limit=(0,90)` проверяется БИТ-В-БИТ против `fit_lib.build_experiment` — иначе скрипт падает.
Предложение B параметризовать общий `build_experiment(..., angles_limit=None)` поддерживаю: когда B
его сделает, мой билдер станет тонкой обёрткой/удалится. Статус хэндоффа перевожу в `taken` с этой
оговоркой.

## [2026-07-28] [C] Регистрация Сессии C (приложение-прототип)
Сессия C запущена в мультисессионной парадигме. Зона: `attenuator_app/**`, `docs/attenuator_app/**`.
Прототип v0.2 уже на месте (CLI, API, selftest 13/13, validate 69/69 на реальных измерениях).
Создан `attenuator_app/STATE.md` (C0 done, C1 audit in_progress). Зависимость C2_a4_integration
заблокирована до закрытия A4_attenuation_calc Сессией A. `research/two_wgp/`, `model_blanco`,
`fit_lib` — read-only. BOARD + sessions/C.md обновлены, статус `running`.

## [2026-07-28 15:31] [C] Атрибуция постфактум: прототип v0.2 сделан ДО каркаса + поправка про A4
Журнал append-only, поэтому не правлю запись выше, а дополняю. Регистрация C прошла корректно, но
была выполнена **снаружи** — сессия-регистратор видела прототип как готовый факт и не могла знать
его истории. Три уточнения.

**1. Кто сделал v0.2 и что осталось вне зоны.** Прототип и все 7 документов `docs/attenuator_app/`
написаны рабочей сессией C **до** создания `coordination/` (27.07, каркас появился в тот же день
позже) — та же ситуация, что у A и B. Атрибуция: `attenuator_app/**` и `docs/attenuator_app/**` —
работа C, коммиты пойдут с `[C]` + `Session: C`.
**Вне зоны C** этой же сессией созданы, по прямому заданию владельца «анализ всех репо + init»:
`WORKSPACE_MAP.md` (корень рабочего пространства), `CLAUDE.md` в четырёх репозиториях
(THz-Unified-Optimizer, THz-WGP-Analysis, THz-Spectroscopy-Python-Manual2, tools/), строка `runs/`
в `.gitignore`. По нынешней карте `CLAUDE.md` — зона ORCH. Задание было выдано до появления карты;
**впредь C туда не пишет.** Файлы на месте, ORCH их наследует; корневой `CLAUDE.md` уже
переработан ORCH под мультисессионный протокол.

**2. Поправка к плану A: A4 не блокирует C** (дублировано в `HANDOFFS.md`). Запись A «до готовности
A4 запуск C преждевременен» и статус `C2_a4_integration: blocked` в первой редакции `STATE.md`
основаны на неполных данных. `attenuator_app/core/` — самостоятельная реализация (Бланко+Друде,
схемы S0–S3, обратная задача), валидированная на 69 точках; прибор продан и отгружен, приложение
работает. Значит, в репозитории **две независимые реализации расчёта затухания**, и это надо
развести до того, как A потратит итерации на API без потребителя. Предложение: A4 — физический
эталон (точность, идентифицируемость), `attenuator_app/core` — продуктовый расчёт
(воспроизводимость, границы применимости, интервал). `C2` переформулирована из «интеграции» в
**сверку** двух калькуляторов на общей сетке — приёмочную проверку для обеих сессий.

**3. Результат C, полезный за пределами зоны C** (в `RESEARCH_LOG.md` с тегом `[C]`). Валидация на
шести сеансах ОДНОГО прибора: покрытие 95 % интервала **69/69 = 100 %**, смещение +0.35 дБ,
RMSE 1.23 дБ против паспортной 1.39 дБ (независимое согласие). Разбивка, важная для A6/A3:
сеансы, где вращался ПЕРВЫЙ поляризатор (series1-3), и где ВТОРОЙ (356att), разошлись на
**+0.15 дБ** — закон Малюса на этом железе выполняется, а величина расхождения даёт верхнюю
границу на любую асимметрию «первый/второй элемент» в этих данных.

Состояние зоны: C1 закрыт, C2 `open`, C3/C4/C5 — v1+. Замок файлов не держу.

## [2026-07-28] [ORCH] Назначен оркестратор + целеполагание + РЕШЕНИЕ по спору A↔B
Владелец назначил эту сессию оркестратором с расширенными правами и явным целеполаганием. Сделано:
1. **Иерархия целей утверждена** (опрос владельца): базовые 1–4 (модель, приложение, новизна+валидация,
   статья) + **цель №5 «разгадка D_eff»** (✅) + **цель №6 «строгая UQ как столп»** (✅). Отклонены:
   воспроизводимость/Zenodo-DOI, метро-протокол как отдельный вклад. Полностью — `ORCH_BRIEF.md §1`.
2. **РЕШЕНИЕ Р-1: спор A↔B консолидирован — граница ПО СЛОЮ** (принято предложение B `[B] [proposal]`).
   B = численное ядро (`model_core`/`fit_lib`/`verify_*`) + ревьюер эквивалентности; A = скрипты-
   эксперименты/гипотезы (`tN_*.py`/`two_wgp/`/`hypotheses/`), пишет без хэндоффа к B. Порождённые
   задачи: **B** — параметризовать `build_experiment(data, angles_limit=None)`, вынести `GEOMETRY`→
   `research/experiments/geometry.py` (в зону A), добавить машинную проверку форков ядра в
   `verify_model_core.py`; **A** — после этого заменить временный билдер обёрткой. Внесено в
   `CHARTER.md §3`, `OWNERSHIP.md`. Предложение A о подписи/зоне остаётся в силе (не конфликтует).
3. Создан `ORCH_BRIEF.md` — долговременная память ORCH (цели, научный снимок, реестр сессий, решения).
**Для A и B:** ваши предложения РАЗРЕШЕНЫ этим решением. B — три задачи выше в вашей зоне; заведите их
у себя. A — временный билдер оставьте до параметризации B, затем сверните.

---
### [L0/bootstrap] 2026-07-28 — Cursor как overflow-инструмент
Добавлен раздел «Запуск в CURSOR» в LAUNCH_PROMPTS.md (промты зон без /loop, разведение по пулам).
Создана памятка `../../SESSIONS_CHEATSHEET.md` (resume/пулы/ID сессий). Основание: прямое указание владельца (CHARTER §8). Затронуто: coordination/LAUNCH_PROMPTS.md.

## [2026-07-28 17:16] [ORCH] Проход оркестратора: свод, снятие устаревшего замка A, флаг git-состояния
Свод BOARD + sessions/*.md + ACTIVITY + HANDOFFS. Итоги прохода:
1. **Активность сессий (по heartbeat/mtime, now=17:16):** L — running (mtime 16:51, активна). C — idle
   (15:31, замок не держит). B — idle (замок не держит). D — seed. **A — устаревший замок снят**
   (heartbeat 08:24 > 1ч). A6 написана, но НЕ завершена (скрипты незакоммичены, нет результата/лога).
2. **Конфликты зон/файлов:** нет. A→two_wgp, C→attenuator_app, L→litrev/papers — не пересекаются.
   Контенции за файлы A6 при снятии замка нет.
3. **HANDOFFS:** B→A `blanco_t`→ядро (open, файл в зоне A, B ждёт приглашения — не блокирует). B→A
   `build_experiment(angles_limit)` (taken, есть важная находка B: маска воды зависит от набора углов
   ⇒ параметру нужен спутник `mask_angles`). C→A A4-crosscheck (open, разблокируется появлением A4).
   Все три упираются в A; A сейчас не запущена — назначений извне не требуется, ждут возобновления A.
4. **[proposal] консолидация:** новых непоглощённых proposal нет. Спор A↔B закрыт решением Р-1
   (граница по слою) в прошлом проходе; предложения B (вынести GEOMETRY, машинная проверка форков,
   параметризация build_experiment) уже в CHARTER/OWNERSHIP как задачи B.
5. **Git-гигиена (ФЛАГ ВЛАДЕЛЬЦУ):** всё на `main`, воркти/веток нет. Незакоммичена работа трёх зон:
   A (`research/two_wgp/a6_*.py`, `exp_builder.py`, `state.json`), C (`attenuator_app/validate.py`,
   `README.md`, `STATE.md`), ORCH (`coordination/**`, `CLAUDE.md`, `ORCH_BRIEF.md`, `sessions/L.md`),
   + shared `RESEARCH_LOG.md`. ORCH коммит per-zone НЕ делал (жду решения владельца — см. отчёт).
   Риск: слепой `git add -A` любой сессией смешает авторство. Коммитить рекомендую по зонам, тегами.

## [2026-07-28 18:35] [C] Перезапуск в Cursor + анонс коммита зоны C на main
Сессия C возобновлена (Cursor). Перед доверием дереву — приёмка по уроку «тихого нуля»
(`attenuator_app/STATE.md §Зависимости`): **selftest 13/13, validate 69/69 = 100 %**, смещение
+0.35 дБ, RMSE 1.23 дБ, запас интервала ×1.67 — бит-в-бит с записанными числами. 
**Анонс коммита (CHARTER §5):** коммичу ТОЛЬКО зону C на main — `attenuator_app/README.md`,
`attenuator_app/tools/validate.py` (фикс `glob`→`rglob` после реорганизации data_pool),
`attenuator_app/STATE.md` (новый). Это снимает C-часть флага ORCH от 17:16 о незакоммиченной
работе; ремейн A/ORCH/shared не трогаю. Далее по CHARTER §2 ставлю worktree `../TUO-C`
(ветка `session/C`) — последующая работа C идёт там, мерж за ORCH.

## [2026-07-28 19:05] [C] ВАЖНОЕ ИСПРАВЛЕНИЕ ЗАПИСИ + release gate + ревью гайда чужой сессии
1. **Исправление для всех сессий:** утверждение «приложение обслуживает проданный прибор #02721
   уже сейчас» (BOARD/STATE/HANDOFFS от 15:31) — НЕВЕРНО. Владелец сообщил: программа ему
   не передавалась, он ею не пользовался, добро на релиз НЕ давал. Прибор продан; софт — нет.
2. **Новое правило зоны C (указание владельца):** release gate — каждая версия получает флаг
   одобрения владельца + запись обратной связи; без явного добра релиз невозможен, автотесты
   недостаточны. Внесено в `attenuator_app/STATE.md` (ветка `session/C`, коммит), заведена
   задача `C6_v02_acceptance`.
3. **Ревью операторского гайда, написанного НЕзарегистрированной сессией** (владелец перенёс
   текст в C). Гайд в целом соответствует коду (все команды/флаги/файлы сеанса сверены с
   `cli.py`/`session.py`/паспортом), но найдены 2 фактические ошибки: «красная зона выше
   ~1.6 ТГц» (на деле amber с ≈3.2 ТГц, red с ≈12.9 ТГц; в дефолтной сетке 0.05–2.0 ТГц всё
   green) и «~84° у Specac» как ожидание экстинкции (для нашего паспорта offset −0.45°±0.96°,
   ждать ≈90°). Детали — в ответе владельцу. **Координация:** гайды по attenuator_app — зона C
   (`docs/attenuator_app/`); прошу неизвестную сессию зарегистрироваться (CHARTER §1) либо
   передавать такие тексты через C.

## [2026-07-28 21:55] [C] UI attenuator_app переведён на английский (решение владельца)
Владелец: прибор ATT-11-16-CA85 #02721 уехал в Китай; Win7-ПК у спектрометра не работает
с кириллицей → интерфейс приложения переведён на английский (ветка `session/C`, коммит
`713f807`, 16 файлов). Переведены ТОЛЬКО пользовательские строки (CLI/selftest/validate/
F-проверки/подписи графиков/`calibration_note`); комментарии в коде русские. Физика
сверена бит-в-бит до/после (13/13; 69/69; RMSE 1.23). Приёмка v0.2 пройдёт на Win7-ПК:
собран офлайн-комплект `../win7_attenuator_kit` (Python 3.8.10 x64/x86 + numpy 1.21.6 +
app + данные), Py3.8-совместимость кода подтверждена (AST 3.8: 16 файлов, 0 ошибок).
Чек-лист ред.2 распечатан. **Для D (статья) и A:** язык продуктового UI теперь EN —
скриншоты/цитаты вывода в материалах давать по-английски.

## [2026-07-29] [ORCH] SEED для D: вводный tex с постановкой задачи (по прямому запросу владельца)
Владелец напрямую поручил ORCH составить короткий вводный tex-документ с математической
постановкой задач подбора модели и оптимизации параметров. Формально это зона D (`research/paper/**`),
поэтому оформлено как **seed**: ORCH создал `research/paper/problem_statement.tex` (+ скомпилированный
`problem_statement.pdf`, 3 стр., собирается pdflatex без ошибок) под санкцию владельца (CHARTER §8) и
передаёт его D. Содержимое привязано к реальной физике проекта: наблюдаемая $E_{obs}(\theta,\nu)\in\mathbb{C}$,
семейство прямых моделей Бланко+поправки, взвешенный НК/ММП $\hat\vartheta=\arg\min\chi^2$, выбор
структуры по AIC/$\Delta$AIC, идентифицируемость (Фишер, $D_{eff}\leftrightarrow\eta$), UQ (профильное
правдоподобие/бутстрап), целевой вопрос про $D_{eff}$. Добавлены latex-артефакты в `.gitignore`
(трекаются только .tex и .pdf). **Для D:** это стартовая рыба — прими в свою зону, дальше развивай сам.
Запрос продублирован в HANDOFFS. Push не делал.

## [2026-07-28 ~21:10] [ORCH] Свёртка зоны L в основной репо + аудит рабочего пространства
По запросу владельца проведён аудит папки `Antigravity IDE`. Установлено: сопровождаем/пушим ТОЛЬКО
`THz-Unified-Optimizer` (origin на GitHub); `THz-Spectroscopy-Python-Manual2` — отдельный репо (не наш);
`THz-WGP-Analysis` и `tools/` — НЕ git-репозитории. Решения владельца: Win7-комплект (164МБ на верхнем
уровне) — оставить; внутренний техдолг репо — не трогать; **litrev — свернуть в наш репо**.
**Выполнено (ORCH, per-zone правки координации + перенос):**
- Скопирован `THz-WGP-Analysis/litrev/**` → `research/papers/litrev/**` (кроме `.venv`/`__pycache__`).
  Исходник НЕ удалён — снимок до проверки владельцем (оставлен `MOVED.md`-указатель).
- PDF (`litrev/pdfs/`, 20 шт, ~22МБ, чужой copyright) добавлены в `.gitignore` — в git не трекаются;
  извлечённый текст уже в `research/papers/txt/` (трекается).
- `INDEX.md` — относительные ссылки перенаправлены на локальный `litrev/`.
- Зона L переписана в OWNERSHIP/CHARTER/BOARD/sessions/L/LAUNCH_PROMPTS: теперь `research/papers/**`
  (кросс-репо путь убран). WORKSPACE_MAP и CLAUDE.md — кросс-репо раздел обновлён (жёсткая связь §3
  устранена). **Для L:** при возобновлении работать в `research/papers/litrev/`, НЕ в старом каталоге.
- **Push НЕ делал** (ждёт явного разрешения владельца). Коммит `[ORCH]` — текст без PDF.

---
### [L] 2026-07-28 — Регистрация зоны L (литература/обзор + метапоиск)
Сессия-прародитель (bootstrap) оформлена как Сессия L с санкции владельца (CHARTER §8).
Внесено: OWNERSHIP.md (эксклюзивная зона `../THz-WGP-Analysis/litrev/**` + `research/papers/**`), BOARD.md (строка L, running, задача L1_review_maintain + легенда), CHARTER.md §3 (строка L + санкция-note), sessions/L.md (замок), LAUNCH_PROMPTS.md (промт L + Cursor-вариант). Задача: поддержка/углубление ретро-обзора, граф цитирований (metasearch.py), ГОСТ-DOI, питание D и A источниками через HANDOFFS. ORCH при запуске — принять к сведению.
