# BOARD — сводка по ролям (одна строка на роль)

> **Разделена 2026-08-15.** Подробности каждой роли — в её файле `coordination/roles/<ID>.md`;
> он же единственный, что роль читает при старте. Здесь — экран для ORCH и для владельца: где что
> стоит и чего ждёт. Раздувать эту таблицу не надо: детали идут в файл роли, историю пишет
> `ACTIVITY.md`.
>
> **Кто сейчас работает — `claude agents --json`**, не этот файл.

| Роль | Где стоит | Чего ждёт | Файл |
|---|---|---|---|
| **A** модель, гипотезы | разбор кода C закрыт; A22–A25: двухэтапная схема, `η` без модели, `τ_leak` без пина `D_eff` | решений владельца `A-4…A-7`; очередь — `research/two_wgp/BACKLOG_A.md` | `roles/A.md` |
| **B** техническая гигиена | дефект `p_max_um` не исправлен на `main` (ветка `session/B-prod-fix` от 08.08 — **не кандидат на мерж**, см. ⚠ ниже) | разбора дефекта `noise_floor` = `4/π` от A (внесён в файл роли 17.08); доводки прогонов; вынос `GEOMETRY` | `roles/B.md` |
| **C** приложения | `track_viewer` фазы 1–3 сданы, приёмка 27/27; аттенюатор v0.3 смержен. **26.08: C9 принят и доставлен на ПК** — ветка `claude/cw-attenuator` (10 коммитов, сервисное приложение на два ротатора) смержена ORCH и выведена в локальный `main` основного каталога fast-forward, приёмка зелёная, `service_gui` импортируется с настоящим tkinter 8.6, **не пушено** (main +12 к origin — так и задумано). Промт запуска — `LAUNCH_PROMPTS.md` | **запуска `c-app` на живую отладку GUI** (в облаке не было tkinter) и прогона `service_limits`/П0…П4 на серии `s2` вместо синтетики — задание `HANDOFFS.md` `ОТ ORCH К C` 26.08. Дальше — остатки сводного задания 17.08 (`C-6`, `C-2`) + прогон владельца у прибора | `roles/C.md` |
| **D** статья и курс | курс 76 страниц завершён; статья — скелет, ноль из пяти рисунков | чтения курса владельцем; свода чисел от A для §4.3 | `roles/D.md` |
| **L** литература | Blanco 1986 опознан, граница `d/p < 0.5` найдена | **новая задача В-19** — норма определения границы в микроскопии; фиты A стоят до ответа. Плюс два пункта от ORCH 17.08: `doi_audit.py` даёт ложные `DEAD` на Бланко; два PDF трекаются в git | `roles/L.md` |
| **P** презентации | два недельных отчёта сданы | утверждённой геометрии — иначе `fig1` переделывать дважды | `roles/P.md` |
| **ORCH** координация | 17.08: хэндофф `4/π` доведён до `roles/B.md`; сплошной обход `open`-хэндоффов; `state.json` и точка входа поправлены; остатки замков вычищены. 23.08: обзор парадигмы; две истории git разведены — `history/pre-2026-08-13` на origin. **26.08: локальный контур синхронизирован** — основной каталог был −30 от `origin/main`, подтянут; C9 смержен и принят | решения по отложенному списку (`reports/PARADIGM_REVIEW_2026-08-23.md` §7); **разрешения на пуш** смерженного C9; решения по `session/C` (см. ⚠ ниже) | `roles/ORCH.md` |
| **orch-cloud** облачный ORCH | 23.08 заведён: аудит, метрики, мержи, дежурство. `claude agents` его не видит — арбитраж живых процессов остаётся за локальным `orch`. 24.08: тег `history/pre-2026-08-13` подтверждён на origin; находки про git-креды облака (два аккаунта, ограничение по refs/tags) внесены в `ORCHESTRATOR.md` | 24.08 поднята вторая облачная сессия `orch-cloud` (`session_016gk8oGRgEBxM9YypDwXzaS`) — аудит git-инструментария, предложение по следующему этапу парадигмы, статус `c-app-cloud` | `roles/ORCH.md` |
| **c-app-cloud** облачная C | **работа сдана и закрыта 26.08.** Отчёт есть (`docs/artifacts/C_service_two_rotators.md`), 10 коммитов приняты ORCH. Упёрлась в границу контура: tkinter в контейнере нет — GUI не проверен живьём, серию `s2` она не видела (появилась позже) | ничего; работа передана **десктопной** сессии C | `roles/C.md` |

> ⚠ **`session/B-prod-fix` — не «несмерженная ветка», а вторая история проекта.** Общего предка
> с `main` нет (`git merge-base` пуст), обычный мерж невозможен. Под ней 180 коммитов всех семи
> ролей за 14.07…08.08 — на `main` их нет.
>
> **Защита есть:** 23.08 заведена вторая ссылка `history/pre-2026-08-13` на тот же коммит
> `ddbe0e7`. Теперь историю держат две независимые ветки, и удаление любой из них ничего не
> уничтожает. Обе не удалять без нужды. Разбор — `reports/GIT_HISTORY_2026-08-23.md`.
>
> ⚠ **`session/C` (каталог `TUO-C`) — кандидат в мёртвые.** С 17.08 не двигалась, своих коммитов
> нет вовсе (`0` впереди `origin/main`, `40` позади). Заводилась под изоляцию сессии C, но C
> работает в облаке и в основном каталоге, так что по факту это третья копия репозитория без
> собственного содержимого. Решение владельца: подтянуть до `origin/main` и держать дальше — или
> признать мёртвой и убрать из `WORKSPACE_MAP.md`. ORCH сам не трогал: в дереве висит
> ` M attenuator_app/passports/validation.png`.
>
> ⚠ **Хук бюджета даёт ложное срабатывание на Windows.** `.claude/hooks/check-context-budget.sh`
> меряет `wc -c`, а в локальном контуре файлы лежат с CRLF — лишний байт на строку. Шесть из семи
> `roles/*.md`, ужатые в облаке ровно под 2400, здесь показывают 2418–2437 и все считаются
> нарушителями. В LF они в норме; лимит верен, ошибается измерение. Правка — `[ORCH]` 26.08.

## Что держит проект прямо сейчас

Четыре вещи, и все четыре — действия владельца, а не сессий:

1. **Приёмка у прибора** — единственная цель, закрываемая целиком.
2. **Ответ по конвенции границы диаметра** (задача зоны L) — от него стоят фиты, `§4.3` и `Fig.4`.
3. **Ответы `A-4…A-7`** — `A-6` (состав модели для `D_eff`) держит `§4.3` и `Fig.4` со второй
   стороны: разброс от выбора варианта сравним со всей «загадкой `D_eff`».
4. **Чтение курса** — приоритет «восстановить контроль над физикой».

Решения владельца — `QUESTIONS.md`. Межролевые запросы — `HANDOFFS.md` (искать поиском, не читать
целиком).
