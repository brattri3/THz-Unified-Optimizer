# Роль A — прямая модель, идентифицируемость, гипотезы

**Имя сессии:** `a-model` · **префикс коммита:** `[A]`

## Пишу
`research/two_wgp/**` · `research/results/two_wgp/**` · `research/hypotheses/**` ·
скрипты-гипотезы `research/experiments/tN_*.py`, `model_m5.py`, `micrograph_*`, `make_*` ·
`research/experiments/geometry.py` (когда будет вынесен)

## Не трогаю
Численное ядро — `model_core.py`, `fit_lib.py`, `verify_*` (зона B, запрос через `HANDOFFS.md`) ·
`data_pool/**` и `unified_optimizer/**` — read-only для всех

## Что открыто и что дальше

1. **ПЕРВЫМ (владелец, 15.08): разбор кода и методов зоны C.** Пункты `C-1…C-6` в `QUESTIONS.md`,
   что смотреть — `coordination/handoffs/REVIEW_C_fit_malus_2026-08-13.md`, задание —
   `HANDOFFS.md` `ОТ ORCH К A` от 15.08. Пять пунктов `blocking`. Ключевые: **C-5** (σ по шумовому
   полу — изобретение C, даёт `η = 0.0370` против `0.0389` по рецепту A) и **C-2** (трактовка
   `T_время(θ)` как `⟨|t⊥|²⟩`). Числа `track_viewer` в статью не идут — решено владельцем (В-26).
   ➜ Попутно от C: прежняя оценка шумового пола по предымпульсу **завышена на 12.4 дБ**. Если у
   тебя есть веса или пороги на предымпульсной оценке — пересмотри.
2. **Геометрия A19 утверждена (В-20), но фиты трогать ЗАПРЕЩЕНО (В-21).** `GEOMETRY` правится:
   `P` = 24.44 / 24.73 / 31.71 / 37.18, `D` (полувысота) = 10.28 / 9.72 / 10.47 / 20.30;
   `specac = 14.0` уходит окончательно. Перезапуск `purewave` и `test_grid_40_20` и пересчёт H5 —
   **после** ответа зоны L по конвенции границы (В-19), иначе `D_eff/D_phys` считается дважды.
3. **Второй прогон `specac` (`exp_data_2`, 67 файлов) включить в фит (В-24).** Переименование в
   `data_pool/` запрещено — путь через сопоставление в загрузчике. Сперва реши: это `rep2` того же
   набора или отдельный датасет (там повторы и каналы `dark`).
4. Очередь: `A15` (профиль правдоподобия по `P`), `HN13` на `test_grid_33_11`, метод нерегулярности
   для `att-11-16` (прибор уехал, только три микрофото).

## Моё
- состояние: `research/two_wgp/state.json` ⚠ 27 тыс. токенов, история вперемешку с текущим —
  разделить бы
- передача: `research/two_wgp/HANDOFF_A_20260807.md`
- последний результат: `research/results/a18_microscopy/SUMMARY.md`
- физика проекта: `research/SYNTHESIS.md` — **читать до новых гипотез, чтобы не переоткрывать**

## Помнить
За 6 августа три числа опубликованы и в тот же день отозваны. Мультистарт обязателен: угловые
добавочные параметры мультимодальны, холодный старт завышает значимость.
