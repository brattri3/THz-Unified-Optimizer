# CHARTER — мультиагентная сессионная работа над THz-Unified-Optimizer

> **ОБЯЗАТЕЛЬНО к прочтению первым в КАЖДОЙ рабочей сессии.** Это общий входной файл
> настроек и правил. Он определяет: кто ты, где твоя зона, как метиться, как обмениваться
> информацией, как коммитить, что делать при выходе за рамки. Дорабатывается оркестратором.

## 0. Зачем это
Над проектом одновременно работают несколько сессий-агентов (A/B/C/D + оркестратор ORCH),
разделяющих ОДИН git-репозиторий и `data_pool/`. Без протокола они затирают файлы друг друга,
конфликтуют в git и дублируют работу. Этот чартер — «правила дорожного движения».

## 1. Кто ты (идентичность)
Твой ID (A/B/C/D/ORCH) задаётся В ЛАУНЧ-ПРОМТЕ сессии. Если не задан — СПРОСИ у владельца, не
работай анонимно. Прочитав чартер, ты обязан:
1. Зарегистрироваться в `coordination/NICKS.md` под коротким ником своей роли и занять лок
   исполнения в `coordination/sessions/<ID>.md` (§12) — это защита от того, что другой физический
   чат той же роли начнёт работать параллельно.
2. Обновить свою строку в `coordination/BOARD.md` (статус, текущая задача, план, время).
3. Обновить `coordination/sessions/<ID>.md` (что делаю, какие файлы трогаю, до какого времени).
4. Работать ТОЛЬКО в своей зоне (§3). Логировать изменения в `coordination/ACTIVITY.md`.

## 2. Модель изоляции (git) — гибрид
- **Общий рабочий каталог на `main`** для того, что требует ЖИВЫХ общих данных/состояния:
  анализ (A, D), `data_pool/` (read-only), `research/logs`, `research/state.json`.
- **Worktree + отдельная ветка** РЕКОМЕНДУЕТСЯ для сессий, активно правящих ОБЩИЙ КОД
  (B — рефактор ядра) или изолированных (C — приложение): `git worktree add ../TUO-<ID> session/<ID>`,
  запуск `claude` В ЭТОМ каталоге. Оркестратор мержит в main. (Практика: не запускать двух
  правящих один и тот же код сессий в одном каталоге.)
- Если сессия остаётся в общем каталоге — она НЕ создаёт свою ветку (все на `main`),
  а соблюдает владение (§3) и «анонс перед коммитом» (§5).

## 3. Зоны ответственности (владение путями)
См. полную карту в `coordination/OWNERSHIP.md`. Кратко:
| Сессия | Специализация | Владеет (пишет) | Читает |
|---|---|---|---|
| **A** | прямая модель, идентифицируемость и проверка гипотез + софт затухания | `research/two_wgp/`, `research/results/two_wgp/`, `research/hypotheses/` | model_blanco, fit_lib, model_core, research/experiments |
| **B** | инфра/рефактор ядра | `research/experiments/model_core.py`, `verify_*`, рефактор `research/experiments/*.py` | всё research |
| **C** | приложение-прототип | `attenuator_app/`, `docs/attenuator_app/` | model_blanco, research/two_wgp |
| **D** | статья | `research/paper/` | SYNTHESIS, RESEARCH_LOG, results |
| **L** | литература/обзор + метапоиск | `research/papers/` (включает свёрнутый `research/papers/litrev/`) | SYNTHESIS, HYPOTHESES, RESEARCH_LOG |
| **ORCH** | контроль/мерж/координация | `coordination/`, `CLAUDE.md`, main `research/state.json`, ветки/мержи | всё |
**Правило shared-кода:** общие модули (`fit_lib.py`, `model_core.py`, `model_blanco`) правит ТОЛЬКО
владелец (B). Другим — read-only; нужна правка → запрос через `ACTIVITY.md` + `HANDOFFS.md`, ждать B.

> **[2026-07-28, санкция владельца, §8 «при отсутствии ORCH — владелец решает»]** Зона A расширена:
> подпись «прямая модель, идентифицируемость и проверка гипотез + софт затухания»; зона на запись =
> `research/two_wgp/**` + `research/hypotheses/**` + `research/results/two_wgp/**`. Новые
> скрипты-гипотезы A пишет в СВОЕЙ зоне (не в `research/experiments/`, где владелец-правщик — B).
> `research/experiments/**` для A остаётся read-only; правка там — через `HANDOFFS.md` к B.
> Основание: запись `[A] [proposal]` от 2026-07-28 в `ACTIVITY.md`.
>
> **[2026-07-28, РЕШЕНИЕ ORCH — консолидация спора A↔B]** Принята граница **по слою, а не по каталогу**
> (предложение B). Итог: **B** владеет численным ядром (`model_core`, `fit_lib`, `verify_*`) + роль
> ревьюера эквивалентности; **A** владеет скриптами-экспериментами/гипотезами `research/experiments/tN_*.py`
> + `research/two_wgp/` + `research/hypotheses/` и пишет новые гипотезы БЕЗ хэндоффа к B. `GEOMETRY`
> выносится из `fit_lib.py` в `research/experiments/geometry.py` (зона A). Инвариант «второй копии
> Бланко/грида/невязки/билдера не заводить» — машинная проверка в `verify_model_core.py` (задача B).
> B параметризует `build_experiment(data, angles_limit=None)`. Детали — в `OWNERSHIP.md` и `ORCH_BRIEF.md`.

> **[2026-07-28, санкция владельца, §8]** Создана зона **L (литература/обзор + метапоиск)** — из
> сессии-прародителя (bootstrap). Питает D (статью) и A (гипотезы) источниками через HANDOFFS.
>
> **[2026-07-28, РЕШЕНИЕ ORCH — санкция владельца]** Зона L **свёрнута из соседнего репо в наш**:
> `../THz-WGP-Analysis/litrev/**` (был не под git) → `research/papers/litrev/**`. Причина: соседний
> каталог не был git-репозиторием ⇒ обзор и единственная копия литературы не версионировались.
> Теперь зона L на запись = `research/papers/**` целиком. PDF первоисточников (`litrev/pdfs/`, ~22МБ,
> чужой copyright) в git НЕ трекаются (`.gitignore`) — извлечённый текст в `research/papers/txt/`.
> Инструмент `metasearch.py` теперь в `research/papers/litrev/`. Исходник в `THz-WGP-Analysis/litrev/`
> оставлен до проверки владельцем, затем удаляется владельцем.

## 4. Общая зона обмена (координация)
- `coordination/BOARD.md` — ЖИВОЙ статус всех сессий (обновляй свою строку при старте, смене задачи,
  раз в ~несколько шагов = «пульс», при остановке).
- `coordination/ACTIVITY.md` — append-only журнал изменений/планов МЕЖДУ сессиями: что изменил, зачем,
  какие файлы, что это значит для других. Пиши сюда ПОСЛЕ значимого изменения.
- `coordination/sessions/<ID>.md` — твой «замок намерений»: старт-метка + план + файлы, что трогаешь,
  до какого времени. Перед правкой ОБЩЕГО файла — проверь чужие sessions/*.md на конфликт.
- `coordination/HANDOFFS.md` — запросы «сделай в другой зоне» (см. §6).

## 5. Коммиты — обязательная разметка
- Префикс сообщения: **`[<ID>] краткое что`** (напр. `[A] two-WGP attenuation calc A4`).
- Трейлеры в конце сообщения:
  ```
  Session: <ID>
  Reason: <почему сделан коммит>
  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```
- Коммить ТОЛЬКО файлы своей зоны (`git add <свои пути>`), не `git add -A` вслепую.
- **Анонс перед коммитом на общий main:** отметь в `ACTIVITY.md` (сессия, файлы, причина) — чтобы
  другая сессия не запушила поверх. При работе в worktree/ветке — коммить свободно, мерж делает ORCH.
- Перед push: `git pull --rebase` (если на общем main). Конфликт общего файла → не форсить, звать ORCH.

## 6. Выход за рамки зоны — НЕ ДЕЛАЙ, перенаправь
Если владелец просит тебя сделать то, что вне твоей специализации/зоны:
1. НЕ выполняй. Кратко объясни, что это зона Сессии `<Y>`.
2. Запиши запрос в `coordination/HANDOFFS.md` (от кого, кому, что нужно, контекст).
3. Предложи владельцу: «переключись в Сессию `<Y>`» ИЛИ «создать новую сессию `<Z>`» с готовым
   лаунч-промтом (шаблон — в `coordination/LAUNCH_PROMPTS.md`).
Исключение: тривиальное чтение/справка — можно, но правки в чужой зоне — только через хэндофф.

## 7. Протокол одной итерации (в дополнение к research/state.json)
1. Прочитать CHARTER (этот файл) + свой `sessions/<ID>.md` + `BOARD.md` (нет ли пересечений).
2. Прочитать свой рабочий `state.json` (напр. `research/two_wgp/state.json`) → первая не-done задача.
3. Проверить: файлы, что буду трогать, не заявлены другой активной сессией. Если заявлены — взять
   другую задачу или согласовать через ACTIVITY.
4. Заявить намерение в `sessions/<ID>.md`, выполнить ОДИН атомарный шаг.
5. Числа → `research/logs/RESEARCH_LOG.md` с тегом `[<ID>]`. Изменение → `ACTIVITY.md`.
6. Обновить свой `state.json` и строку в `BOARD.md` (пульс).
7. Гардрейлы `research/RESEARCH_PLAN.md §6` действуют всегда.

## 8. Разрешение конфликтов
- Устаревший «замок» (sessions/<ID>.md heartbeat старше ~1 ч) → считать сессию неактивной ЦЕЛИКОМ
  (никто давно не работает над этой ролью), ORCH снимает замок и может переназначить задачи.
  Это ДОЛГОСРОЧНАЯ эвристика — не путать с краткосрочным локом исполнения §12 (таймаут 15 мин,
  взаимное исключение МЕЖДУ чатами одной роли, которые могли стартовать почти одновременно).
- Пересечение зон/двойная работа → ORCH арбитр; при отсутствии ORCH — владелец решает.
- Общий файл нужен двум → сериализовать через ACTIVITY (кто первый заявил), второй ждёт.

## 9. Оркестратор (ORCH) — опционально, но рекомендуется
Отдельная сессия-контролёр: читает BOARD/ACTIVITY/sessions, снимает устаревшие замки, мержит ветки
worktree в main, разруливает конфликты общих файлов, поддерживает CHARTER/OWNERSHIP в актуальности,
назначает/перенаправляет задачи. Спец и лаунч — `coordination/ORCHESTRATOR.md`.

## 10. Доработка
Этот чартер — живой. Предложения по улучшению парадигмы → в `ACTIVITY.md` с тегом `[proposal]`;
ORCH консолидирует и правит CHARTER (это его зона).

## 11. Стиль общения с владельцем и педагогический слой (конвенция, 2026-07-29)
Владелец — постановщик задачи и заказчик, но **НЕ математик/физик** в тех разделах, где идёт работа
(поляризационная оптика, матрицы Джонса, статистика нелинейных фитов, идентифицируемость, UQ).
Поэтому **все сессии** (и артефакты, и ответы владельцу в чате) обязаны:
- **Профессиональный регистр:** рассуждать и писать как физик-теоретик / прикладной математик —
  корректная терминология, аккуратные формулировки, методы называть по именам.
- **Обучать, а не только отчитываться:** каждый нетривиальный термин и символ — определять при
  первом употреблении; давать физическую/геометрическую интуицию ДО формализма; называть
  пререквизиты; по запросу разворачивать любой пункт в мини-курс.
- **Слоистая подача:** сначала суть простыми словами (1–2 фразы), затем строгая формулировка,
  затем разбор/пример. Не вываливать «стену формул» без интуиции.
- **Глоссарий и методички:** термины копятся в глоссарии; вводные курсы — в `research/paper/primers/`
  (ведёт **D**); любая сессия может предложить тему методички через `HANDOFFS.md` к D.
Ведущая по образовательному слою — Сессия **D** (статья + primers + глоссарий); остальные соблюдают
регистр и питают D темами.

## 12. Ники чатов и лок исполнения (конкурентный доступ, конвенция 2026-07-31)

**Зачем.** Одну «роль» (`A`/`B`/`C`/`D`/`L`/`ORCH`) во времени может обслуживать НЕСКОЛЬКО
физических чатов: новый запуск, `--resume`, восстановление после краха. Если два таких чата
работают под одной ролью ПАРАЛЛЕЛЬНО — они топчут файлы своей же зоны и `state.json` друг у друга.
Правило §8 (heartbeat старше ~1 ч) ловит только полностью заброшенную роль — слишком грубая шкала
для «сейчас-занято / сейчас-свободно». Нужна более мелкая защита с коротким таймаутом на случай краха.

**Ники.** Каждый физический чат, начинающий работать под ролью `<ID>`, получает короткий ник
`<ID><n>` (следующий свободный номер для этой роли: `A1`, `A2`, `L3`…) вместо длинного chat id
в повседневном обиходе. Регистрация — в `coordination/NICKS.md` (роль · ник · полный chat id,
если известен · когда зарегистрирован · статус `active`/`retired`). Ники одной роли НЕ
переиспользуются — новый чат всегда берёт следующий номер, даже если все прежние `retired`.

**Лок исполнения** — структурированный блок в каждом `coordination/sessions/<ID>.md`
(дополняет, не заменяет, обычные поля Статус/Heartbeat из §4):
```
## 🔒 Замок исполнения (см. CHARTER §12)
- Статус: IN_PROGRESS | FREE
- Держатель (ник): <ник>
- Взят: <время>
- Heartbeat: <время, обновляется на каждом «пульсе»>
- Таймаут простоя: 15 минут
```

**Протокол:**
1. Перед началом работы под ролью `<ID>` — прочитать этот блок.
   - `FREE` → занять: Статус=`IN_PROGRESS`, Держатель=свой ник, Взят=Heartbeat=сейчас. Ник ещё
     не зарегистрирован → сначала зарегистрировать в `NICKS.md`.
   - `IN_PROGRESS`, heartbeat свежий (< 15 минут) → роль занята ДРУГИМ чатом ПРЯМО СЕЙЧАС.
     **Не начинать работу.** Сообщить владельцу, что роль `<ID>` занята чатом-ником `<X>`;
     ждать освобождения или, если владелец настаивает — решение за владельцем/ORCH (§8).
   - `IN_PROGRESS`, heartbeat старше 15 минут → лок считается зависшим (предыдущий чат, скорее
     всего, упал, не успев отметиться). Разрешено реклеймить (переписать Держателя на себя),
     НО ОБЯЗАТЕЛЬНО тем же шагом:
     a. append в `ACTIVITY.md` с тегом `[STALE-LOCK]`: какой ник не снял лок, последний
        heartbeat, кто и когда снял по таймауту;
     b. запись в `HANDOFFS.md` «ОТ `<новый ник>` К ORCH» с той же сутью — чтобы ORCH знал и мог
        проверить (иногда «завис» — ложное срабатывание: чат просто долго считает, не упал).
        Если реклейм делает сам ORCH — хэндофф себе не нужен, достаточно записи в ACTIVITY.
2. Heartbeat лока обновлять на каждом «пульсе» итерации, пока Статус=`IN_PROGRESS`.
3. При штатной остановке — ОБЯЗАТЕЛЬНО перевести Статус → `FREE` (это и есть отметка о
   завершении). Именно отсутствие этого шага при крахе ловит правило 1 (таймаут 15 мин).

**Контроль доступа — важная оговорка.** Это ПРОЦЕДУРНОЕ правило, а не технический ACL: в
markdown-файлах нет файловой системы прав по нику, и никакой инструмент это не проверяет
автоматически. Правило гласит: чат с ником `<Y>` НЕ ДОЛЖЕН редактировать лок, где Держатель —
чужой ник `<X>` (кроме реклейма по таймауту, п.1, и явного вмешательства ORCH/владельца).
Оно работает только потому, что каждый чат обязан прочитать CHARTER первым шагом (§1) и следовать
ему. Технической защиты от нарушения нет; при подозрении (лок сброшен без записи `[STALE-LOCK]`
и хэндоффа) — сигнал ORCH разобрать при следующем проходе.

**Отношение к §8.** §8 (эвристика ~1 ч) — для ДОЛГОСРОЧНОГО простоя роли целиком (никто над ней
давно не работает — можно переназначить задачи). Лок §12 (таймаут 15 мин) — для КРАТКОСРОЧНОГО
взаимного исключения между чатами ОДНОЙ роли, способными стартовать почти одновременно.
