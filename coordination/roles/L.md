# Роль L — литература и метапоиск

**Имя сессии:** `l-lit` · **префикс коммита:** `[L]`

## Пишу
`research/literature/**` (включая `litrev/`: обзоры, corpus, JSON-графы, `metasearch.py`)

## Гардрейлы зоны
Легальный доступ (Unpaywall, arXiv, репозитории), пейволл не трогать. Недоступное → `WANTED.md`,
не «по памяти» (формулы тоже). PDF не трекаются, текст — в `txt/`.

## Что открыто и что дальше

1. **В-19 (15.08, методвопрос, не волей):** норма «края объекта» по микроскопии для круглой
   проволоки? Определяет `D_eff/D_phys` (§4.3) — **фиты A стоят**. Задание — `HANDOFFS.md`,
   `ОТ ORCH К L` 15.08.
2. **Непрочитанный конкурент, приоритет:** «Revisiting the performance of free-standing polarizing
   wire grids», IRMMW-THz 2025, `10.1109/IRMMW-THz61557.2025.11319566` — перекрытие заявок.
3. **Почини `doi_audit.py`** — 3 дефекта (→ `ACTIVITY.md` 24.08): ложный `DEAD` на **Blanco 1986**
   (regex рвёт DOI со скобкой), regex на прозу, кодировка «µ», хардкод `REPO`. На хук — после
   починки (одобрено 29.07).
4. **Два PDF в git не по правилу зоны** (ORCH 17.08, дубль) — решить с владельцем: снять с
   индекса, закрыть путь.
5. Очередь: `Kaltenecker 2019` (в `txt/`) · граф цитирований P3 · `P4` (Costley 1977) ·
   Castro-Camus.

## Главное, что уже установлено
`Blanco 1986` = *Infrared Phys.* **26**(6):357–363, `10.1016/0020-0891(86)90058-8` (прежняя ссылка
— фантом). Граница `d/p<0.5`, наш `D/P=0.71` вне неё: конкурирующее объяснение `D_eff`.

## Владельцу
15 работ в `WANTED.md`, топ-4 блокируют: Laman & Grischkowsky 2008, El-Agez & Taya 2011, Hauge
1980, **Volkov 1982** (ключ — Бланко сравнивает при `d/p=0.5`).

## Моё
`HANDOFF_L_20260807.md` · `INDEX.md` · `litrev/RETROSPECTIVE_REVIEW.md`
