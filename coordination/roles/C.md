# Роль C — приложения

**Имя сессии:** `c-app` · **префикс коммита:** `[C]`

## Пишу
`attenuator_app/**`, `docs/attenuator_app/**` · `track_viewer/**`, `docs/track_viewer/**`
(расширено санкцией владельца 12.08)

## Не трогаю
`data_pool/**` — read-only. ⚠ Зона хрупка к именам/путям: дважды ломалась **молча**.

## Что открыто и что дальше

1. **СВОДНОЕ ЗАДАНИЕ** (`HANDOFFS.md`, `ОТ ORCH К C` 17.08). Осталось: `C-6` — отвязать
   побинный фит от полосы показа; `C-2` — фраза в `docs/track_viewer/01_FORMATS.md` §6.3.
   Прочее (знак `θ₀`, `eta0=0`) вне зоны C — `QUESTIONS.md`. После п.2.
2. **C9 принят ORCH 26.08**: приёмка зелёная, код в локальном `main` **основного** каталога,
   **не пушен** — за владельцем. По порядку: (1) **живая отладка GUI** — в облаке не было
   tkinter; (2) **серия `ATT-11-16-CA85_02721_s2`** вместо синтетики. Зацепки, числа и три
   ловушки — `HANDOFFS.md`, `ОТ ORCH К C` 26.08; промт запуска — `LAUNCH_PROMPTS.md`.
   ⚠ Работать в основном каталоге: серия вне git. Отчёт — `docs/artifacts/C_service_two_rotators.md`.
3. **Два release gate у владельца**: `C6_v02_acceptance` и track_viewer ТЗ п.5 (Win7).
   До ответа C3/C4/C5 не начинать. ⚠ `.exe` v0.3 под Python 3.14 на Win7 не пойдёт.

## Находки, переданные A/B
шумовой пол по предымпульсу завышен на 12.4 дБ · `T_time` без вычитания DC завышает тёмные
углы в 121 раз · `argmax(abs(E))` в гашении мимо на 5 шагов. Разбор — `ACTIVITY.md`.

## Моё
`attenuator_app/HANDOFF_C_20260807.md` · `attenuator_app/STATE.md` ·
`coordination/handoffs/REVIEW_C_fit_malus_2026-08-13.md`

⚠ `fit_malus.py` — **копия** `malus_linearization.py` (A), не импорт; A сверяет при правке
оригинала (`C-1`). Сверка ORCH 17.08: 48/48, `0.000e+00`.
