# BOARD — что открыто и что делать дальше

> **Кто сейчас активен — `claude agents` / `claude agents --json`** (см. `coordination/reports/MIGRATION_2026-08-07.md`),
> не здесь. Живой статус убран 2026-08-07: колонка систематически отставала от факта.

> **⚠ Регламент объёма (с 2026-08-08).** Строка роли — **до ~10 строк**: текущий статус, что открыто,
> что делать дальше, куда идти за подробностями. **История находок сюда не пишется** — её место в
> `ACTIVITY.md` (append-only, в холодный старт не входит) и в передачах ролей `HANDOFF_*`.
> Причина жёсткая: этот файл читает каждая роль при каждом старте. К 08.08 он разросся до 62 КБ
> (~21 тыс. токенов на старт) и был сжат до ~10 КБ. Дословный снимок «до» — в `ACTIVITY.md`,
> запись «Снимок BOARD.md перед сжатием» от 2026-08-08.

> **➜ План на понедельник 2026-08-10 — `coordination/reports/PLAN_2026-08-10.md`.**
> Что кому делать, в порядке зависимостей, и чего делать НЕ надо. Роли B, L, D можно
> запускать параллельно; A ждёт таблицу измерений владельца.

| Роль | Статус, что открыто, что дальше | Обновлено |
|---|---|---|
| **A** модели / идентифицируемость / гипотезы | **stopped**, лок свободен, следующий чат берёт ник **A4**. **➜ читать первым: `research/two_wgp/HANDOFF_A_20260807.md`.** **Диаметры получены от владельца 2026-08-09** — свод с источниками: `coordination/reports/DIAMETERS_DECISION_2026-08-09.md`. Паспорт: `purewave` 10.0, `specac` **10.0** (в коде стоит 14.0 — ⚠ грубо неверно, `D/P` уходит 0.562→0.402), `test_grid_40_20` **20** (в коде 18.0), `att-11-16` 11.0. Измерения владельца: `specac` 10.2, `att-11-16` 11.2, `test_grid_33_11` 11.2. **Диаметров теперь ТРИ на образец** (паспортный / измеренный / эффективный) — `GEOMETRY` с одним полем `D_phys_um` структуре не соответствует, это работа A + B. **10.08 владелец переизмеряет всё**, включая период; `test_grid_40_20` даст проверку гипотезы о систематике микроскопа (+0.2 мкм ровно на трёх образцах). ⚠ `att-11-16` **уехал** — период только по трём микрофото, метод предложен владельцем и передан: `HANDOFFS.md`, `ОТ ORCH К A` от 09.08. **До таблицы владельца от 10.08 НЕ начинать** правку `GEOMETRY` и пересчёт H5 — иначе работа дважды. ⚠ За 6 августа три числа опубликованы и отозваны (§2 передачи). Действующее: H5 **не опровергнут и не подтверждён**; вырождение `D_eff↔η₀↔ε_cross` — свойство **плотного** режима, не модели. Очередь: `A15` (профиль правдоподобия по `P`), `HN13` на `test_grid_33_11`. | 2026-08-09 |
| **B** техническая гигиена | **⚠ Роль переопределена владельцем 2026-08-08:** было «численное ядро + ревьюер эквивалентности», стало **техническая гигиена** — рефакторинг, дубли, инварианты, работоспособность кода. Правила сохранены: нулевая доля в физических выводах, допуск ноль со снимком «до». Режим — **по вызову, не постоянной сессией**. Зона расширена на `unified_optimizer/**` **по разовой санкции владельца на конкретный дефект**. **⏸ Задача 1 приостановлена до 2026-08-10** (слабый ноутбук): починка двух `NameError` продакшна, правка на ветке **`session/B-prod-fix`** (`ddbe0e7`), помечена «НЕ ПРОВЕРЕНО», **в `main` не мержить**; остаток проверок — в `HANDOFFS.md`. **Очередь после неё:** (1) вынос `GEOMETRY` → `research/experiments/geometry.py` — **со сроком**, иначе каждая правка диаметров требует сессии B; (2) `build_experiment(angles_limit, mask_angles)` — снимает второй билдер; (3) машинная проверка инварианта. ⚠ Цифра «копии фильтра 5 → 20» из передачи **не подтвердилась** (их пять); настоящий риск — **два билдера** и зависимость водяной маски от набора углов. Передача: `research/experiments/HANDOFF_B_20260807.md` (числа §3.1 читать с этой поправкой). | 2026-08-08 |
| **C** приложение (аттенюатор) | **stopped**, лок свободен, следующий чат берёт ник **C2**. **➜ читать первым: `attenuator_app/HANDOFF_C_20260807.md`.** v0.3 (GUI + автономный `.exe`) написана, проверена и смержена; **ни одна версия не выпущена и не одобрена** — release gate требует личного прогона владельца. Открыта одна задача **C6_v02_acceptance**, она **ждёт владельца**; до его обратной связи C3/C4/C5 не начинать. **⚠ Открытый вопрос к владельцу (ORCH, 08.08):** `.exe` собран под Python 3.14, а целевая машина в документации — Win7-ПК у спектрометра; Python с 3.9 Windows 7 не поддерживает ⇒ **на какой машине идёт приёмка?** Проверить до поездки к прибору. Win7-комплект и распечатанный чек-лист **намеренно** остались на старых именах наборов и согласованы между собой — приводить к именам репозитория только после приёмки и сразу все три части. ➜ всем, кто двигает `data_pool/**`: зона C зависит от имён и расположения файлов, дважды ломалась молча. | 2026-08-08 |
| **D** статья + образовательный слой | **Курс §0–§10 + приложения A/Б завершён (2026-08-08)**, 76 страниц, сборка чистая, проверено дважды независимо. **Ждёт чтения владельца** — это и есть его приоритет «восстановить контроль над физикой». ⚠ Восемь параграфов из одиннадцати написаны одним непрерывным прогоном **без пауз на отзыв** (разовое исключение, закрыто) — замечания собирать по ним первым делом. ⚠ Приложение Б (образцы и геометрия) стоит на числах A16, которые владелец **ещё не утвердил** — вероятно, потребует переиздания. Правило «один параграф → PDF → пауза до отзыва» снова действует по умолчанию. Жанр — самостоятельная учебная литература, регламент `research/paper/course/STYLE.md` (без имён сессий, путей, номеров гипотез и кодовых имён наборов). Следующий ник — **D4**, промт и передача: `research/paper/course/LAUNCH_PROMPT.md`. Ждём от A/B свод чисел для `DRAFT §4.2/§4.7`. | 2026-08-08 |
| **L** литература / метапоиск | **stopped**, лок свободен, следующий чат берёт ник **L3**. **➜ читать первым: `research/literature/HANDOFF_L_20260807.md`.** **Главное для цели №5:** Blanco 1986 = *Infrared Phys.* **26**(6):357–363, DOI `10.1016/0020-0891(86)90058-8` (прежняя ссылка была фантомом); автор объявляет границу применимости `d/p < 0.5`, а наш `D/P = 0.71` — **вне** её ⇒ самостоятельное объяснение загадки `D_eff`, конкурирующее с «недостающей физикой». Разводить это с работой A **до** того, как загадка будет заявлена как главная новизна. Аудит DOI: 34 записи, битый `…884786` → `…838786` (исправлен). Предложение проекту без решения: прогонять Crossref-проверку списка литературы перед отправкой. Очередь: P2-выжимки, P4/P5, 2-й уровень графа цитирований, Kaltenecker на гармоники. Владельцу — 15 закрытых работ в `research/literature/WANTED.md`, первые три блокируют содержательные ответы. | 2026-08-07 |
| **P** отчётные презентации | **stopped**, лок свободен, следующий чат берёт ник **P2**. **➜ читать первым: `research/presentations/HANDOFF_P_20260807.md`.** Зона развёрнута с нуля, первый недельный отчёт сдан (`weekly/2026-08-04/`, 8 слайдов + `SOURCES.md`). ⚠ Его числа **устарели 06.08** после A16 — `fig1` обновлять **только после утверждения диаметров владельцем**, иначе переделывать дважды. **Четыре постоянных решения владельца по оформлению** (светлая тема; 8 ± 2 слайда; минимум выносок на графиках; обязательный слайд «теория и эксперимент в двух шкалах») — в `STYLE_MANIFEST.md`, действуют для всех отчётов. Владелец 04.08 разрешил зоне P строить графики по данным других зон, **но фитить нельзя** — параметры берутся готовыми из артефакта зоны-автора. Очередь: `P2_weekly_next` (не заблокирована, материала много), `P3_deff_deepdive` (ждёт утверждения геометрии). | 2026-08-07 |
| **ORCH** координация | **Уборка репозитория, шаги 1–5 закрыты (2026-08-09).** Слой Antigravity отменён и заархивирован (В-1), заведён `archive/` с регламентом; владельческие дыры закрыты — ~258 бесхозных файлов получили строку в `OWNERSHIP.md`, правило «нового каталога верхнего уровня без строки не заводить»; генерируемое выведено из git, **но результаты июля заархивированы, а не удалены** — в них 2D-числа, невоспроизводимые из-за поломки 2D-ветки; `coordination/` получил карту `README.md` (что читать при старте, что по поводу). Разбор — `ACTIVITY.md`, записи от 09.08. **Второй опрос владельца 09.08 — шесть решений (`QUESTIONS.md`), четыре исполнены сразу:** `docs/` заморожен (212 файлов → `archive/docs-legacy-2026-07/`, живым остался только `attenuator_app/`); **`research/papers` → `research/literature`**, 91 ссылка в 25 файлах починена, старые пути в append-only истории намеренно не тронуты — таблица подстановки в шапке `HANDOFFS.md`; `README.md` репозитория переиздан. **Открыто:** (1) 10.08 доводим задачу B — ветка `session/B-prod-fix`; (2) **В-10 отменил В-5**: три трека сразу (диаметры, приёмка, курс), все три упираются в действия владельца, а не агентов; (3) для L: два PDF в `research/literature/pdfs/` трекаются вопреки политике зоны — решение за L; (4) `research/state.json` главной линии устарел (2026-07-25); (5) старые хэндоффы не перепроверялись с 07.08. | 2026-08-09 |

## Легенда владения (полностью — `OWNERSHIP.md`)
A→`research/two_wgp/`+`research/hypotheses/` · B→численное ядро + техническая гигиена ·
C→`attenuator_app/` · D→`research/paper/` · L→`research/literature/` · P→`research/presentations/` ·
ORCH→`coordination/`, `CLAUDE.md`, main `research/state.json`.

## Активные «замки» намерений
См. `coordination/sessions/*.md`. Перед правкой общего файла — проверь их. Каждый файл содержит блок
«🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, имя держателя, таймаут 15 минут.
