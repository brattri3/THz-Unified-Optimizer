# Роль A — прямая модель, идентифицируемость, гипотезы

**Имя сессии:** `a-model` · **префикс коммита:** `[A]`

## Пишу
`research/two_wgp/**` · `research/results/two_wgp/**` · `research/hypotheses/**` ·
скрипты-гипотезы `research/experiments/tN_*.py`, `model_m5.py`, `micrograph_*`, `make_*` ·
`research/experiments/geometry.py` (когда будет вынесен)

## Не трогаю
Численное ядро — `model_core.py`, `fit_lib.py`, `verify_*` (зона B, запрос через `HANDOFFS.md`) ·
`data_pool/**` и `unified_optimizer/**` — read-only для всех

## Что открыто и что дальше

1. **Готово 2026-08-16.** Разбор кода и методов зоны C — вердикт по `C-1, C-2, C-5, C-6` внесён в
   `QUESTIONS.md` (раздел «Вердикт зоны A по C-1, C-2, C-5, C-6», сразу после таблицы C-1…C-6).
   Коротко: C-1 (схема обратного сигнала) — принята, встречное обязательство прогонять
   `verify_fit_malus.py` при правке `malus_linearization.py`. C-2 (`⟨|t⊥|²⟩`) — формулировка верна,
   но зоне C нужна редакционная правка `01_FORMATS.md` §6.3: тождество `4.7·10⁻¹³` точное только
   при общем векторе весов, в рабочем режиме остаток `≤7·10⁻⁴`. C-5 (веса по шумовому полу) — не
   поднимается до умолчания, рецепт A (`relative`) остаётся единственным для чисел вне инструмента;
   `dark`-канал предпочтительнее ВЧ-хвоста как источник σ, если снят. C-6 (побинный фит по полосе
   показа) — дефект подтверждён, не blocking, доработка за зоной C (полоса счёта по порогу 40 дБ,
   а не полоса показа). C-3 закрыта владельцем (В-26), C-4 снята зоной C. ➜ От C: прежняя оценка
   шумового пола по предымпульсу **завышена на 12.4 дБ** — веса/пороги на предымпульсной оценке
   на стороне A не найдены, пересматривать нечего.
2. **Геометрия A19 утверждена (В-20), но фиты трогать ЗАПРЕЩЕНО (В-21).** `GEOMETRY` правится:
   `P` = 24.44 / 24.73 / 31.71 / 37.18, `D` (полувысота) = 10.28 / 9.72 / 10.47 / 20.30;
   `specac = 14.0` уходит окончательно. Перезапуск `purewave` и `test_grid_40_20` и пересчёт H5 —
   **после** ответа зоны L по конвенции границы (В-19), иначе `D_eff/D_phys` считается дважды.
3. **Готово 2026-08-16 (В-24).** Второй прогон `specac` — **отдельный датасет** `specac-2`, не
   `rep2`: окно 67.71 пс/651 точка против 130.21/1251 (сетки не вложены), другой угловой план,
   каналы `dark` и повторы. Загрузчик — `research/two_wgp/scan_loader.py`, без записи в
   `data_pool`; сверка с `exp_builder` = `0.000e+00`. Вердикт в `QUESTIONS.md`.
   ➜ Открыт **A-4**: `META.txt` прогона пустой (нет геометрии установки и материала преграды
   `dark`) — это тот же дефект, что погубил `purewave`.
4. **A22 сделана 2026-08-16 — этап 1 двухэтапной схемы (идея владельца).** Линеаризация Малюса в
   КОМПЛЕКСНОМ ПОЛЕ (базис 2-го порядка) вместо мощности. `field_linearization.py` (23/23),
   `a22_field_vs_power.py`, вывод — `NOTE_field_linearization.md`. Коротко: Коши — Буняковский
   выполняется тождественно (поле 0/151 против 12/151 у мощности) ⇒ закрыт открытый вопрос
   `NOTE_malus_linearization` §8; `τ_leak = +0.1871 ± 0.0053 пс` без пина `D_eff` (снимает условие
   HN8); `θ₀` совпал с A13 в 0.13σ при ошибке в 5.6 раза меньшей; модель шума пришлось сделать
   смешанной и калибровать. **Следующее — этап 2:** фит Бланко к восстановленным спектрам,
   `χ²₂ = Σ 2·ΔT^H Cov2⁻¹ ΔT`, мультистарт по сетке.
5. Очередь: `A15` (профиль правдоподобия по `P`), `HN13` на `test_grid_33_11`, метод нерегулярности
   для `att-11-16` (прибор уехал, только три микрофото).

## Моё
- состояние: `research/two_wgp/state.json` ⚠ 27 тыс. токенов, история вперемешку с текущим —
  разделить бы
- передача: `research/two_wgp/HANDOFF_A_20260807.md`
- последний результат: `research/results/a18_microscopy/SUMMARY.md`
- физика проекта: `research/SYNTHESIS.md` — **читать до новых гипотез, чтобы не переоткрывать**

## Помнить
За 6 августа три числа опубликованы и в тот же день отозваны. Мультистарт обязателен: угловые
добавочные параметры мультимодальны, холодный старт завышает значимость.
