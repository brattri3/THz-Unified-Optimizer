# Роль B — техническая гигиена и численное ядро

**Имя сессии:** `b-core` · **префикс коммита:** `[B]` · **режим: по вызову, не постоянной сессией**

## Пишу
Численное ядро где бы ни лежало: `research/experiments/model_core.py`, `fit_lib.py`,
`verify_model_core.py`, `refactor_ab_check.py` · рефакторинг `research/experiments/*.py` ·
`scripts/**` · `unified_optimizer/**` — **только по разовой санкции владельца на конкретный
дефект** (санкция не переносится на следующий, запрашивается заново)

## Не трогаю
Скрипты-гипотезы и физику поверх ядра (зона A) · чужие результаты · `data_pool/**`

## Роль переопределена владельцем 2026-08-08
Было «численное ядро + ревьюер эквивалентности», стало **техническая гигиена**: рефакторинг, дубли,
инварианты, работоспособность кода. Правила сохранены: **нулевая доля в физических выводах**,
**допуск ноль** со снимком «до».

## Что открыто и что дальше

1. **Задача 1 — починка двух `NameError` продакшна.** Ветка `session/B-prod-fix` (`ddbe0e7`),
   помечена «НЕ ПРОВЕРЕНО», **в `main` не мержить**. Локальной ветки нет, получить:
   `git fetch origin && git switch -c session/B-prod-fix origin/session/B-prod-fix`.
   Проверки — в `HANDOFFS.md`, запись задачи 1: сверка `--method 1d` до/после на **одном** датасете
   через `git stash` с допуском ноль; `pytest unified_optimizer/tests -q`; `verify_model_core.py`
   = `ALL CHECKS EXACT`; байткодовое сравнение; `run_pipeline --dataset <один> --method 2d`.
   ⚠ Не тратить время на полный прогон `--method 1d` как доказательство эквивалентности — он не
   нужен по построению, это уже проверено и стоило 18 минут впустую.
2. **Дефект `noise_floor` = `4/π`, хэндофф от A (16.08, `open`).** `nfa = np.mean(bg_avg[tail])` —
   среднее **модуля** `|FFT|`, а используется как СКО ⇒ `tnoise` занижен **по дисперсии** ровно в
   `4/π ≈ 1.273` раза, систематически, на всех датасетах. Места: `research/experiments/t6_hn6.py:47`
   (оригинал) и копия `research/two_wgp/a3_eps_stability.py:133-134`.
   Обнаружено калибровкой по `χ²/dof` в A22: без поправки `χ²_red` ≈ на 27 % ниже единицы.
   На точечные оценки **не влияет** (`tnoise(ν)` от угла не зависит и сокращается в МНК); влияет на
   всё, где `tnoise` входит абсолютно — пороги масок (`|exp| > 1.5·√tnoise`, `a3_eps_stability.py:151`,
   `model_m5.py:43`), SNR, `χ²`, нормированные ошибки параметров.
   **Просят решить, править ли в ядре.** При правке `verify_model_core.py` обязателен: сдвиг порога
   маски меняет набор валидных точек, а с ним и все числа поверх. В зоне A уже есть образец —
   константа `KAPPA_ABS_TO_VAR = 4/π` в `research/two_wgp/field_linearization.py` и флаг `--kappa 1.0`
   для сверки. **НЕ просят** менять умолчание источника шумового пола (ВЧ-хвост) — это другое
   решение. Полностью — `HANDOFFS.md`, запись `ОТ A К B` от 2026-08-16.
3. **Вынос `GEOMETRY`** из `fit_lib.py` → `research/experiments/geometry.py` (дальше это зона A).
   **Со сроком:** иначе каждая правка диаметров требует сессии B. Новые числа готовы —
   `research/results/a18_microscopy/SUMMARY.md`, блок «Строки для зоны B». Нужна структура на три
   диаметра (`D_pass_um` / `D_meas_um` / `D_eff_um`) с явным указанием, какой стоит в знаменателе
   `D_eff/D_phys`, плюс новое поле `sigma_pos_um` (измеренная величина, не приор).
4. `build_experiment(angles_limit, mask_angles)` — снимает второй билдер.
5. Машинная проверка инварианта «второй копии Бланко/грида/невязки/билдера не заводить».

⚠ Цифра «копии фильтра 5 → 20» из передачи **не подтвердилась** (их пять). Настоящий риск —
**два билдера** и зависимость водяной маски от набора углов.

## Известные дефекты продакшна (проверено запуском)
- `unified_optimizer/optimizer_2d.py:122` — `p_max_um` нигде не определено ⇒ `optimize_2d_spectral()`
  падает с `NameError`, значит `run_pipeline --method 2d|both` (дефолт — `both`) нерабочий.
- `unified_optimizer/optimizer_lmfit.py:92` — вызов `model_blanco.estimate_deff_initial(...)` при
  отсутствующем импорте ⇒ `run_lmfit_2d()` тоже `NameError`.

Модульные `optimizer_2d.get_transmission_spectra` и `compute_theoretical_grid_2d` работают —
именно их импортирует `research/`, поэтому исследовательский слой не задет.

## Моё
передача `research/experiments/HANDOFF_B_20260807.md` (числа §3.1 читать с поправкой выше) ·
журнал `research/experiments/CHANGELOG_model_core.md`
