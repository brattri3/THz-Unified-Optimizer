# Передача владельцу на утро — состояние на 2026-08-17

Написано ORCH в конце дня 17.08, чтобы утром на рабочем ПК не восстанавливать контекст.
Всё отправлено в `origin/main` (последний коммит — `a4999a5`). Рабочее дерево чистое,
неотправленного нет.

Порядок чтения: §1 (что решаешь только ты) → §2 (что запустить и чем) → §3 (что висит на мне).

---

## 1. Ждёт твоего решения — без этого зоны стоят

Пять развилок. Первые две держат статью, третья — точку закона H5.

| # | Вопрос в одну строку | Что заблокировано | Где полностью |
|---|---|---|---|
| **A-6** | Какой состав модели канонический для `D_eff` — с `eps_cross` или без? На `test_grid_33_11` при одной маске, одном `θ₀`, одной метрике: **7.3176 ± 0.0332** без `eps_cross` против **4.3377 ± 1.2283** с ним, ΔAIC = −23.7 **в пользу** `eps`. `D_eff/D_phys` = 0.699 либо 0.414 | **§4.3 и Fig. 4 статьи.** Заявленная точность `D_eff` — это точность ВНУТРИ варианта; разброс от выбора варианта сравним со всей «загадкой `D_eff`» (0.43–0.64) | `QUESTIONS.md` A-6 |
| **В-19** | Как микроскопия определяет границу объекта (задача зоны L, поставлена 15.08) | Перефит `purewave` и `test_grid_40_20`, пересчёт закона H5, задача A15. Заморозка В-21 держится на этом | `QUESTIONS.md` В-19 |
| **A-5** | Был ли пучок первого прогона `specac` смещён от оси вращения ротатора? Ты сказал 16.08: `exp_data_1` снимался **в перетяжке**, `exp_data_2` — в коллимированном пучке, юстировка между ними не восстанавливалась | Годится ли `specac` как точка закона H5. Тот же класс дефекта, из-за которого вывод A7 по `purewave` был отозван. По `η` грубого искажения не видно (0.0383 против ≈0.036, 6 %), но `D_eff` так не проверялся | `QUESTIONS.md` A-5 |
| **A-7** | Покрывает ли запрет В-21 модельно-НЕзависимое измерение? В A25 `purewave` и `test_grid_40_20` задействованы, но фита модели там нет — только линейный МНК в угловом базисе, без единого параметра Бланко | Положительный контроль A25 «тот же тракт, другой участок решётки» (×1.66 при 3.1σ). Если читать В-21 широко — флаг `--skip-frozen` пересчитывает всё без них | `QUESTIONS.md` A-7 |
| **A-4** | Материал преграды для канала `dark` и центровка второго прогона — единственные незаполненные поля `META.txt` | Что означает уровень `dark` как независимая оценка шума. Остальное заполнено 16.08 по твоей санкции | `QUESTIONS.md` A-4 |

**Почему A-6 стоит решать первым:** это единственный из пяти, где нельзя отложить и работать
дальше. Пока состав модели не выбран, любое число `D_eff/D_phys` идёт с оговоркой о варианте,
а §4.3 и Fig. 4 — центр статьи.

---

## 2. Что запускать утром

### Зона C — готова к работе прямо сейчас

```
claude -n c-app
```
Первым сообщением: прочитать `coordination/roles/C.md`, пункт 1 — сводное задание
`HANDOFFS.md`, `ОТ ORCH К C` от 17.08. Четыре пункта в порядке приоритета:

1. **Знак `θ₀` на `att-11-16-356`** — A22 даёт `+1.2238 ± 0.0243`, `_calib_raw.json` `−1.2730`.
   Модуль совпал, знак нет. На `test_grid_33_11` знаки сходятся ⇒ дефект специфичен для схемы
   «вращается ВТОРОЙ WGP». Ворота ко всему приборному: пока не разобран, сопоставлять числа
   A22 с паспортом бессмысленно.
2. **Паспорт прибивает `eta0 = 0`**, вся утечка идёт в `D_eff = 5.67 ± 0.249`. A24 показала
   механизмом: перекос неподвижного элемента **имитирует утечку** (при 20–30° эффективная `η`
   растёт с 0.011 до 0.027–0.068), и паспорт подмену не различает.
   ⚠ Тест ранга 1 на приборе **провален** — `T⊥`/`T∥` из A22 там эффективные; зона A их в
   паспорт не предлагает и прямо просит не ставить.
3. **C-6** — отвязать побинный фит от полосы показа (единственная правка кода из всего разбора).
4. **C-2** — одна редакционная фраза в `docs/track_viewer/01_FORMATS.md` §6.3.

### Зона B — если дойдут руки

```
claude -n b-core
```
Два дела, оба лежат с прошлой недели:

- **Ветка `session/B-prod-fix`, один коммит `ddbe0e7` от 08.08, помечена «НЕ ПРОВЕРЕНО».**
  Содержит восстановление `p_max_um` в `optimizer_2d.py` (удалена коммитом `b0372ce` 19.07 —
  это и есть источник падения `--method 2d`) и добавление `model_blanco` в импорт
  `optimizer_lmfit.py`. Правка ценная, но **не доказана**: список из пяти проверок до мержа
  записан в теле самого коммита. Это единственная задача проекта, которой нужен полный прогон
  пайплайна — то есть не для слабого ноутбука.
- **Дефект `noise_floor` от зоны A:** `nfa` считается как среднее МОДУЛЯ FFT, а используется
  как СКО ⇒ `tnoise` занижен по дисперсии ровно в `4/π ≈ 1.273` раза, систематически, на всех
  датасетах. На точечные оценки не влияет, но задевает пороги масок, SNR и все нормированные
  ошибки. Решить, править ли в ядре; при правке `verify_model_core.py` обязателен — сдвиг
  порога маски меняет набор валидных точек. `HANDOFFS.md`, запись `ОТ A К B` от 16.08.

### Зона A — вернуться позже

Очередь целиком в `research/two_wgp/BACKLOG_A.md` (написан специально под холодный возврат).
Готовы к запуску и ничем не заблокированы: **HN13 на `test_grid_33_11`** (~1 сессия, самая
дешёвая из содержательных — единый `θ₀` против отдельного на канал, на чистейшем образце
проекта тест ещё не делался) и **показатель `a` у пары `specac`**.

---

## 3. Что висит на мне (ORCH), сделаю без тебя

- Проследить исполнение C-6 и C-2 зоной C — единственный остаток разбора.
- Провести три хэндоффа зоны A до ответа: к B (`4/π`), к C (паспорт, знак `θ₀`).
- `research/state.json` главной линии устарел с 2026-07-25.
- Старые хэндоффы не перепроверялись с 07.08.

---

## 4. Что закрыто за 16–17.08 — можно не возвращаться

- **Разбор кода зоны C сессией A (В-22, твой первый приоритет) — ВЫПОЛНЕН.** `C-1, C-2, C-5,
  C-6` закрыты вердиктом 16.08, **ни один пункт не отклонён**, код фита менять не требуется.
  `C-1` принят со встречным обязательством зоны A прогонять сверку при правке оригинала; лока
  и CI не заводили — со ссылкой на твоё решение В-27.
- **В-24 решено измерением:** второй прогон `specac` — **отдельный датасет**, не `rep2` (окно
  67.71 пс против 130.21, частотные сетки не вложены, свой угловой план, `dark` и повторы).
  Загрузчик сделан без записи в `data_pool`, сверка с `exp_builder` — `0.000e+00`.
- **A-4 закрыт** в части, заполнимой по твоему опросу; источник каждого поля помечен внутри
  `META.txt` — ответы по памяти, вычисленное из трасс и «не записано» разделены явно.
- **Открытый вопрос `NOTE_malus_linearization` §8 закрыт:** нарушения Коши — Буняковского есть
  шумовой пол, предсказуемый по SNR. В поле неравенство выполняется тождественно — 0/151 и 0/94
  против 12/151 и 43/94 в мощности.
- **`τ_leak` измерена без пина `D_eff`** (`+0.1871 ± 0.0053` пс на `att-11-16-356` против
  эталонных 0.195/0.19) — снимает условие HN8.
- **Вырождение `D_eff ↔ η` НЕ снято** и зоной A не заявляется — оставлено отрицательным
  контролем в самопроверке.

**Проверено запуском 17.08, а не по журналу:** `verify_fit_malus.py` — 48 сверок,
расхождение `0.000e+00`, порт совпадает с оригиналом бит-в-бит; селфтесты
`field_linearization` 23/23, `a23_stage2_blanco` 11/11, `a24_two_element_rank` 11/11,
`a25_eta_invariance` 12/12.
