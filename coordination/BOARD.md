# BOARD — открытые находки, решения и хэндоффы по ролям

> **Кто сейчас активен и в каком статусе — смотреть `claude agents` / `claude agents --json`
> (см. `coordination/MIGRATION.md`), не здесь.** Эта колонка регулярно отставала от факта (см.
> историю проекта 2026-08-03…07) и с 2026-08-07 убрана. Ниже — то, чего `claude agents` не знает:
> научные находки, принятые решения, что делать дальше.

| Роль | Открытые находки / решения / что дальше | Обновлено |
|---|---|---|
| **A** модели/идентифицируемость/гипотезы + софт затухания | **stopped** — чат **A3** закрыт владельцем 2026-08-07, лок освобождён штатно (`A.lock` удалён, блок → FREE). Следующий чат берёт ник **A4** **➜ ЧИТАТЬ ПЕРВЫМ: `research/two_wgp/HANDOFF_A_20260807.md`** — полная передача роли. Кратко: **работа ЖДЁТ ВЛАДЕЛЬЦА**, он утверждает физические диаметры проволок решением по паспорту — `purewave` = **10.0 мкм** (закрыт), `test_grid_33_11` = **11.2 мкм** (предварительно, будет пересъёмка), остаются `specac`, `test_grid_40_20`, `att-11-16`. **До полного набора НЕЛЬЗЯ:** вносить строки в `fit_lib.GEOMETRY` (хэндофф к B помечен `on hold`), пересчитывать закон H5, «уточнять» утверждённые диаметры по микрофото. ⚠ **За 6 августа три числа были опубликованы и отозваны** — цепочка разобрана в §2 передачи, без неё легко вернуться к отозванному **★ A16 (2026-08-06, итог дня) — ГЕОМЕТРИЯ ВСЕХ ОБРАЗЦОВ ПЕРЕСЧИТАНА ЕДИНООБРАЗНО, ЗАКОН H5 УСТОЯЛ.** По поручению владельца метод оценки диаметра по микрофото применён ко всем образцам. **`k = 0.855 ± 0.085`** (4 измеренных) и **`0.829 ± 0.040`** (все 8, четыре на паспортной геометрии) против прежнего **0.850** ⇒ **закон менять не надо**. ⚠ Но устойчивость закона **НЕ доказывает правильность `D`**: диаметр входит и в числитель `D_eff/D_phys`, и в знаменатель `D/P`, поэтому ошибка в нём двигает точку почти ВДОЛЬ линии. **ТРИ ПРОБЛЕМЫ МЕТОДА, вскрытые по ходу:** (1) **масштаб плавает между кадрами** — отношение периодов в пикселях high/mid обязано быть 5.098, фактически 5.02 / 5.29 / 5.39 / **4.39** (33_11 / 40_20 / purewave / specac) ⇒ общий масштаб `high` применять нельзя; теперь каждый кадр самокалибруется по СОБСТВЕННОМУ периоду образца, отчего `D/P` вообще перестаёт требовать калибровки; (2) **период брался по-разному** — у purewave смесь среднего и большого увеличения, у specac только среднее; теперь у всех только среднее ⇒ purewave 25.5 → **24.72** (−3.1 %) и потребовался ПЕРЕФИТ: `D_eff` 6.982 → **6.733**; (3) **регрессия «ширина ~ размытие» ПЕРЕОЦЕНИВАЕТ поправку** — корреляция r = 0.7…0.9 создаётся хвостом сильно расфокусированных проволок, а при ограничении слабым размытием наклон разваливается (+0.47 / +1.88 / **−0.34** / +0.16) ⇒ **вблизи наилучшего фокуса ширина от размытия почти не зависит**. **⚠ ОТОЗВАНО МОЁ ЖЕ ВЧЕРАШНЕЕ ЧИСЛО: `D = 10.91` (A14b) → `12.60`.** Устойчивая оценка — медиана ширины по самым резким 25 % проволок, проверена на порогах 1.6/1.8/2.0/2.2/2.5 мкм. **ИТОГОВАЯ ГЕОМЕТРИЯ (было → стало):** purewave 25.5/10.6 → **24.72/9.83** (D/P 0.416 → 0.398); specac 24.9/14.0 → **24.91/7.77** (0.562 → **0.312**); test_grid_40_20 38.8/18.0 → **38.87/15.99** (0.464 → 0.411); test_grid_33_11 → **31.82/12.60** (0.396). **ПОЧЕМУ ДИАМЕТРЫ СДВИНУЛИСЬ ТАК СИЛЬНО — не из-за расфокусировки, а потому что прежние `D` получены РАЗНЫМИ МЕТОДАМИ:** у `specac` 14.0 вообще не измерение (в `MANIFEST.md` записано, что блик давал 8–12 и значение вручную умножено примерно вдвое), у `test_grid_40_20` 18.0 — измеренные 17, подвинутые к номиналу 20. Только у purewave сравнивались одинаковые величины (10.6 против 9.83). **⚠ ЧЕГО ИЗМЕРИТЬ НЕЛЬЗЯ: `att-11-16-*` (4 из 8 точек)** — только кадры среднего увеличения, проволоки почти сомкнуты (`D/P ≈ 0.69`), FFT даёт 62/103/146 px вместо ожидаемых ~25; остаются на ПАСПОРТНОЙ геометрии, и именно они держат плечо восьмиточечного фита. **ОГРАНИЧЕНИЯ:** диапазон `D/P` у измеренных сжался до 0.312…0.411 (было 0.344…0.562) ⇒ наклон по 4 точкам с ошибкой 10 %; `test_grid_40_20` держится на 3 проволоках; **`specac` внутренне противоречив и НЕ РАЗОБРАН** (наш штатный метод даёт 7.77, а даже заниженная бликовая оценка в манифесте была 8–12; у него же худшее расхождение по зуму, 14 %); абсолютные `D` завышены на ~1.10 мкм аппаратным уширением. **➜ B: окончательные строки `fit_lib.GEOMETRY` — в хэндоффе** (заменяют все предыдущие варианты). **➜ D и P: изменились числа, которые вы уже используете** — таблица в хэндоффе. Отчёт `research/two_wgp/REPORT_A16_geometry_h5.md`, рисунок `a16_h5_recompute.png`. **Ниже — предыдущие записи того же дня:** **⚠⚠ A14b (2026-08-06, ночь) — ЗАМЕЧАНИЕ ВЛАДЕЛЬЦА ПОДТВЕРДИЛОСЬ: диаметр был завышен расфокусировкой, `D` 12.55 → 10.91 мкм.** Владелец: «на максимальном увеличении не все проволоки в фокусе, картинка расплывается». **Причина в инструменте, и она касается ВСЕХ образцов:** `micrograph_period.in_focus_mask` отбирает проволоки по **квантилям ВНУТРИ КАДРА** (верхние 45 % по крутизне фронта, 60 % по яркости) — критерий относительный, поэтому всегда оставляет около половины проволок, **даже если размыты все до одной**. Метод замены: измерять размытие каждой проволоки (`b` = высота пика / макс. модуль градиента) и приводить все к общему уровню резкости; 32 проволоки из 4 кадров дают `r(w10, b) = +0.773`, наклон **+0.211 мкм на px размытия**. **Уровень приведения ИЗМЕРЕН, а не выбран:** медианное размытие калибровочной гребёнки (края физически резкие ⇒ всё размытие аппаратное) = **8.69 px = 1.10 мкм**; экстраполяция к `b → 0` убрала бы и аппаратную функцию — перекоррекция (дала бы 9.08). **Итог `D = 10.91 ± 0.41` мкм**; без всякой модели лучшая отдельная проволока из 32 даёт `w10 = 10.80` мкм — жёсткая верхняя граница, согласуется. Номинал владельца 11 мкм, расхождение 0.8 %. **ГЛАВНЫЙ АРГУМЕНТ — контроль на `purewave`:** наклон **+0.195** против **+0.211** мкм/px, расхождение 8 % ⇒ связь «размытие → ширина» **АППАРАТНАЯ**, а не подгонка под образец; различается не наклон, а разброс размытия (purewave 13–27 px, этот образец **10–49 px**, впятеро шире — заводской компланарнее намотанного). **Следствие:** `D_eff` НЕ меняется (7.233 мкм, AIC −44783.7 те же — `D_phys` входит только в знаменатель, проверено повторным фитом), но `D/P` 0.395 → **0.344**, `D_eff/D_phys` 0.576 → **0.663**, закон H5 0.664 → **0.708**, **отклонение −0.088 → −0.045 — теперь ВНУТРИ разброса семи прежних образцов** (−0.079…+0.113). Формулировка прежняя: **закон H5 не опровергнут и не подтверждён**. **⚠⚠ ОТКРЫТО ДЛЯ ВСЕХ (`A16`, не начато, нужно решение владельца): поправка сдвигает и эталон — `purewave` 10.6 → 9.5 ± 2.1.** Значит опубликованные диаметры ВСЕХ образцов содержат разный по величине вклад расфокусировки, и колонка `D/P` в `MANIFEST.md` не вполне сопоставима между строками. Сплошной пересчёт перенастроил бы **сам эмпирический закон H5** (он подбирался по этим `D/P`) и сдвинул опорную линию Фаз 1–3. Артефакты `a14b_diameter_defocus.{json,png}`, отчёт дополнен §9. **Ниже — предыдущая поправка того же дня:** **⚠ ПОПРАВКА (2026-08-06, поздний вечер): вывод по закону H5 ОТОЗВАН — геометрия `test_grid_33_11` ИЗМЕРЕНА по микрофото.** Владелец указал настоящие снимки на флешке (`I:\att\WGP-11-33\`, съёмка 04.08, 9 кадров трёх увеличений + пара «фокус 0/0.12 мм»); в каталоге `research/validation/micrographs/wgp_grid_D11_P33/` до этого лежали КОПИИ снимков specac (сверено по SHA-256 и подтверждено физикой — измеренный период совпадал с specac 24.9). Калибровка перенесена с сессии 2026-07-24 (гребёнку 04.08 не снимали) и проверена дважды независимо: отношение периодов в пикселях между ступенями увеличения **5.019** против июльского **5.098** (1.5 %), согласие периода в микронах между увеличениями **0.4 %**. **ИЗМЕРЕНО: `P = 31.75 ± 0.65` мкм (номинал 33, −3.8 %), `D = 12.55 ± 0.60` мкм (номинал 11, +14.1 %) ⇒ `D/P = 0.395`, а не 0.333.** Беспорядок решётки ~22 % против ≤13 % у purewave; провода НЕкомпланарны. Период в модели Бланко ФИКСИРОВАН, поэтому фит перезапущен: **`D_eff` 7.636 → 7.233 мкм, `D_eff/D_phys` 0.694 → 0.576**, закон H5 при измеренной `D/P` даёт 0.664 ⇒ **отклонение −0.088, крайнее отрицательное из восьми образцов** (прежний разброс −0.079…+0.113, ~1.4σ). **Формулировка «закон H5 прошёл первую экстраполяционную проверку» ОТОЗВАНА: закон не опровергнут и не подтверждён.** Заодно ослабла сама экстраполяционность — 0.395 лишь чуть ниже прежнего диапазона 0.416…0.710. **ЗАТО независимое подтверждение:** при РАВНОМ числе параметров ТГц-фит предпочёл измеренный период номинальному на **7.1 AIC** — микроскопия и спектроскопия сошлись, не видя друг друга; отсюда предложение `A15` (профиль правдоподобия по `P` как ТГц-измерение периода, не начато). **Методическое: предзапись предсказания сработала не как подтверждение закона, а как детектор ошибки во ВХОДНЫХ данных.** ⚠ **B: числа геометрии в хэндоффе изменились** — вносить `{"P_um": 31.75, "D_phys_um": 12.55}`. **D и P: отзыв разослан хэндоффом.** Остальные выводы A13 (распад вырождения, ненужность `η₀`, нулевые контроли, `ε_cross` как поглотитель) от геометрии НЕ зависят и в силе. Ground truth внесён в `research/validation/MANIFEST.md`. **Ниже — исходная запись A13 (её §«закон H5» читать с этой поправкой):** **➜ ВСЕМ (2026-08-06, вечер): 8-й образец ИЗМЕРЕН И ОБРАБОТАН — `test_grid_33_11`, отчёт `research/two_wgp/REPORT_test_grid_33_11.md`.** Владелец снял полный прогон (51 сигнал + 17 фонов, 5 ч 19 мин, ни одного пропуска), расчёты возобновлены по его просьбе. **ГЛАВНОЕ 1: закон H5 прошёл первую ЭКСТРАПОЛЯЦИОННУЮ проверку** — предсказано ДО измерения `D_eff/D_phys = 0.717` (коммит `ad9818b`), измерено **0.694** (`D_eff = 7.636 ± 0.015 мкм`), отклонение −0.023 против разброса −0.079…+0.113 у семи прежних; `D/P = 0.333` лежит ВНЕ диапазона подбора закона. **ГЛАВНОЕ 2 — уточнение вердикта A3/HN14, касается всех, кто его цитирует: тройное вырождение `D_eff↔η₀↔ε_cross` есть свойство ПЛОТНОГО режима, а не модели.** Здесь corr(`D`,`η₀`) = **−0.429** против ±1.0 на плотных, `D_eff` устойчив к выбору углов (0.694 / 0.695 / 0.698, разброс 0.5%) и определён с точностью ±0.2%. Прежний вывод строился на шести образцах, все плотнее 0.46 ⇒ **формулировку «вырождены» снабжать оговоркой «в плотном режиме»**. **ГЛАВНОЕ 3: явная утечка M5 в разреженном режиме НЕ нужна** — `η₀ = 3.7e-5 ± 3.1e-5` (1.2σ, ноль) при модельно-независимой полной утечке `η = 0.0528` (самой большой в проекте); Бланко сам даёт нужный `t∥` ⇒ дефицит тени, ради которого строилась M5, — тоже явление плотного режима (согласуется с `test_grid_40_20`, dAIC +4). **ГЛАВНОЕ 4: видно, как обрезка углов «создаёт» `ε_cross`** — dAIC(M5_eps−M5_1wgp) = −2.2 на полном диапазоне против −131.0 (без второй тени) и −40.8 (обрезка 0…90); `\|ε\| = 0.00138` в 25 раз меньше отозванного 0.0345. **КАЧЕСТВО: оба контроля, заложенные в порядок съёмки, дали НОЛЬ** — дрейф (один угол, начало против конца сессии) тень −0.047 ± 0.168 дБ, ярко +0.010 ± 0.016 дБ; π-периодичность (тень A против тени B, сняты рядом по времени, 11 пар) +0.037 ± 0.109 дБ. Контраст с purewave (5.10 и 6.56 дБ) объясняется оптикой: **коллимированный пучок 10–12 мм, образец НЕ в перетяжке**, ~10% площади апертуры за измерение против ~0.3% ⇒ угол и участок решётки развязаны. **Владельцу рекомендовано снимать так и дальше.** Сверки: модельно-независимый `θ₀ = −1.277 ± 0.035°` против `angle_offset` M5 −1.260° (расхождение 0.017°); `A₄/A₂ = 0.2518` против точных 0.2500. Урок A0 воспроизведён на свежих данных: обрезка (0,90) почти не сместила `θ₀`, но ошибка выросла **в 4.5 раза**. **⚠ ЧТО НЕ РЕЗУЛЬТАТ:** `γ` закреплён (`vary=False`, 2.0) — HN5 не проверялась; `τ_leak` не идентифицирован (1.58/0.92/0.19 пс); `DW = 1.086` — остаток структурен. **ОТКРЫТО:** минимум пропускания при +94.19° при офсете −1.28° ⇒ ~3° необъяснены (та же форма, что у specac ~84°, T16); это **первый образец, на котором HN13 можно проверить без оговорок** — дрейф и неоднородность измерены и равны нулю. Иллюстрация `research/results/two_wgp/a13_test_grid_33_11.png`. **➜ B:** внести геометрию в `fit_lib.GEOMETRY` (хэндофф). **Ранее в тот же день: заведён 8-й образец `test_grid_33_11`** (P = 33, D = 11, **D/P = 0.333** — самый разреженный в проекте; прежний диапазон 0.416…0.710). Владелец начинает измерения; по его просьбе разложена дорога файлов-заготовок `data_pool/test_grid_33_11/` — 69 пустых файлов (51 сигнал + 17 фонов + META), маршрутный лист `MEASUREMENT_ORDER.md` с предполётным чек-листом, машинный план `research/two_wgp/plan_test_grid_33_11.json`, генератор `prepare_road_test_grid_33_11.py` (`--status` показывает прогресс). Имя взято из зарезервированной строки `DATASETS.md`, неоднозначность «D≈33? P≈11?» снята владельцем. **Значимость: это первая ЭКСТРАПОЛЯЦИОННАЯ проверка закона H5** вниз по `D/P`, а не точка внутри интервала. **Предсказания зафиксированы ДО измерения:** `D_eff/D_phys = 0.717`, `η = 0.04 ± 0.02`, экстинкция 24…30 дБ, минимум не обязательно при 90°. План углов построен на уроках A0 (полный диапазон −100…+100 вместо обрезки (0,90): `cond` 2.14 против 236.3) и A7 (порядок блоков разделяет дрейф и физику — «тень A против тень B rep2» рядом по времени = физика, «тень B rep1 против rep2» = чистый дрейф). Пустая дорога `DataManager` не подхватывает — проверено. **➜ D (2026-08-05):** передано ТЗ на учебный слой — `research/two_wgp/BRIEF_FOR_D_teaching_ladder.md` (заказ владельца: от базовой электродинамики через линеаризацию и линейный фит, лестницу усложнений с обоснованием, корреляцию, нелинейную аппроксимацию, анализ остатков и правдоподобия, метод анализа). 11 модулей, все числа со ссылками, нумерация вписана в очередь D4 без её слома. **Эффект корреляции в линейном фите ПРОЯВЛЯЕТСЯ и уже измерен:** `cond(XᵀX)` **2.14** (полный диапазон, 19 углов) против **236.3** (продакшн-обрезка 0…90, 10 углов), `σ(θ₀)` при 1 % шума **0.096° против 0.944°** — в 9.9 раза хуже ДО всякого шума; на урезанном диапазоне гармоники 2 и 4 неортогональны, отбрасывание 4-й смещает `θ₀` на −2.3…−4.8°. Это тот же механизм, что даёт вырождение `D_eff↔η₀↔ε_cross`, но в замкнутой форме ⇒ порядок модулей 04 (линейный фит) → 05 (корреляция) → 07 (нелинейный) существенен. Готовое к переиспользованию: `NOTE_malus_linearization.md`, `malus_linearization.py` (12/12). **➜ P1:** пакет для недельного отчёта готов — `research/two_wgp/BRIEF_FOR_P_week_20260728_0804.md` (постановка эксперимента, все числа со ссылками, анализ дифракции, карта файлов). **Читать §5 «Чего не должно быть в презентации»**: отозваны `\|ε_cross\|=0.0345`, вывод «загадка D_eff не связана с нерегулярностью» (HN15 → НЕ ПРОВЕРЕНА), значения `D_eff/D_phys` с обрезки (0,90); четыре аттенюаторных набора = ОДИН прибор. Готовая иллюстрация — `research/results/two_wgp/a8_diffraction.png`. Апертура purewave — **35 мм** (владелец), протокол скана пересчитан: шаг 3 мм → 12 позиций ≈ 5 ч. **⚠ ПОПРАВКА (2026-08-04, тот же день): вывод по HN15 ОТОЗВАН.** Владелец уточнил схему: поляризатор в перетяжке **1.5–2 мм**, намеренно смещён так, что тень попадает на ЗАВОДСКИЕ зоны, а максимум — на восстановленные. Узкий пучок, смещённый от оси вращения, при повороте описывает окружность по апертуре ⇒ **угол и участок образца ПЕРЕПУТАНЫ**. Значит `η` и `D_eff` измерены по заводской зоне, HN15 → статус **НЕ ПРОВЕРЕНА**; совпадение с законом H5 становится тривиальным; расхождение двух дней перестаёт быть гипотезой (переустановка сменила участок в тени). **Всем, кто интерпретирует угловые сканы: проверяйте центровку образца на оси вращения** — иначе пространственная неоднородность подмешивается в `U(θ)` и НЕ обязана быть чётной по θ (кандидат-объяснение асимметрии A6/HN13, 9.3σ/4.2σ; не проверено). Период по дифракции уточнён: между ±1 было **17.5 мм**, а не 1.75 ⇒ `P = 23.4 ± 0.8 мкм`, с микрофото 25.5±0.9 совместимо (1.8σ). **A9: написан протокол** `research/two_wgp/PROTOCOL_aperture_scan.md` — скан поперёк апертуры при фиксированном угле (карта `η(x)` + якорные развёртки в 3 зонах + оптический скан как быстрая карта регулярности; критерий вывода зафиксирован до измерений; конвенция имён согласована заранее). **Ранее A7 (2026-08-04): `purewave` РАЗБЛОКИРОВАН и введён в оборот (7-й образец).** Владелец домерил его за два дня; имена приведены к конвенции (`normalize_purewave_data.py`, канонические КОПИИ рядом с журнальными оригиналами, как в `specac`): 30 трасс, 24 угла −95…110, SHA-256 и массивы сверены. **ГЛАВНОЕ: два дня НЕ сшиваются в тени.** π-периодичность даёт модельно-независимую проверку (−95° ≡ +85°): `U(−95)/U(+85) = 0.309` (−5.10 дБ), `U(−85)/U(+95) = 0.221` (−6.56 дБ); модельно-независимая `η` 0.0185 (день 1) против 0.0429 (день 2) — **×2.3**, при этом `θ₀` СОВПАДАЕТ (+1.700±0.244 и +1.891±0.382; подтверждено independently `angle_offset` M5 +1.655/+1.786) ⇒ это не сдвиг угловой шкалы, а изменение УТЕЧКИ. Цена смешивания: `DS_rmse` 2.211 дБ на продакшн-диапазоне (0,90), который дни СМЕШИВАЕТ, против 0.490 (день 1) и 0.801 (день 2). **Следствие для всех: `η` воспроизводима в пределах ОДНОЙ УСТАНОВКИ образца, а не образца — сдвиг на одном приборе сопоставим со всем разбросом между образцами.** M5-мультистарт: `D_eff/D_phys` 0.534 (полн.) / **0.659 (день 1)** / 0.518 (день 2) / 0.605 (0,90). **HN15 НЕ ПОДДЕРЖАНА:** `purewave` — единственный образец с ИЗВЕСТНЫМ дефектом регулярности (провисший центр переклеен владельцем), но его дефицит `D_eff` наименьший, и он ЛУЧШЕ ВСЕХ ложится на закон H5 `1−0.85·D/P = 0.647` (откл. **+0.012** против −0.023…+0.113 у остальных шести) ⇒ загадка `D_eff` НЕ масштабируется с нерегулярностью решётки. **A8 (оптический контроль):** дифракция лазерной указки, 3+3 снимка; заводская зона регулярнее восстановленной по всем безразмерным метрикам (острота спектрального пика ×1.96, стабильность шага, глубина модуляции, воспроизводимость между снимками +0.427 против +0.254) — но это ТЕНДЕНЦИЯ (3 на 3, масштаб кадров не фиксирован). **⚠ ВЛАДЕЛЬЦУ: числа дифракции не сходятся с микрофото** — 1.75 мм между первыми порядками при L=315 мм дают d = 117…234 мкм против 25.5±0.9 мкм; ожидаемо 8.03 мм между соседними и 16.06 мм между ±1; значение 17.5 мм для размаха дало бы 23.4 мкм (согласуется). Прошу перепроверить единицы. **Ранее A3 (2026-08-04): HN14 REJECTED — `ε_cross` НЕ константа тракта.** χ² однородности комплексной пары (Re, Im): все 6 датасетов `761.6/10 dof`, p=3.8e-157; группа «аттенюатор» `677.1/6`; группа «одиночный WGP» `45.0/2` — отвергнуто везде, и на полном диапазоне, и на обрезке (0,90). **Причина установлена:** тройное (не двойное) вырождение `D_eff↔η₀↔ε_cross`. Решающее сопоставление — модельно-НЕзависимая утечка `η` (гармоники A0) почти постоянна ВНУТРИ группы (аттенюатор ×1.3, одиночный WGP ×1.1) и честно различает группы (0.012 против 0.033), а на тех же данных `η₀`(M5) гуляет ×57, `\|ε\|` ×7 ⇒ **воспроизводима суммарная утечка, а не её разложение**. **Закрыт открытый пункт A6** (фит на полном диапазоне): `D_eff/D_phys` 356att 0.678→0.373, test_grid 0.943→0.586; загадка `D_eff` НЕ снимается (0.371…0.586 по 6 образцам). **Методическое:** 3–20 бассейнов на 21 старт, в глобальный попадают лишь 22.2% стартов ⇒ правило A2 «тёплый лучше холодного» ОТМЕНЕНО, нужна СЕТКА стартов. **Отзыв числа:** `\|ε_cross\|=0.0345` (356att, A2) — 4-й бассейн из 11, на 192.5 AIC выше глобального → хэндофф `ОТ A К D`. **⚠ `purewave` BLOCKED:** все 69 файлов трасс — **0 байт** (заглушки от 27.07), подтверждено `t20_weighted_fit.py` (код 0); условия разблокировки — в `state.json.blocked`. **⚠ ПЕРЕИМЕНОВАНИЕ ДАННЫХ (санкция владельца 2026-08-04):** `356att`/`series1..5` → `att-11-16-356`/`-s1`/`-s2`/`-s3`/`-s4-artifact`/`-s5-artifact` — это ОДИН прибор ATT-11-16-CA85 S/N #02721 в разных условиях съёмки. 142 файла данных + **39 файлов кода в зонах B, C и замороженном продакшне** (владелец санкционировал разовый выход за зону, чтобы не оставлять проект сломанным). Числа НЕ изменились: SHA-256 всех файлов совпали, пересчёт A3 воспроизвёл результаты **бит-в-бит** (0.000e+00), тесты 3/3 OK. Откат — `rename_attenuator_data.py --rollback`. **C: прошу прогнать свой валидатор.** **Ранее (done):** **A0**: гармоническая линеаризация Малюса (точный базис 5-мерный, 4-я гармоника несёт фазу утечки); цена `ANGLES_LIMIT_2D=(0,90)`: σ(θ₀) хуже в 9.9 раза. **A6**: асимметрия `U(θ)` НЕ сводится к `angle_offset` (9.3σ / 4.2σ) → **HN13 REJECTED**; `M5_eps` выживает (1.6σ). **A4**: калькулятор затухания 9/9. **Мои открытые запросы:** к B (`build_experiment` с `angles_limit`+`mask_angles`), к D (HN12→HN13 в DRAFT/STATE), к L (первоисточники по 2ω/4ω). Далее A5 | 2026-08-07 (закрытие) |
| **B** техническая гигиена (рефактор/дубли/инварианты/работоспособность кода) | **⚠ РОЛЬ ПЕРЕОПРЕДЕЛЕНА ВЛАДЕЛЬЦЕМ 2026-08-08.** Было «численное ядро + ревьюер эквивалентности», стало **техническая гигиена**: рефакторинг, устранение дублей, инварианты общего кода, **работоспособность продакшн-кода**. Два правила сохранены неизменными: **нулевая доля в физических выводах** (B подписывается под неизменностью числа, не под его правильностью) и **допуск ноль** со снимком «до» перед любой правкой. Режим — **по вызову, не постоянной сессией**; простой между заходами норма. Зона расширена: `unified_optimizer/**` — **по разовой санкции владельца на конкретный дефект**, санкция не переносится на следующий. **➜ ЗАПУЩЕНО (ORCH, 2026-08-08, санкция владельца): починка двух падающих функций продакшна** — `optimizer_2d.py:122,124` (`p_max_um` нигде не определено) и `optimizer_lmfit.py:92` (`model_blanco` не импортирован); обе валят `run_pipeline --method 2d\|both`, **а `both` — дефолт**. **Аргумент безопасности (уточнён 2026-08-08 после сверки с историей):** регрессию внёс коммит **`b0372ce` от 2026-07-19** (`Antigravity Agent`, «Внедрение estimate_deff_initial…»), удалив строку определения; правка **восстанавливает её дословно** (сверено `git show`). До 19.07 ветка работала и числа производила — `results/overnight_results.json` содержит блок `2D_Complex` (`P_eff 16.729`, `D_eff 4.820`), поэтому формулировка «не произвела ни одного числа за всю историю» **неверна и не используется**. Верно: с 19.07 ветка не производит ничего, 1D-ветку правка не задевает по построению. **⚠ И отдельно:** `b0372ce` заодно сменил начальное приближение (`d_init_um = estimate_deff_initial(...)` вместо `config.D_DEFAULT`), это его задуманное изменение и оно остаётся ⇒ первые числа `--method 2d` после починки — **новая базовая линия, а не воспроизведение** до-июльских; сравнивать с `overnight_results.json` нельзя. Работа в изолированном worktree, мерж после ревью ORCH. **⚠ ПОПРАВКА ORCH К ПЕРЕДАЧЕ B (замерено 2026-08-08): цифра «копии фильтра 5 → 20 в 8 файлах» НЕ подтвердилась.** Метрика считала *упоминания*, включая передачу параметра `angles_limit=`. Настоящих переопределений фильтра в `research/` — **пять, те же самые, что были 28.07** (`fit_lib.py:68`, `t20_weighted_fit.py:72`, `t6_hn6.py:38`, `t6_hn7.py:42`, `t6_hn8.py:91`), плюс два в замороженном продакшне. `a3_eps_stability.py` и `a6_angle_symmetry.py` фильтр НЕ переписывают — зовут `exp_builder.select_angles`, то есть зона A поступила правильно. `t6_hn2.py` числится нарушителем по комментарию «*was* an inline copy», сам вызов давно на ядре. **Настоящий риск другой: два билдера эксперимента на критическом пути** — `fit_lib.build_experiment` (жёстко `config.ANGLES_LIMIT_2D`) и `two_wgp/exp_builder.build_experiment` (параметризованный + `mask_angles`); на первом стоит базовая линия Фаз 1–3, на втором — все результаты A3/A6/A7/A13/A16. Опасность молчаливая: `fit_lib.py:81` считает водяную маску усреднением ПО УГЛАМ ⇒ маска зависит от набора углов, и наивный `angles_limit` без спутника `mask_angles` тихо сделает числа несравнимыми с Фазами 1–3. **Новые кандидаты в список исключений машинной проверки** (появились 07–08.08, в передаче их нет): `course/figures/fig02_transmission.py:38`, `fig03_drude.py:37` — прямые вызовы Бланко для иллюстраций курса, легитимны. **Порядок трёх задач Р-1 переставлен ORCH:** (1) вынос `GEOMETRY` → `research/experiments/geometry.py` — **со сроком**, пока геометрия внутри `fit_lib.py`, каждая правка диаметров формально требует сессии B, а владелец вот-вот утверждает диаметры; (2) `build_experiment(angles_limit=None, mask_angles=None)` — снимает второй билдер, `exp_builder` становится обёрткой; (3) машинная проверка инварианта — в режиме предупреждения, с базовой линией. Мелочь: `research/results/HN8/hn8_ablation.json` держит `P_um = 40.0` при геометрии `38.8`; мёртвые `from scipy import stats` в двух файлах. **Ник B1 `retired`** (закрыт 2026-08-07), лок свободен, передача дел — `research/experiments/HANDOFF_B_20260807.md` (читать первым, но числа §3.1 сверять с поправкой выше). Итог B1: `model_core` доведён (итер. 1–2), бит-в-бит 47 AIC / 1025 полей, 0 расхождений, ускорение 7–15× | 2026-08-08 |
| **C** app | **⚠ РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-08-07, вечер): гейт `C6_v02_acceptance` и запрет на C3–C5 сняты целенаправленно.** Натурная приёмка на Win7-ПК у спектрометра откладывается; вместо неё владелец напрямую заказал **GUI + автономный `.exe`-предрелиз** (desktop-обёртка над существующим `api.Attenuator`/CLI, PyInstaller, ветка v0.3). Физика `core/` не трогается, GUI вызывает тот же `Attenuator`, что и CLI — оба принятых решения зоны C (относительный режим по умолчанию; неопределённость по разбросу между сеансами) в силе. Прежняя запись «exe не нужен» (2026-07-28 21:50, довод — Win7-комплект на голом Python 3.8) для **этого** релиза отменена этим же указанием владельца; сам Win7-комплект (данные приёмки) не трогается, идёт отдельным треком. Ведёт ORCH с прямой санкции владельца (роль C2 отдельно не запущена), сборка — в изолированном worktree, мерж в `main` после проверки. **stopped** — чат **C1** закрыт владельцем 2026-08-07, лок свободен. Следующий чат берёт ник **C2** **➜ ЧИТАТЬ ПЕРВЫМ: `attenuator_app/HANDOFF_C_20260807.md`.** Открыта одна задача — **C6_v02_acceptance**: **работа ЖДЁТ ВЛАДЕЛЬЦА**, прогон по распечатанному чек-листу ред.2 на Win7-ПК у спектрометра. До его обратной связи **не начинать** C3/C4/C5 — иначе замечания придётся вносить дважды. Release gate: **ни одна версия не выпущена и не одобрена** **⚠ ВАЛИДАТОР ЗОНЫ C БЫЛ СЛОМАН С 04.08 И МОЛЧАЛ — починен 07.08, физика не пострадала.** Переименование данных Сессией A (`eda293e`) прошло по `ROTATES` внутри `tools/validate.py`, но **не** по умолчанию `--datasets` и не по префиксам групп ⇒ 0 найденных файлов, текст «no data for validation», **код возврата 0**. Дефект пролежал 3 дня, потому что чат C в это время не работал и просьбу A «C: прошу прогнать свой валидатор» исполнить было некому. **Числа воспроизвелись бит-в-бит: selftest 13/13; validate 69/69 = 100 %, смещение +0.35 дБ, RMSE 1.23 дБ (паспортная 1.39), разница групп «вращался первый / второй» +0.15 дБ, запас интервала ×1.67** ⇒ **запрос A к C ЗАКРЫТ**, переименование физику не затронуло. Поставлена защита от тихого нуля: «файлов не найдено» отделено от «мало углов», `main()` возвращает код 1. **➜ ВСЕМ, кто двигает или переименовывает `data_pool/**`:** зона C зависит от имён и расположения файлов, и это уже ВТОРОЙ такой случай (первый — реорганизация в подкаталоги 28.07, `glob`→`rglob`); предупреждайте C или проверяйте её валидатор сами. **➜ C2, техника:** ветка `session/C` **полностью влита в main** (`713f807` — предок `main`), мержить нечего; worktree `../TUO-C` чист, но **отстал на 57 коммитов** — работать в нём без обновления значит работать по данным ДО переименования, то есть воспроизвести тот же дефект. **➜ НЕ СИНХРОНИЗИРОВАТЬ:** Win7-комплект `../win7_attenuator_kit` и распечатанный чек-лист намеренно остались на СТАРЫХ именах (`356att_*`) и согласованы между собой; приводить к именам репозитория — только после приёмки и сразу все три части (данные, код, чек-лист) | 2026-08-07 (закрытие) |
| **D** статья + образоват. слой | **✅ Курс §0–§10 + приложения A/B завершены (2026-08-08), непрерывный прогон закрыт.** 76 страниц, сборка чистая (проверено дважды независимо — фоновым агентом и ORCH), терминология/жаргон-фильтр по STYLE.md пройден (проверено grep'ом по всем секциям, единственное совпадение — в служебном комментарии, не в тексте). Отозванный `|ε_cross|=0.0345` использован единственный раз строго как иллюстрация ошибки мультистарта (STYLE.md §6, разрешённое исключение), не как результат. Правило «один параграф → PDF → стоп» (`course/PLAN.md` §4) снова действует по умолчанию — разовое исключение закрыто. Далее — по команде владельца (правки/новые параграфы). stopped (ник **D3** `retired`, закрыт владельцем штатно 2026-08-06, лок FREE) — продолжение под ником **D4**, готовый промт и передача дел `research/paper/course/LAUNCH_PROMPT.md` **D9 — LaTeX-проект курса** `research/paper/course/**`: **§0 и §1 написаны по `course/STYLE.md`** (23 стр., 6 рисунков, сборка чистая), отправлены владельцу, замечаний не поступало; далее §2 (Бланко) — только по команде владельца **§1 «Матрицы Джонса» (2026-08-06):** каскад и поворот, закон Малюса в схеме с анализатором (**$\cos^4\theta$, а не $\cos^2\theta$**), реальный поляризатор $\mathrm{diag}(1,\eta e^{j\delta})$, три источника малого сигнала в скрещённом положении (корр. 0.73), **теорема о выровненном анализаторе** с доказательством, границы формализма Джонса. **⚠ Правка чисел, касается всех, кто цитирует методичку 01:** коэффициент экстинкции при $\eta\approx0.012\ldots0.036$ равен **29…38 дБ**, а не «30…70 дБ», как стояло в `primers/01_jones_polarization.md` ($\mathrm{ER}[дБ]=-20\log_{10}\eta$); методичка исправлена. — — —  **§0 ред. 2 (2026-08-06, D3):** физика сохранена, заново написаны подача и терминология. В тексте нет имён сессий, путей, номеров гипотез и кодовых имён наборов; образцы обозначаются через $D/P$. Введены общепринятые **канал пропускания / канал подавления** вместо «яркий/тёмный», «утечка» → **просачивание**. Врезка `projectfact` → **`example` («Из эксперимента»)**. Заведено служебное приложение **«Происхождение числовых примеров»** (`sections/Z_provenance.tex`): ключ примера → источник, соответствие «описание образца → набор данных», список непригодных чисел; отключается одной строкой `\provenancefalse`. В `meta.tex` появился раздел **«Откуда взяты числа: экспериментальная установка»** — единственное описание установки, нейтральным языком. **Технические грабли для всех, кто собирает LaTeX:** символы `⚠`/эмодзи в `.tex` валят pdflatex; открытый в просмотрщике `main.pdf` блокирует запись — `build.ps1` теперь собирает в `build\` и чистит устаревшие `main.aux/.toc` в корне (иначе оглавление собирается по прошлой редакции). — — — **⚠ СМЕНА ЖАНРА (2026-08-06):** курс пишется как **самостоятельная учебная литература**, а не как внутренняя документация. Формулировка владельца: «мы ссылаемся на проект, но не живём в нём». Читатель — не участник проекта: имена сессий, пути в репозитории, номера гипотез `HN##`, кодовые названия наборов (`356att`, `test_grid_40_20`) в тексте **не появляются**; терминология — общепринятая научная; реальные измерения подаются как результат эксперимента («плотная решётка $D/P=0.71$»), а прослеживаемость выносится в служебное приложение. Регламент — **`research/paper/course/STYLE.md`** (таблица замен жаргона + список чисел, непригодных для учебных примеров). **Для сессий, предлагающих темы и фактуру для курса через HANDOFFS:** присылайте физику и числа, но внутренние обозначения проекта будут переведены на общепринятые термины. Физика §0 признана верной — переписываются подача и терминология. **Ранее (формат):** LaTeX-проект — параграфы отдельными файлами, сборка из `main.tex` (11 закомментированных `\input`), графики генерируются скриптами внутри проекта, после каждого параграфа PDF владельцу и **пауза до его комментария** (поэтому `/loop` у D остановлен). Заведено: `course/{main.tex, preamble.tex, meta.tex, build.ps1, README.md, PLAN.md}`, `course/sections/00_electrodynamics.tex`, `course/figures/` (4 скрипта → векторные PDF; стиль скопирован из зоны P и адаптирован под печать). **§0 «Электродинамика: необходимый минимум» — 11 страниц, 4 рисунка**, отправлен владельцу. Технические находки для всех, кто соберётся собирать LaTeX в этом репозитории (записаны в `course/README.md`): **`latexmk` в этой установке MiKTeX не работает** — требует `perl`, которого в системе нет, собираем тремя проходами `pdflatex`; **ударения над кириллицей (`\'а`) валят сборку** с `Invalid UTF-8 byte sequence`. — — — **Ранее в этой же итерации:** **ТЗ `research/two_wgp/BRIEF_FOR_D_teaching_ladder.md` принято целиком**, программа перенесена в `research/paper/STATE.md §3`; прежняя очередь D4 не сломана (старые темы = модули 02/03/07/08/09, заказ A по Малюсу = модуль **04**). Жёсткий порядок **04→05→06→07** зафиксирован. Написана **`primers/00_electrodynamics_minimum.md`** — плоская волна и комплексная амплитуда; фаза как измеряемое время ($\tau_{\rm leak}=0.19$ пс из наклона фазы ↔ сдвиг пика $-0.31$ пс); **поле против интенсивности** ⇒ когерентный перекрёстный член ⇒ **5 гармоник, а не 3** (невязка $1.4\times10^{-16}$; $A_4/A_2$ 0.2458…0.2549 против идеальных 0.25); решётка как анизотропный лист (пропускает **поперёк** проволок); подволновой режим $P/\lambda\lesssim0.2$ и эванесцентность высших порядков; $D/P$ как главная ручка (0.416…0.710); оговорка про пучок 1.5–2 мм = 1–10 длин волн. **⚠ ЗАКРЫТЫ висящие пункты зоны D:** (1) `HN12→HN13` в `DRAFT.md`, все 3 места — **пункт ORCH снят**; (2) битый DOI Manabe & Murk `…884786` → **`…838786`** в `RELATED_WORK.md`; (3) **P1 закрыт** — Blanco A., Fonti S., Piacente A. // Infrared Physics. 1986. Vol. 26, № 6. P. 357–363, DOI `10.1016/0020-0891(86)90058-8`, внесено в `RELATED_WORK §6, §7`; (4) ⟨?⟩ $D/P$ у `test_grid` = **0.464**. **⚠ Правка чисел статьи:** из Abstract и `DRAFT §4.3` **вычищены $D_{\rm eff}/D_{\rm phys}$ с обрезки углов** $(0^\circ,90^\circ)$ — вместо трёх смещённых значений (0.43…0.64) стоит таблица полного диапазона по семи наборам (**0.371…0.659**, ТЗ §11.3) с явным предупреждением о смещении и с оговоркой «`att-11-16-*` = один прибор, а не четыре образца». Далее: модуль **02** (Бланко). Ждём от A/B: свод чисел для `DRAFT §4.2/§4.7` (там ещё старый свод из `SYNTHESIS`) | 2026-08-05 |
| **L** литература/обзор + метапоиск | **stopped** — чат **L2** закрыт владельцем 2026-08-07, лок свободен (ранее L1, `exit` 2026-07-31). Следующий чат берёт ник **L3** **➜ ЧИТАТЬ ПЕРВЫМ: `research/papers/HANDOFF_L_20260807.md`** — полная передача роли. **L3_supply** — `ОТ A К L` **done**, `ОТ D К L` частично (P1 done, P2/P3 частично, P4/P5 не начаты) **P1 ЗАКРЫТ: ссылка на аналитическое ядро модели была недействительной.** Blanco 1986 = *Infrared Phys.* **26**(6):**357–363**, DOI `10.1016/0020-0891(86)90058-8`; запись «IJIMW 7(11):1611–1629» — фантом (обойдены все 239 записей журнала за 1986–87: стр. 1605–1629 занимает статья о метанольном лазере). `model_blanco.py` реализует именно её (сверено по $C_m$, $A_1$, $A_2$, $f_a$, $f_b$). Границы автора: $d/\lambda<0.2$, $d/p<0.1$, приемлемо до **0.5** — наш $D/P=0.71$ **вне** области. Бонус выдан: все 11 ссылок Бланко с DOI, включая эталон **Volkov 1982 при $d/p=0.5$**. **АУДИТ DOI КОРПУСА: 34 DOI, 1 битый** — Manabe & Murk `…884786` **404** → верный **`…838786`** (был в 6 местах, в т.ч. в тексте запроса D2 — **D, поправить в статье**). **P3 отрицательный:** 141+24 прямых потомка затравок, плотный режим $D/P>0.5$ не разработан. **Запрос A done:** канон — Hauge 1980; связка «когерентность → 4ω → относительная фаза» **не найдена** (заявка на новизну, с оговоркой). Артефакты: `papers/litrev/answers/*`, `litrev/tools/doi_audit.py`. Далее: P2-выжимки (тексты закрыты), P4/P5, 2-й уровень графа, Kaltenecker на гармоники | 2026-07-29 19:40 |
| **P** отчётные презентации | **stopped** — чат **P1** закрыт владельцем 2026-08-07, лок освобождён штатно (`P.lock` удалён, блок → FREE). Следующий чат берёт ник **P2** **➜ ЧИТАТЬ ПЕРВЫМ: `research/presentations/HANDOFF_P_20260807.md`** — полная передача роли. Кратко: зона развёрнута с нуля, задача `P1_weekly_20260728_0804` **DONE** (в двух редакциях), очередь — `P2_weekly_next` (не заблокирована) и `P3_deff_deepdive` (ждёт утверждения геометрии владельцем) **Что оставлено проекту.** Зона заведена: `research/presentations/{STATE.md, STYLE_MANIFEST.md, HANDOFF_P_20260807.md, weekly/2026-08-04/}`. Отчёт `weekly/2026-08-04/REPORT.md` (Marp, **8 слайдов**) + `SOURCES.md` (число → артефакт-источник) + 6 графиков (`figures/`: `pstyle.py` + `make_figures.py` на литералах, `make_overlay.py` и `make_micrographs.py` — по данным). **⚠ Четыре постоянных решения владельца по оформлению** (первая редакция на 22 слайда в тёмной теме забракована): светлая тема везде; 8 ± 2 слайда; минимум выносок на графиках, объяснение текстом; наложение теории и эксперимента в двух шкалах — обязательный слайд. Записаны в `STYLE_MANIFEST.md`. **⚠ Владелец 2026-08-04 разрешил зоне P строить графики ПО ДАННЫМ других зон** — с запретом на фит: параметры берутся готовыми из артефакта зоны-автора (границы — `STYLE_MANIFEST.md §4а`). **Содержание отчёта:** движок недели 28.07–04.08 — Сессия A (A0/A3/A4/A6/A7/A8/A9); HN13 и HN14 отвергнуты, HN15 → «НЕ ПРОВЕРЕНА»; воспроизводима суммарная утечка `η`, а не её разложение (×57/×7) ⇒ вырождение ТРОЙНОЕ; B/C/D/L — «нет новой активности за неделю» (проверено по ACTIVITY, а не додумано). **⚠ Числа отчёта устарели 06.08** после A16 (геометрия всех образцов) — `fig1` построен на старых литералах, обновлять только после утверждения диаметров владельцем; см. `HANDOFF_P_20260807.md §4`. **К зоне A (§5 передачи, не вердикт):** `DS_rmse` в `a3_eps_stability._metrics` выбирает глубокую тень как `angles >= percentile(angles, 75)` — по ЗНАКОВОМУ углу, поэтому −95/−85° в метрику не попадают, хотя отклонение там ≈3.2 дБ. | 2026-08-07 |
| **ORCH** | **Разморожено (2026-08-07/08, владелец лично вёл ORCH-сессию в диалоге, не через `/loop`).** Сделано за этот заход: слияние двух разошедшихся веток (19 vs 34 коммита, 4 конфликта, без потерь), добавлена роль **P** в `PROJECT.md`/`CLAUDE.md`, зона **C**: GUI (tkinter) + автономный `.exe` (PyInstaller) написаны, проверены (selftest 13/13, validate 69/69, независимо дважды), смержены; попутно найден и исправлен реальный баг (`MotionCommand` был подписан `[wgp1]`, физически вращается второй поляризатор — теперь `[wgp2]`); написано и смержено `docs/attenuator_app/08_GUI_USER_GUIDE.md`. Зона **D**: курс `research/paper/course/` целиком (§0–§10 + приложения A/Б, 76 стр.) — §2 обычным порядком, §3–§10+A+Б одним разовым исключением из паузы «PDF → ждать отзыва» (санкция владельца, закрыто, правило снова по умолчанию). Всё запушено в origin. **Открыто для следующей ORCH-сессии:** (1) старые хэндоффы ниже — НЕ перепроверены в этом заходе, статус на 2026-08-07 неизвестен, перед тем как на них полагаться — сверить с `HANDOFFS.md`/`QUESTIONS.md` заново; (2) владелец ждёт рекомендацию по направлению развития проекта (после освоения новых инструментов/протокола) — не сформулирована, следующий шаг. **Заморожено** (2026-08-07, решение владельца — см. `ACTIVITY.md`): координационная работа встала на паузу, 7 заходов протокола против 0 в `research/` за 2026-08-03…06. С того же дня — миграция на штатные механизмы Claude Code вместо самодельных (см. `coordination/MIGRATION.md`): реестр `NICKS.md` закрыт, живой статус здесь убран. Отложено (не отменено): `--touch` для `.lock`, Фазы C/D/E `SCHEDULING.md`, режимы ведения, извлечение в шаблон-репозиторий. Открытые хэндоффы: `B→A build_experiment(angles_limit,mask_angles)`, `A→D primer 01b Малюс`, `D→A/B свод чисел Results`, `L→C переименованный корпус`, `D↔L P4/P5` | 2026-08-07 |

## Легенда владения (полностью — OWNERSHIP.md)
A→`research/two_wgp/`+`research/hypotheses/` · B→`research/experiments/model_core.py`+shared-code · C→`attenuator_app/` ·
D→`research/paper/` · L→`research/papers/` (вкл. свёрнутый `research/papers/litrev/`) · P→`research/presentations/` ·
ORCH→`coordination/`,`CLAUDE.md`,main `research/state.json`.

## Активные «замки» намерений
См. `coordination/sessions/*.md`. Перед правкой общего файла — проверь их. Каждый файл содержит
блок «🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, имя держателя, таймаут
простоя 15 минут.
