# STATE — Сессия C (attenuator_app)

> Машина задач сессии C. Обновляется при каждой итерации. Главная линия исследования — `research/state.json` (ORCH).

## Текущая версия
**v0.2** — CLI + библиотека `Attenuator`, паспорт ATT-11-16-CA85 #02721, 13/13 selftest, 69/69 validate.
**Статус релиза: НЕ выпущена, НЕ одобрена владельцем** (см. «Версии и одобрение владельца»).

Полное описание возможностей, структуры и запуска — `attenuator_app/README.md`.
Проектная документация — `docs/attenuator_app/` (7 документов).

## Версии и одобрение владельца (release gate) — правило с 2026-07-28

**Указание владельца (2026-07-28 18:51):** ни одна версия программы НЕ считается выпущенной
и НЕ передаётся никому, пока владелец лично не воспользовался ею и не дал явное добро.
Каждая версия получает флаг одобрения и запись обратной связи в таблице ниже.
Автотесты (selftest/validate) — необходимое, но НЕ достаточное условие релиза.

| Версия | Готовность кода | Одобрение владельца | Обратная связь владельца | Релиз |
|---|---|---|---|---|
| v0.1 | устарела (заменена v0.2) | НЕТ | — | НЕ было |
| v0.2 | selftest 13/13, validate 69/69; **UI переведён на английский** (2026-07-28, решение владельца: прибор в Китае, Win7-ПК не читает кириллицу) | **ожидает ручной проверки** (C6_v02_acceptance, отложена — см. ниже) | ожидается (прогон по чек-листу ред.2 на Win7-ПК у спектрометра) | НЕ было |
| v0.3 (в работе) | GUI (tkinter, поверх `api.Attenuator`, без новых расчётов) + автономный `.exe` (PyInstaller) | заказано владельцем напрямую, 2026-08-07 | — | предрелиз |

**⚠ РЕШЕНИЕ ВЛАДЕЛЬЦА (2026-08-07):** гейт `C6_v02_acceptance` и запрет на C3–C5 сняты для трека
v0.3 целенаправленно — натурная приёмка на Win7-ПК откладывается, вместо нее владелец заказал
GUI+exe предрелиз напрямую. Прежняя запись «exe не нужен» (2026-07-28 21:50) для этого релиза
отменена. Физика/`core` не трогается — GUI дергает тот же `api.Attenuator`, что и CLI. Ведёт ORCH
с прямой санкции владельца (роль C2 отдельно не запущена), сборка — в изолированном worktree.

**Исправление записи:** ранее в STATE/BOARD/HANDOFFS значилось «приложение обслуживает
проданный прибор уже сейчас» — это НЕВЕРНО. Прибор #02721 продан, но программа владельцем
не проверялась, никому не отправлялась и добро на релиз не выдавалось. Утверждение снято.

## Задачи

| ID | Задача | Статус | Примечание |
|---|---|---|---|
| C0_init | Инициализация сессии C, STATE.md | done | 2026-07-28 |
| C1_audit | Аудит v0.2: сверка README ↔ код, открытые вопросы roadmap | done | 2026-07-28, см. «Что подтверждено запуском» |
| C2_a4_crosscheck | Сверка `core/` с двух-WGP моделью A (A4) | open | **НЕ blocked** — см. «Отношения с A4» |
| C3_web | Веб-версия UI | todo | v1+, архитектура — `docs/attenuator_app/04_ARCHITECTURE.md` |
| C4_motor | Драйверы ротаторов / `MotionCommand` → hardware | todo | v1+, каркас есть в `api.py` |
| C5_peak_field | Третья метрика `peak_field` (пик поля, не энергия) | todo | сознательно отложено, `06_ROADMAP…` §M4–M5 |
| C6_v02_acceptance | Ручная приёмка v0.2 владельцем: прогон по чек-листу, сбор обратной связи, флаг одобрения | **open, отложена** | 2026-07-28; чек-лист `docs/attenuator_app/07_ACCEPTANCE_CHECKLIST_v0.2.txt` распечатан; прогон будет на Win7-ПК у спектрометра — собран офлайн-комплект `../win7_attenuator_kit` (Python 3.8.10 x64/x86 + numpy 1.21.6 + app + данные + README_WIN7); код проверен на совместимость с Py3.8 (16 файлов, AST 3.8 — 0 ошибок); дымовой тест из раскладки комплекта: 13/13, RMSE 1.23 воспроизведён |
| C7_gui_exe_prerelease | GUI (tkinter, обёртка над `api.Attenuator`) + сборка автономного `.exe` (PyInstaller) | **done, смержено в main** | заказано владельцем напрямую 2026-08-07, минуя C6; проверено ORCH (независимый повтор selftest 13/13, validate 69/69 — совпало бит-в-бит) и смержено (`e2389e6`); `.exe` не в git (build-артефакт), собирается локально `pyinstaller attenuator_app/gui.spec` |
| C8_measured_curve_prototype | Лабораторный прототип: угловая/спектральная кривая по измерениям `s2`, ручная подстройка модели | **done, находки переданы A** | 2026-08-17/19, санкция владельца, вне трека v0.2/v0.3; см. «Последнее обновление» и `tools/FINDINGS_measured_curve_2026-08-19.md` |
| C9_app_single_scenario | Минимальное рабочее приложение аттенюатора на основе модели C8, один сценарий: THz-TDS спектрометр | **done, код готов к ревью** | заказано владельцем 2026-08-19; интерфейс сценария уточнён владельцем перед кодом (см. «Последнее обновление») |

## Отношения с A4 — ВАЖНО, поправка к записи от 2026-07-28

В `ACTIVITY.md` Сессия A планирует `A4_attenuation_calc` как «API для C» и делает вывод:
«до готовности A4 запуск C преждевременен». Первая редакция этого STATE записала
`C2_a4_integration` как `blocked`.

**Фактически C не заблокирована и никогда не была.** У `attenuator_app/core/` собственная
реализация: `blanco.py` (векторизованный Бланко + Друде, LRU-кэш), `forward.py` (схемы S0–S3),
`inverse.py` (обратная задача). Она валидирована на 69 точках реальных измерений; прибор
#02721 продан, но программа НЕ выпущена — ждёт ручной приёмки владельцем (release gate выше).

Это значит, что в репозитории **две независимые реализации** расчёта затухания. Предлагаемое
разделение (решает ORCH/владелец, запрос — в `HANDOFFS.md`):

| | Сессия A (`research/two_wgp/`) | Сессия C (`attenuator_app/core/`) |
|---|---|---|
| Назначение | физический эталон, проверка гипотез | продуктовый расчёт для оператора |
| Модель | двух-WGP Джонс, per-element геометрия | схемы S0–S3, паспортная формула |
| Что важно | точность, идентифицируемость | воспроизводимость, границы применимости, интервал |
| Приоритет | истина | не соврать оператору |

Тогда C2 — не «интеграция», а **сверка**: прогнать оба калькулятора на одной сетке и
задокументировать расхождение. Расхождение больше паспортной RMSE 1.39 дБ — сигнал, что
в одной из реализаций ошибка. Это приёмочная проверка для обеих сессий, а не зависимость.

## Что подтверждено запуском (C1)

```
selftest       13/13   физика: идеальный предел cos^4, сохранение энергии,
                       теорема выравнивания S1 == S0 (-0.0000 дБ),
                       inverse(forward(θ)) == θ, 95 % интервал содержит значение
validate       69/69 = 100 %   покрытие 95 % интервала на 6 сеансах одного прибора
                       смещение +0.35 дБ, RMSE 1.23 дБ (паспортная 1.39 дБ)
                       запас интервала ×1.67
```

Побочный физический результат, ценный вне зоны C: разница между сеансами, где вращался
ПЕРВЫЙ поляризатор (series1-3), и где ВТОРОЙ (356att), составила **+0.15 дБ**. Закон Малюса
на этом железе выполняется — затухание задаётся относительным углом. Продублировано в
`research/logs/RESEARCH_LOG.md` с тегом `[C]`.

## Два ключевых решения, которые нельзя потерять

**1. Относительный режим — по умолчанию, и это физика, а не удобство.**
При SET ZERO общий множитель потерь `ℓ(ν)` и вносимое затухание `|t⊥|⁴` сокращаются
тождественно, остаётся только `t∥_eff/t⊥`. Проверено численно: `|dA| = 1.8e-15` дБ при
изменении `loss`/`gamma`. Следствие — **хуже всех определённый параметр `gamma` из
относительного режима выпадает**, поэтому экстраполяция за полосу калибровки в нём
на порядок надёжнее, чем в абсолютном.

**2. Неопределённость берётся по разбросу МЕЖДУ СЕАНСАМИ, а не по ковариации одного фита.**
По всем трём подгоняемым параметрам разброс между сеансами победил паспортный 95 % CI —
он в 3–27 раз шире. Паспортный интервал получен на отфильтрованном по дрейфу ансамбле и
оптимистичен для повседневной работы, где прибор переставляют и заново юстируют.
Валидация подтвердила выбор: 69/69 при запасе ×1.67.

## Допущения, требующие подтверждения владельцем

1. Граница апертур 45 мм для выбора деления шкалы (2°/1°) — взята по технологическому
   делению Tydex (плёнки D25-45 и D50-100). Для ATT-11-16-CA85 (85 мм) роли не играет:
   деление 1° подтверждено владельцем.
2. `gamma = 1.58` заморожен (паспортное значение), диапазон 1.0…2.5 для огибающей.
   Свободный фит даёт 4.18 — для экстраполяции вверх непригодно, параметр вырожден с утечкой.
3. Штраф за экстраполяцию 1 дБ на октаву за полосой — величина эмпирическая.
4. Плёночные поляризаторы Tydex учитываются только в схеме S3 (`use_film`); в паспортной S0
   не участвуют.
5. `D_eff = 5.67` мкм трактуется как ширина эквивалентной плоской полоски (модель Бланко
   отображает круглую проволоку D=11 мкм на полоску ≈D/2), НЕ как геометрический диаметр.
   Связано с открытой «загадкой D_eff» (`research/SYNTHESIS.md`) — C её не решает и не должна.
6. Схема S0 (паспортная формула) принята как способ, которым покупатель реально использует
   прибор. Не подтверждено.

## Открытые вопросы владельцу

1. Шаг/люфт ротаторов — до M2 обратная задача не может быть «честной» (`02_SCENARIOS.md` §7).
2. Тип детектора по умолчанию для операторского сценария (сейчас — когерентный, Scenario A).
3. Разграничение A4 ↔ `attenuator_app/core` (см. выше, запрос в `HANDOFFS.md`).

## Зависимости (read-only для C)

- `unified_optimizer/model_blanco` — read-only (гардрейл §6); правки → HANDOFFS к B
- `research/experiments/fit_lib.py` — используется только `tools/make_passport.py` при
  КАЛИБРОВКЕ, не в рантайме приложения; правки → HANDOFFS к B
- `research/two_wgp/**` — зона A, read
- `data_pool/**` — read-only. **Внимание:** `tools/validate.py` зависит от РАСПОЛОЖЕНИЯ файлов,
  которое меняет A/ORCH. Реорганизация в `data_pool/two_wgp_attenuator/` (запись A от 2026-07-28)
  сломала валидацию: нерекурсивный `glob` молча находил 0 файлов и печатал «нет данных» вместо
  падения. Исправлено на `rglob` (2026-07-28 15:31), числа воспроизвелись бит-в-бит.
  Урок: при перезапуске сессии C **сначала прогнать `validate` и посмотреть на КОД ВОЗВРАТА**,
  и только потом доверять записанным числам — тихий ноль опаснее ошибки.
  **Повторилось 2026-08-04** при переименовании `356att`/`series*` → `att-11-16-*` (коммит
  `eda293e`): дефект пролежал 3 дня. Исправлено 2026-08-07 вместе с защитой от тихого нуля.

Рантайм приложения зависит **только от numpy**. `matplotlib` опционален (нет — остаётся
ASCII-псевдографика). Это сознательное решение: прибор уже продан, приложение должно
запускаться у покупателя без научного стека.

## Долги вне зоны C

- `research/literature/INDEX.md` не содержит `JOSA.67.000979.pdf` (Costley 1977) — 20 PDF в
  `litrev/pdfs`, проиндексировано 19. Зона ORCH/D, C не правит.

## Режим работы (с 2026-07-28 18:40)
Сессия C работает в worktree `../TUO-C` на ветке `session/C` (CHARTER §2); мерж в `main` — за ORCH.
Координация (`coordination/BOARD.md`, `sessions/C.md`, `ACTIVITY.md`) остаётся на общем `main`.
Этот STATE.md ведётся в worktree; на `main` он попадает мержем.

## Последнее обновление
2026-08-22 — **CW-вариант приложения: `attenuator_app/cw_curve.py`** (санкция владельца
2026-08-22). Неинтерактивная CLI-команда (argparse): одна команда → PNG с угловой
зависимостью ослабления A(θ) для монохроматического CW-источника. База — КЛИЕНТСКОЕ ЯДРО
(`core/forward`, `core/passport`, `core/plots`, `api`), только сборка вызовов, НОВОЙ ФИЗИКИ
НЕТ; интерактивный `cli.py` не тронут. Два независимых тумблера: детектор
(`coherent`=поляризационно-чувствительный, есть ось анализатора / `power`=нечувствительный) и
источник (`linear` DOP=1 / `depolarized` DOP=0) — источник реализован ПЕРЕИСПОЛЬЗОВАНИЕМ
существующего DOP-механизма (`Setup.dop` + unpol-ветка `intensity`), механизма хватило. Пресеты
анализатора H=0°/V=90°/произвольно; свип −90…+90°, шаг 1° (параметризуемо); пасспорт по
умолчанию `ATT-11-16-CA85_02721.json` (как у `cli.py`). Конвенция угла двух ротаторов —
относительный θ=θ₂−θ₁, схема S0 ≡ двух-WGP S1 по теореме выравнивания (взято из паспорта+кода,
подтверждение — вопрос C-7). Новый self-check `--selftest` 5/5 (power=Malus cos², coherent-H=cos⁴,
coherent-V=гашение в кресте, depol+power=плоско, CW-точка==broadband на той же частоте);
`attenuator_app.selftest` без регресса 13/13. 6 демо-PNG — `research/results/cw_attenuator/`.
Документация: `docs/attenuator_app/09_CW_CURVE.md` (запуск, тумблеры, конвенция) +
`10_CALIBRATION_DESIGN.md` (DESIGN-ONLY алгоритм калибровки и ДД, без кода). Отчёт —
`docs/artifacts/C_cw_attenuator_curve.md`. Вопросы владельцу: C-7…C-10 (все non-blocking).
Не сделано: ревью/одобрение владельца, push.

2026-08-18 (третий круг: SET ZERO доведена до конца) — владелец: «идея с set zero не
до конца реализована», нужен режим отображения с выходом в усиление. Что было не так:
в опоре `absolute` рабочая точка вообще не влияла на кривую (жила только числом в
тексте), а в `relative` она вытеснила прежнюю нормировку на максимум — то есть
поставить рабочую точку и одновременно смотреть кривую «в долях от максимума» было
нельзя. Сделано:

1. **Опор стало три вместо двух** (`REFS`, было `MODES`): относительных нормировок на
   самом деле две, их и развели.
   * `P_0` — абсолютная, доля мощности на входе аттенюатора. От SET ZERO не зависит.
   * `P_max` — к максимуму (совмещённое положение), **по умолчанию**; это прежний
     `relative` без изменений. От SET ZERO не зависит.
   * `P_zero` — к рабочей точке SET ZERO; она и даёт выход в усиление (>100 %, >0 дБ).
2. **Добавочная величина не зависит от опоры** — `relative_to_zero_db()`:
   `att(theta) − att(zero)`, общий знаменатель сокращается. Поэтому «на сколько дБ мы
   отошли от рабочей точки» теперь показывается ВО ВСЕХ трёх опорах одинаково, а не
   только там, где нормировка на неё. Проверено численно: +4.75 дБ в `P_zero` и в `P_0`.
3. **Окно результатов показывает работу со сдвигом**, а не одно число: таблица из двух
   строк (SET ZERO / запрос) со значениями во всех трёх опорах сразу, плюс строка
   добавочной величины в дБ и «во сколько раз» по мощности и по полю. Сдвинутая точка
   помечена звёздочкой. `describe_point()` в `service_calc` считает всё это разом.
4. **Переключатель шкалы** (владелец: «возможно нужен переключатель режима отображения
   с выходом в усиление»): «затухание» — жёсткие 0–100 % и 0…−45 дБ; «с усилением» —
   верх раздвигается по кривой. Автоскейла нет ни в одном; в жёстком режиме, если
   кривая не помещается, в заголовке появляется явная подсказка переключиться (раньше
   верх молча раздвигался сам — неявное поведение убрано).
5. **Разметка рабочей точки на графике**: своя вертикаль и квадратный маркер (фиолетовый,
   отличается от зелёных точек запроса), горизонтальный пунктир на её уровне, область
   выше уровня залита как зона усиления, а между уровнем рабочей точки и уровнем
   запроса — двусторонняя стрелка с подписью (×2.99 на панели процентов, +4.75 дБ на
   панели децибел). Добавочная величина стала видна геометрически.
6. **Кнопка «сюда текущий угол»** у SET ZERO — типовой ход из формулировки владельца:
   выставили начальное затухание, объявили его нулём, дальше добавляют или убавляют.
7. Исправлено: `ANGLE_ATOL = 1e-4` вместо `1e-6` при сравнении углов. Поля ввода
   округляли значение, и после кнопки «= SET OFFSET» точка считалась сдвинутой
   (offset −0.56174, в поле −0.562).

Проверено: CLI по трём опорам со сдвинутой точкой (добавочная величина совпадает),
headless-смоук GUI 4 метрики × 3 опоры × 2 шкалы + сценарий сдвига + краевые, картинки
графиков просмотрены, `attenuator_app.selftest` 13/13.
Не проверено: живое окно GUI руками. Не сделано: ревью владельца, push.

2026-08-18 (второй круг обратной связи владельца по C9) — семь правок, все в
`tools/service_calc.py` + `tools/service_gui.py`:
1. **Знак затухания.** Затухание — ОТРИЦАТЕЛЬНЫЕ децибелы: `attenuation_db = 10*log10(T)`
   (было `-10*log10(T)`). T=1 → 0 дБ, T=0.5 → −3.01 дБ. Положительное значение теперь
   означает усиление относительно рабочей точки — физически осмысленно, см. п.2.
   Обратная задача принимает отрицательный вход; на положительный даётся подсказка
   («возможно, нужно −12.00 дБ»), а не молчаливая ошибка.
2. **SET OFFSET и SET ZERO разделены** — это были две разные вещи под одним именем:
   * **SET OFFSET** (`offset_deg`, он же theta0) — ФИЗИЧЕСКИЙ офсет совмещения осей
     WGP1/WGP2, входит в формулу Джонса как `d = theta − offset`. Подгоночный параметр
     модели, зашит в калибровку. Именно под него пишется алгоритм автокалибровки
     в следующей версии.
   * **SET ZERO** (`zero_deg`) — РАБОЧАЯ ТОЧКА ОТСЧЁТА на кривой, в физику не входит;
     от неё считается добавочное затухание/усиление. По умолчанию = SET OFFSET.
   В режиме `relative` нормировка теперь на T(SET ZERO), а не на T(theta0) — при
   zero==offset это в точности прежнее поведение, при сдвинутом — даёт усиление (>0 дБ).
   В GUI две отдельные рамки; при смене OFFSET привязанный ZERO едет вместе с ним.
3. **Метрика полосы — выпадающий список, 4 варианта** (`Metric`, dataclass): полная
   мощность (полей ввода нет), одна частота (одно поле), полоса центр+ширина, полоса
   мин+макс (по два поля). Интерфейс перестраивается под выбор. Полосы усредняются тем
   же весом |E_ref(nu)|^2, но только по точкам сетки внутри полосы; пустая полоса и
   выход за откалиброванный диапазон дают внятные сообщения.
4. **Жёсткие шкалы графика, автоскейл выключен**: пропускание 0–100 %, затухание
   0…−45 дБ. Единственное исключение — верх раздвигается, если сдвинутый SET ZERO
   уводит кривую в усиление (иначе данные были бы просто за кадром).
5. **Серая блёклая кривая полной мощности** рисуется фоном под яркой кривой выбранной
   метрики — в обоих режимах, чтобы видеть, насколько частотная выборка отличается от
   широкополосной.
6. **Разметка точки запроса**: вертикальный отрезок вниз до оси X и горизонтальный
   влево до оси Y — оба ЗАКАНЧИВАЮТСЯ на графике, не уходят в бесконечность. Подписи
   (угол / T в % / затухание в дБ) посередине отрезков, автоматически переставляются
   внутрь у правой и верхней границ; значение вне шкалы помечается. Для двух
   симметричных решений горизонталь рисуется один раз — иначе подписи накладываются.
7. **Мощность на входе аттенюатора названа `P_0`** (было «P_вход»). Знаменатель T:
   `P_0` в режиме absolute, `P_zero` в relative — подписан на оси и в выводе.

Проверено: CLI (прямая/обратная × relative/absolute × все 4 метрики × усиление ×
подсказка про знак × пустая полоса, exit-коды), headless-смоук GUI по всем метрикам и
режимам, картинки графиков просмотрены, `attenuator_app.selftest` 13/13.
Не проверено: живое окно GUI руками. Не сделано: ревью владельца, push.

2026-08-19 (вечер, правка по обратной связи владельца) — четыре доработки C9 после
первого прохода (**частично заменены правкой 2026-08-18 выше**: знак дБ и смысл
theta0/SET ZERO пересмотрены):
1. **theta0 зашит в калибровку как значение по умолчанию** (`Calibration.theta0_calibration_deg`,
   из подгонки по серии s2) — приложение готово к расчётам сразу при старте, SET ZERO
   больше не блокирует; поле и кнопка остаются для ручной переустановки (прибор
   переставили в роторе, или нужна произвольная рабочая точка отсчёта — владелец
   подтвердил, что эта возможность нужна) + кнопка «из калибровки» для отката.
2. **Явно указана величина затухания**: дБ — ПО МОЩНОСТИ (`-10*log10(P/P_ref)`, не по
   амплитуде поля); `power_field_ratios()` возвращает обе величины
   (P/P_ref=10^(-дБ/10), E/E_ref=10^(-дБ/20)), печатаются в CLI и GUI рядом с дБ.
3. **Вернулся переключатель relative/absolute** (был в `measured_curve.blanco_angular_curve`,
   потерялся при первом упрощении C9): `relative` — нормировка на T(theta0)=1 (как было);
   `absolute` — доля мощности, падающей НА аттенюатор, включает собственную вносимую
   потерю пары WGP даже в совмещённом положении (`floor_min_db` в обратной задаче
   тогда >0, а не 0). theta0 нужен в обоих режимах одинаково — это координата
   совмещения осей WGP1/WGP2 в формуле Джонса, а не только точка нормировки.
4. **Графики угловой кривой** (matplotlib, встроены в GUI, две панели: T в % и
   затухание в дБ, для текущих theta0/режима/частоты, с вертикальной линией theta0
   и маркером последнего запроса) — визуально проверены (relative: ровно 100%/0 дБ в
   theta0; absolute: ~89%/0.5 дБ на 0.8 ТГц — собственная потеря видна).

Headless-смоук-тест повторён на обеих правках (режимы, ручной SET ZERO на
произвольную точку, восстановление из калибровки, частота вне полосы, недостижимое
значение) — без исключений; `selftest` 13/13 не задет.

2026-08-19 (вечер) — **C9_app_single_scenario реализована: минимальное приложение
обслуживания аттенюатора в THz-TDS спектрометре.** Интерфейс сценария сначала уточнён
владельцем (4 вопроса): направление — **оба** (дБ→угол и угол→дБ в одном окне); метрика —
**оба** (интегральная широкополосная по умолчанию + опционально на конкретной частоте);
офсет ротатора theta0 — **SET ZERO вручную** в приложении (оператор физически выставляет
совмещённое положение, вводит показание шкалы, жмёт кнопку; **владелец отметил: настоящий
алгоритм авто-калибровки офсета нужен в следующей версии**, здесь — заглушка через ручной
ввод); неопределённость — **только точка, без интервала** (сознательно упрощено для v1).

Реализация (`attenuator_app/tools/`, НЕ трогает `core/`/`api.py`/v0.2-v0.3 клиент):
- `calibration/build_service_calibration.py` — офлайн-шаг, гоняет `measured_curve.analyze_road()`
  по дорожной серии `ATT-11-16-CA85_02721_s2` и сохраняет P/D/потери/gamma + референсный
  спектр мощности (вес для интегрального усреднения, теорема Парсеваля) в
  `calibration/att_11_16_ca85_02721.json`. **Важно для будущих запусков:** правильный источник
  калибровки — дорожная папка `data_pool/two_wgp_attenuator/ATT-11-16-CA85_02721_s2/`
  (`analyze_road`), НЕ плоская конвенция `att-11-16-s2_*.txt` в корне `data_pool` — та даёт
  вырожденный фит на границе поиска (gamma→0.2, P→3, D→0.5, `at_bound=True`); дорожная даёт
  P=32.95, D=9.92, потери=0.261 дБ/ТГц^0.569, `at_bound=False`, что и совпадает с числами
  `FINDINGS_measured_curve_2026-08-19.md`. Билд-скрипт отказывается писать JSON при
  `at_bound=True`.
- `service_calc.py` — библиотека (прямая/обратная задача, формула `T(theta)/T(theta0)` из
  находки п.1 продублирована в минимальном виде, чтобы не тащить импорт `track_viewer` в
  рантайм) + CLI (`--zero`, `--to-db`/`--from-angle`, `--freq`). Рантайм читает только
  зашитый JSON, `data_pool/` не трогает (принцип v0.2: приложение не зависит от научного
  стека/лабораторных данных). Обратная задача — интерполяция по монотонной (после
  `np.maximum.accumulate`) сетке 901 точки на `delta∈[0,90]`, а не root-finder — минимум
  зависимостей; возвращает ДВА симметричных решения (`theta0±delta`, т.к. cos²/sin² чётные).
  Частота вне полосы калибровки (0.3-1.5 ТГц) — явное предупреждение в выводе, не тихая
  экстраполяция.
- `service_gui.py` — tkinter, поверх `service_calc` (обвязка физику не пересчитывает).
  Headless-смоук-тест (`ServiceGUI()` без `mainloop`, SET ZERO + оба направления + частота
  вне полосы + невалидный ввод) прошёл без исключений.

Проверено: `attenuator_app.selftest` 13/13 не задет (только читал `core/blanco.dressed_t`,
ничего в `core/`/`api.py` не менял). Не сделано: натурная проверка живого GUI-окна
(headless-тестами не покрыта), ревью/одобрение владельца.

2026-08-19 — **новый лабораторный прототип `attenuator_app/tools/measured_curve.py` +
`measured_gui.py`** (санкция владельца 2026-08-17, задача сугубо прикладная: угловая и
спектральная кривая аттенюатора прямо по измерениям `data_pool/two_wgp_attenuator/
ATT-11-16-CA85_02721_s2/`, без сверки с зоной A). НЕ часть клиентского `attenuator_app.gui`/
`cli` (v0.2/v0.3 ниже) — отдельный инструмент, `api.Attenuator`/`core/` не тронуты.
Реализовано: гармонический фит Малюса (переиспользован `track_viewer.core.fit_malus`),
полная Джонс-матричная модель схемы S1 с двумя ИДЕНТИЧНЫМИ WGP (не одиночная формула — важное
отличие, см. находки ниже), ручная подстройка P/D/потери/gamma/theta0 в GUI, переключатель
относительный/абсолютный режим. **4 физические находки переданы зоне A**
(`coordination/HANDOFFS.md`, запись `[2026-08-19] ОТ c-app К A`, статус `open`) — подробности
и формулы в `attenuator_app/tools/FINDINGS_measured_curve_2026-08-19.md`: (1) канал `I_perp`
для двух идентичных WGP — `|t_perp|⁴`, не `|t_perp|²`; (2) подогнанный `P` устойчиво сходится
к 33-41 мкм при паспортных 16 (возможно родственно `D_eff↔η` вырожденности из
`SYNTHESIS.md`); (3) 5 линий поглощения атмосферного водяного пара в `I_perp`; (4)
расхождение пика ~5-6% в абсолютном режиме — не невязка фита, а нарушение теоремы Парсеваля
плоским усреднением по узкой полосе, исправлено весом `|Ê_ref(ν)|²`.

**Следующий шаг (владелец, 2026-08-19): на основе этой модели написать МИНИМАЛЬНОЕ рабочее
приложение аттенюатора для одного сценария — обслуживание в THz-TDS спектрометре.** Не
начинать без прочтения находок выше и `FINDINGS_measured_curve_2026-08-19.md` целиком.

2026-08-07 (вечер) — **C7_gui_exe_prerelease: GUI + автономный .exe реализованы, код готов к
ревью.** Ветка `session/C-gui-exe`, worktree-изоляция, НЕ смержено в `main`, не запушено.

Добавлено: `attenuator_app/gui.py` (desktop GUI на tkinter поверх `api.Attenuator` — паспорт,
сводка устройства, схема/детектор/источник, SET ZERO/AUTO-ZERO/AUTO-CROSS, relative/absolute,
прямая и обратная задачи, спектр и угловая кривая с графиком через `core/plots.py` или ручным
`tkinter.Canvas`-фолбэком без matplotlib, проверки F1..F8); `attenuator_app/gui_entry.py` и
`attenuator_app/gui.spec` (PyInstaller, onefile, `console=False`); дополнения в `README.md`
(запуск GUI, сборка exe). `core/` не тронут — GUI вызывает только `api.Attenuator`, как и CLI.

**Проверка PyInstaller + Python 3.14 + tkinter (главный риск задачи) — блокирующих проблем
НЕ обнаружено.** В `.venv` репозитория (Python 3.14.0) поставлен `pyinstaller==6.21.0`; сборка
`attenuator_app/gui.spec` проходит без ошибок (~40 МБ onefile-exe, matplotlib 3.11.1 внутри —
он был в venv, поэтому упакован, хотя формально опционален). Запуск `THz-Attenuator.exe`
проверен: headless-старт не падает, `Get-Process` показывает живой процесс с правильным
заголовком окна `THz Attenuator Calculator`, `Responding=True`, без диалогов ошибок.

Проверка бит-в-бит до/после (в этом же worktree, тем же `.venv\Scripts\python.exe`):
`selftest` 13/13, `validate` 69/69 = 100 %, смещение +0.35 дБ, RMSE 1.23 дБ, запас x1.67 —
совпадает с базовой линией из `HANDOFF_C_20260807.md` §2. Дополнительно: headless-смоук-тест
самого GUI (`AttenuatorGUI()` без `mainloop`, все вкладки/кнопки по очереди, включая обе ветки
графика — с matplotlib и с форсированным Canvas-фолбэком) прошёл без исключений.

Оставлено на ревью/владельцу: смерж ветки `session/C-gui-exe` в `main` (не пуш — по заданию);
GUI не пишет папку сеанса `runs/...` (это осталось только у CLI, сознательно не дублировалось);
натурная проверка живого окна (открыть/покликать) — headless-тестами не покрыта.

2026-08-07 — **закрытие чата C1, передача роли: `attenuator_app/HANDOFF_C_20260807.md`** (читать
первым). Переименование данных Сессией A (`eda293e`, 04.08) снова сломало `tools/validate.py`
**молча** — были переименованы ключи `ROTATES`, но не умолчание `--datasets` и не префиксы групп,
поэтому валидатор находил 0 файлов, печатал «no data for validation» и выходил с кодом 0.
Исправлено; **числа воспроизвелись бит-в-бит** (selftest 13/13; validate 69/69 = 100 %,
смещение +0.35 дБ, RMSE 1.23 дБ, разница групп +0.15 дБ, запас ×1.67) ⇒ переименование физику не
затронуло, **запрос A «C, прогони свой валидатор» закрыт**. Поставлена защита от тихого нуля:
«файлов не найдено» отделено от «мало углов», `main()` возвращает код 1. Приведены к новым именам
`README.md` и docstring валидатора; в чек-лист приёмки добавлено предупреждение, что Win7-комплект
и распечатанная ред.2 **намеренно** остались на старых именах и синхронизируются только после
приёмки. Задача C6 не двигалась — ждёт владельца.

2026-07-28 21:50 — **интерфейс переведён на английский** (решение владельца: прибор уехал
в Китай, Win7-ПК у спектрометра не работает с кириллицей; exe не нужен — комплект на
Python 3.8). Переведены все пользовательские строки (CLI, selftest, validate, F0–F8,
подписи PNG/ASCII, `calibration_note` паспорта); комментарии в коде остались русскими.
Физика сверена бит-в-бит до/после: 13/13, 69/69, смещение +0.35, RMSE 1.23, запас ×1.67,
пол 40.59 дБ @ 89.50°. Дымовой прогон CLI: 25 команд без ошибок. Чек-лист ред.2 (цитаты
ответов — EN) распечатан; Win7-комплект пересобран (zip 78 МБ).

2026-07-28 19:05 — по указанию владельца введён release gate (флаги одобрения по версиям);
исправлено ложное «приложение обслуживает прибор» — релизов не было, v0.2 ждёт ручной
приёмки; заведена C6_v02_acceptance; отревьюирован операторский гайд другой сессии
(итог — в ответе владельцу и в ACTIVITY).

2026-07-28 18:40 — перезапуск в Cursor: приёмка воспроизведена бит-в-бит (selftest 13/13,
validate 69/69 = 100 %, смещение +0.35 дБ, RMSE 1.23 дБ, запас ×1.67); зона C закоммичена
на main per-zone (`3e4cd24`, снят C-флаг ORCH от 17:16); создан worktree `../TUO-C`
(ветка `session/C`). Открытые задачи без изменений: C2 open (ждёт A4), C3–C5 v1+.

2026-07-28 15:31 — C1 закрыт; C2 переведён из `blocked` в `open` с переформулировкой
(сверка вместо интеграции); зафиксированы допущения и два ключевых решения.
