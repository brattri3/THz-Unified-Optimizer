# Роль A — прямая модель, идентифицируемость, гипотезы

**Имя сессии:** `a-model` · **префикс коммита:** `[A]`

## Пишу
`research/two_wgp/**` · `research/results/two_wgp/**` · `research/hypotheses/**` ·
скрипты-гипотезы `research/experiments/tN_*.py`, `model_m5.py`, `micrograph_*`, `make_*`,
`geometry.py`

## Не трогаю
Численное ядро — `model_core.py`, `fit_lib.py`, `verify_*` (B, через `HANDOFFS.md`) ·
`data_pool/**`, `unified_optimizer/**` — read-only

## Что открыто и что дальше

1. **Закрыто 16.08** (→ `ACTIVITY.md` 24.08 [ORCH]): C-вердикт; A22 Малюс в поле; A24 ранг-1
   (только `att-11-16-s1`).
2. **Геометрия A19 (В-20), фиты — ЗАПРЕЩЕНО (В-21).** `GEOMETRY`: `P`=24.44/24.73/31.71/37.18,
   `D`=10.28/9.72/10.47/20.30; `specac=14.0` уходит. Перезапуск `purewave`/`test_grid_40_20`+H5 —
   после L (В-19).
3. **specac-2 закрыт (В-24)**, отдельный датасет (не `rep2`). ➜ **A-4**: `META.txt` пуст (нет
   геометрии/`dark`) — как `purewave`.
4. **A23 закрыт 16.08.** ➜ **A-6**: `D_eff`(`eps_cross`) `test_grid_33_11` 7.3176±0.0332 без vs
   **4.3377±1.2283** с, ΔAIC=−23.7. ➜ **A-5**: `specac` χ²/dof=0.893 vs `specac-2` **24.611** —
   непригоден для `D_eff`.
5. **A25 закрыт 17.08**: `η` — свойство элемента, широкополосная `η` НЕ инвариантна
   (`SYNTHESIS.md`, `η₀=η(1ТГц)`). ➜ **A-7**: покрывает ли В-21 `purewave`/`test_grid_40_20`.
6. Очередь: `A15` (профиль по `P`), `HN13` на `test_grid_33_11`, нерегулярность `att-11-16`
   (прибор уехал, три микрофото)

## Моё
`research/two_wgp/state.json` · `HANDOFF_A_20260807.md` ·
`research/results/a18_microscopy/SUMMARY.md` · `research/SYNTHESIS.md` — **читать до гипотез**

## Помнить
6 августа три числа опубликованы и в тот же день отозваны. Мультистарт обязателен: угловые
параметры мультимодальны, холодный старт завышает значимость.
