# NICKS — реестр ников чатов (см. CHARTER §12)

Одну «роль» (`A`/`B`/`C`/`D`/`L`/`ORCH`) во времени может обслуживать НЕСКОЛЬКО физических чатов
(новый запуск, `--resume`, восстановление после краха). Ник — короткая замена длинного chat id
в повседневном обиходе (`sessions/<ID>.md`, `ACTIVITY.md`, `HANDOFFS.md`). Формат ника: `<Роль><n>`,
`n` — следующий свободный номер для этой роли. Регистрируй здесь ПЕРЕД тем, как занять лок
(CHARTER §12) под новым ником.

| Ник | Роль | Chat ID (полный, если известен) | Зарегистрирован | Статус | Примечание |
|---|---|---|---|---|---|
| **O1** | ORCH | `e41a7ae7-6ea2-46a8-99e3-6df6d5532a9e` | 2026-07-31 | active | текущий чат ORCH; первая регистрация по протоколу §12 |
| **A1** | A | `b91ac21e-b1f1-4ae6-b00b-1334d3db8e6b` | 2026-07-31 (постфактум) | active | резюмированный чат A; id известен из команды владельца `claude --resume ...` |
| **B1** | B | не зафиксирован | 2026-07-31 (постфактум) | idle | ранний чат(ы) B, работал(и) до введения протокола §12; id не сохранялся |
| **C1** | C | не зафиксирован | 2026-07-31 (постфактум) | idle | работает в worktree `../TUO-C`, ветка `session/C`; id не сохранялся |
| **D1** | D | не зафиксирован | 2026-07-31 (постфактум) | active | запущена владельцем 2026-07-29 13:19; id не зафиксирован при старте |
| **L1** | L | не зафиксирован | 2026-07-31 (постфактум) | active (пауза до отложенного запуска) | ⚠ если отложенный запуск (`CronCreate` 509d0d7d) поднимет НОВЫЙ процесс чата — это будет **новый ник `L2`**, а не продолжение L1 под тем же ником; см. отдельное разъяснение владельцу про механизм cron |

## Правила ведения

1. Новый физический чат под ролью `<ID>` перед тем, как занять лок в `sessions/<ID>.md`:
   - смотрит последнюю строку с этой ролью здесь → берёт следующий номер (`<ID><n+1>`);
   - если знает свой chat id (например, дан во входной команде `claude --resume <uuid>`) —
     вписывает; если нет — пишет «не зафиксирован» (не блокирует регистрацию).
   - добавляет строку сюда со статусом `active`.
2. Когда чат останавливается штатно (переводит лок в `FREE`) — меняет свой статус здесь на
   `retired`, НЕ удаляя строку (история ников — не переписывается, только append/пометка).
3. Ники одной роли **никогда не переиспользуются** — новый чат всегда берёт следующий номер,
   даже если предыдущий ник давно `retired`. Это отличает «третий чат под ролью A» от
   «второй чат под ролью A», даже если оба уже остановлены.
4. Эта таблица — процедурный реестр, не техническая база данных: ведение вручную, добросовестно,
   каждым чатом за себя. Чужие строки не редактировать (кроме ORCH при разборе конфликта).
