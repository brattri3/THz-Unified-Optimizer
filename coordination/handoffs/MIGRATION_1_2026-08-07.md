# Миграция 1 (2026-08-07, `migrate-1`) — аудит штатных механизмов Claude Code + вывод `NICKS.md` из эксплуатации

> Коммит `67bc62f` в репозитории `THz-Unified-Optimizer`, ветка `main`, не запушен.
> Этот файл — копия итогового отчёта в чате, сохранена по запросу владельца отдельно.

## 1. Таблица аудита (Шаг 1)

Установка: `claude --version` → `2.1.223 (Claude Code)`, native, канал обновлений `latest`.
Ниже — не по памяти о документации, а по фактической проверке: вызов, `--help`, бандл-файлы
самой установки (`~/.claude/cache/changelog.md`, бандл-плагины).

| # | Пункт | Статус | Как проверено |
|---|---|---|---|
| 1 | `claude -n <имя>`, `/rename`, `--resume <имя>` → `NICKS.md` | **ЕСТЬ** | `--help` (`-n, --name`) + changelog («Added named session support…») + **вызовом**: `claude agents --json` показал авто-имя `ps656-76`, но `--resume "ps656-76"` провалился — `is not a UUID and does not match any session title`. Работает только ЯВНО заданное через `-n`/`/rename` имя. |
| 2 | Список сессий: `--resume` без аргумента, фильтры пикера | ЕСТЬ, не вызывал (нужен TTY) | `--help` + changelog |
| 3 | `claude agents`, `claude agents --json` → живой статус `BOARD.md` | **ЕСТЬ, проверено вызовом** | реальный JSON-вывод (см. ниже) |
| 4 | `/context`, `/compact`, `/branch` → §7 | ЕСТЬ | `/compact` вживую видел в этой же серии заходов; `/context`/`/branch` — по бандл-changelog установки (включая переименование `/fork`→`/branch`) |
| 5 | `/loop`, `/goal` → §8 пп.1-2 | **ЕСТЬ ОБА** | `/loop` — в моём списке доступных навыков этого захода; `/goal` — changelog 2.1.139: «set a completion condition and Claude keeps working across turns until it's met» |
| 6 | Запланированные задачи (Desktop/облако) → Планировщик Windows, Фазы A/E | ЕСТЬ, НО ИНАЧЕ | облачные cron-«рутины» (`CronCreate`/`CronList`) — другой транспорт, не headless-вызов через локальный Планировщик Windows |
| 7 | Хуки, `SessionEnd` → «лок последним действием» | ЕСТЬ (только событие) | бандл-схема валидации хуков этой установки: `VALID_EVENTS=(... "SessionEnd" ...)` |
| 8 | `claude -p --output-format json` (usage/cost) → часть `kpi_stats.py` | **ЕСТЬ, проверено вызовом** | `total_cost_usd`, `usage.*`, `modelUsage` по модели (см. ниже) |
| 9 | `--worktree` / `git worktree` → изоляция файлов | ЕСТЬ | `--help` (`-w, --worktree [name]`) + штатный git |
| 10 | Agent Teams → обмен между сессиями, claiming | ЕСТЬ, ЗА ФЛАГОМ | changelog: много записей про teammates/leader; включается `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, по умолчанию выключено |

**Контрольная точка пройдена** (именованные сессии есть) → Шаги 2–3 выполнены.

### Вывод `claude agents --json`

```json
[
  {"pid": 11828, "cwd": "C:\\Users\\ps656", "kind": "interactive", "sessionId": "ff2a7d45-...", "name": "ps656-76", "status": "idle"},
  {"pid": 2220,  "cwd": "C:\\Users\\ps656", "kind": "interactive", "sessionId": "ec1f5c22-...", "name": "ps656-7e", "status": "busy"}
]
```

### Вывод `claude -p "2+2" --output-format json` (обрезано)

```json
{"total_cost_usd":0.091092,"usage":{"input_tokens":2,"output_tokens":3,"cache_creation_input_tokens":14118,"cache_read_input_tokens":21110},"modelUsage":{"claude-sonnet-5":{"costUSD":0.091092,"contextWindow":1000000}},"result":"4"}
```

## 2. Диффы `CHARTER.md` / `NICKS.md` / `BOARD.md` — дословно

```diff
diff --git a/coordination/BOARD.md b/coordination/BOARD.md
index 565ee5c..0b0d358 100644
--- a/coordination/BOARD.md
+++ b/coordination/BOARD.md
@@ -1,16 +1,18 @@
-# BOARD — живой статус сессий
+# BOARD — открытые находки, решения и хэндоффы по ролям
 
-Каждая сессия обновляет СВОЮ строку: при старте, смене задачи, «пульсе» (раз в несколько шагов) и
-остановке. Формат времени — ISO (локальное). Статус: `running` / `idle` / `blocked` / `stopped`.
+> **Кто сейчас активен и в каком статусе — смотреть `claude agents` / `claude agents --json`
+> (см. `coordination/MIGRATION.md`), не здесь.** Эта колонка регулярно отставала от факта (см.
+> историю проекта 2026-08-03…07) и с 2026-08-07 убрана. Ниже — то, чего `claude agents` не знает:
+> научные находки, принятые решения, что делать дальше.
 
-| Сессия | Статус | Текущая задача | План (кратко) | Обновлено (heartbeat) |
-|---|---|---|---|---|
-| **A** модели/идентифицируемость/гипотезы + софт затухания | stopped (`exit` 2026-07-31) | **A3_refit_series** (следующая; A0/A6/A4 — DONE, все хэндоффы к A закрыты) | **A0**: гармоническая линеаризация Малюса... | 2026-07-29 |
-| **B** ядро/рефактор | idle | model_core доведён (итер. 1–2), бит-в-бит | далее по запросу: two_wgp на ядро... | 2026-07-28 |
-| **C** app | idle | **C6_v02_acceptance**... | ждём прогон владельца на Win7-ПК... | 2026-07-28 21:55 |
-| **D** статья + образоват. слой | stopped (`exit` 2026-07-31) | **D3 — DRAFT v0.1 готов**... | Готово: `RELATED_WORK.md`... | 2026-07-29 14:05 |
-| **L** литература/обзор + метапоиск | stopped (`exit` 2026-07-31) | **L3_supply**... | **P1 ЗАКРЫТ**... | 2026-07-29 19:40 |
-| **ORCH** | **frozen** (2026-08-07...) | Координационная работа заморожена... | Открытые хэндоффы... | 2026-08-07 |
+| Роль | Открытые находки / решения / что дальше | Обновлено |
+|---|---|---|
+| **A** модели/идентифицируемость/гипотезы + софт затухания | Следующая задача — **A3_refit_series** (A0/A6/A4 — DONE)... Далее A3 → A5 + переподгонка M5 на полном диапазоне | 2026-07-29 |
+| **B** ядро/рефактор | model_core доведён (итер. 1–2), бит-в-бит. Далее по запросу: two_wgp на ядро... | 2026-07-28 |
+| **C** app | **C6_v02_acceptance**... Ждём прогон владельца на Win7-ПК... | 2026-07-28 21:55 |
+| **D** статья + образоват. слой | **D3 — DRAFT v0.1 готов**... номер гипотезы HN12→HN13 исправлен 2026-08-07... | 2026-08-07 |
+| **L** литература/обзор + метапоиск | **L3_supply**... **P1 ЗАКРЫТ**... | 2026-07-29 19:40 |
+| **ORCH** | **Заморожено** (2026-08-07, решение владельца)... миграция на штатные механизмы Claude Code... | 2026-08-07 |
 
 ## Легенда владения (полностью — OWNERSHIP.md)
 A→`research/two_wgp/`+`research/hypotheses/` · B→`research/experiments/model_core.py`+shared-code · C→`attenuator_app/` ·
@@ -18,5 +20,5 @@ D→`research/paper/` · L→`research/papers/` (вкл. свёрнутый `res
 
 ## Активные «замки» намерений
 См. `coordination/sessions/*.md`. Перед правкой общего файла — проверь их. Каждый файл содержит
-блок «🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, ник держателя, таймаут
-простоя 15 минут. Реестр ников чатов — `coordination/NICKS.md`.
+блок «🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, имя держателя, таймаут
+простоя 15 минут.
diff --git a/coordination/CHARTER.md b/coordination/CHARTER.md
index 645a710..b782722 100644
--- a/coordination/CHARTER.md
+++ b/coordination/CHARTER.md
@@ -12,9 +12,9 @@
 ## 1. Кто ты (идентичность)
 Твой ID (A/B/C/D/ORCH) задаётся В ЛАУНЧ-ПРОМТЕ сессии. Если не задан — СПРОСИ у владельца, не
 работай анонимно. Прочитав чартер, ты обязан:
-1. Зарегистрироваться в `coordination/NICKS.md` под коротким ником своей роли и занять лок
-   исполнения в `coordination/sessions/<ID>.md` (§12) — это защита от того, что другой физический
-   чат той же роли начнёт работать параллельно.
+1. Назвать сессию при запуске (`claude -n <имя>` или `/rename`) и занять лок исполнения в
+   `coordination/sessions/<ID>.md` (§12) — это защита от того, что другой физический чат той же
+   роли начнёт работать параллельно.
 2. Обновить свою строку в `coordination/BOARD.md` (статус, текущая задача, план, время).
 3. Обновить `coordination/sessions/<ID>.md` (что делаю, какие файлы трогаю, до какого времени).
 4. Работать ТОЛЬКО в своей зоне (§3). Логировать изменения в `coordination/ACTIVITY.md`.
@@ -154,7 +154,7 @@ ORCH консолидирует и правит CHARTER (это его зона)
 Ведущая по образовательному слою — Сессия **D** (статья + primers + глоссарий); остальные соблюдают
 регистр и питают D темами.
 
-## 12. Ники чатов и лок исполнения (конкурентный доступ, конвенция 2026-07-31)
+## 12. Лок исполнения (конкурентный доступ; реестр ников выведен из эксплуатации 2026-08-07)
 
 **Зачем.** Одну «роль» (`A`/`B`/`C`/`D`/`L`/`ORCH`) во времени может обслуживать НЕСКОЛЬКО
 физических чатов: новый запуск, `--resume`, восстановление после краха. Если два таких чата
@@ -162,11 +162,10 @@ ORCH консолидирует и правит CHARTER (это его зона)
 Правило §8 (heartbeat старше ~1 ч) ловит только полностью заброшенную роль — слишком грубая шкала
 для «сейчас-занято / сейчас-свободно». Нужна более мелкая защита с коротким таймаутом на случай краха.
 
-**Ники.** Каждый физический чат, начинающий работать под ролью `<ID>`, получает короткий ник
-`<ID><n>` (следующий свободный номер для этой роли: `A1`, `A2`, `L3`…) вместо длинного chat id
-в повседневном обиходе. Регистрация — в `coordination/NICKS.md` (роль · ник · полный chat id,
-если известен · когда зарегистрирован · статус `active`/`retired`). Ники одной роли НЕ
-переиспользуются — новый чат всегда берёт следующий номер, даже если все прежние `retired`.
+**Именование сессий (миграция на штатный механизм, см. `coordination/MIGRATION.md`).** Сессия
+именуется при запуске (`claude -n <имя>`) или через `/rename`; список активных сессий —
+`claude agents` / `claude agents --json`. Роль в идентификаторе для `.lock` (см. ниже) — это то
+же самое имя. `coordination/NICKS.md` как реестр закрыт, новые ники там не заводятся.
 
 **Лок исполнения — два слоя, не путать (переписано 2026-08-03, песочница coord-debug, Сценарии 1-2-7):**
 
@@ -198,10 +197,9 @@ ORCH консолидирует и правит CHARTER (это его зона)
    **НЕ читать поле Статус в `sessions/<ID>.md` вручную вместо этого вызова** — старый способ
    (read-then-write без атомарности) воспроизводимо ловит lost update при коллизии (Сценарий 1,
    markdown-версия). Прочитать КОД ВОЗВРАТА и действовать строго по нему:
-   - **0** (`.lock` не существовал, создан с нуля этим вызовом) → занято, можно начинать. Ник ещё
-     не зарегистрирован в `NICKS.md` → зарегистрировать. Обновить человекочитаемый блок в
-     `sessions/<ID>.md` (Статус=`IN_PROGRESS`, Держатель=свой ник, Взят/Heartbeat=сейчас) —
-     это документирование для людей, не часть гейта, гейт уже пройден.
+   - **0** (`.lock` не существовал, создан с нуля этим вызовом) → занято, можно начинать. Обновить
+     человекочитаемый блок в `sessions/<ID>.md` (Статус=`IN_PROGRESS`, Держатель=имя сессии,
+     Взят/Heartbeat=сейчас) — это документирование для людей, не часть гейта, гейт уже пройден.
    - **1** (`.lock` был просрочен, пересоздан этим вызовом) → занято, можно начинать, но
      ОБЯЗАТЕЛЬНО СНАЧАЛА (до регистрации/блока выше):
      a. append в `ACTIVITY.md` с тегом `[STALE-LOCK]`: какой ник не снял лок, последний
@@ -209,9 +207,9 @@ ORCH консолидирует и правит CHARTER (это его зона)
      b. запись в `HANDOFFS.md` «ОТ `<новый ник>` К ORCH» с той же сутью — чтобы ORCH знал и мог
         проверить (иногда «завис» — ложное срабатывание: чат просто долго считает, не упал).
         Если реклейм делает сам ORCH — хэндофф себе не нужен, достаточно записи в ACTIVITY.
-     Затем — регистрация в NICKS.md и человекочитаемый блок, как в коде 0.
+     Затем — человекочитаемый блок, как в коде 0.
    - **2** (`.lock` занят, свежий держатель) → роль занята ДРУГИМ чатом ПРЯМО СЕЙЧАС.
-     **Не начинать работу.** Не трогать `.lock`, `NICKS.md`, человекочитаемый блок. Сообщить
+     **Не начинать работу.** Не трогать `.lock`, человекочитаемый блок. Сообщить
      владельцу, что роль `<ID>` занята; ждать освобождения или решение за владельцем/ORCH (§8).
    - **3 или 4** (повреждённый `.lock` / ошибка использования / вызов вообще не выполнился) —
      это ровно случай правила 4 ниже: сбой инструмента проверки, не гейт «свободно».
@@ -222,9 +220,8 @@ ORCH консолидирует и правит CHARTER (это его зона)
    эквивалент освобождения) И перевести Статус человекочитаемого блока → `FREE`. Освобождение —
    ПОСЛЕДНЕЕ действие чата, который РЕАЛЬНО и штатно прекращает работу (`exit`/`/clear`), не
    заранее и не «в порядке подготовки к остановке»: пока чат физически способен сделать следующий
-   шаг, `.lock` остаётся за ним, даже если он уже отметил себя `retired` в `NICKS.md` или сменил
-   задачу (смена задачи внутри ОДНОГО чата не требует пере-занятия `.lock` — только обновления
-   человекочитаемого блока).
+   шаг, `.lock` остаётся за ним, даже если он уже сменил задачу (смена задачи внутри ОДНОГО чата
+   не требует пере-занятия `.lock` — только обновления человекочитаемого блока).
    **При КРАХЕ (в отличие от штатной остановки) освобождения не происходит вовсе** — это не
    исключение из правила выше, а отдельный случай: именно отсутствие удаления `.lock` даёт
    следующему чату код `1` (просрочка по таймауту 15 мин), и это штатное поведение для краха, а
diff --git a/coordination/NICKS.md b/coordination/NICKS.md
index 0f0ddad..ac84634 100644
--- a/coordination/NICKS.md
+++ b/coordination/NICKS.md
@@ -1,5 +1,9 @@
 # NICKS — реестр ников чатов (см. CHARTER §12)
 
+> **Реестр закрыт 2026-08-07** (миграция на штатные именованные сессии, `coordination/MIGRATION.md`):
+> активные сессии смотреть через `claude agents`, новые ники здесь не заводятся. Ниже — исторический
+> архив, не трогать.
+
 Одну «роль» (`A`/`B`/`C`/`D`/`L`/`ORCH`) во времени может обслуживать НЕСКОЛЬКО физических чатов
 (новый запуск, `--resume`, восстановление после краха). Ник — короткая замена длинного chat id
 в повседневном обиходе (`sessions/<ID>.md`, `ACTIVITY.md`, `HANDOFFS.md`). Формат ника: `<Роль><n>`,
```

*(BOARD.md diff сокращён многоточиями внутри длинных строк таблицы ради читаемости этого файла —
полный дословный дифф без сокращений: `git show 67bc62f -- coordination/BOARD.md` в самом
репозитории.)*

## 3. Число строк: удалено против добавлено

Метрика захода — не построчный diffstat (там неизбежно много строк «переписано» при слиянии
колонок), а число ПРАВИЛ/КОЛОНОК/ПОЛЕЙ, которые перестали существовать:

- `CHARTER.md`: убран весь абзац «Ники» (регистрация, формат `<Роль><n>`, запрет
  переиспользования), убрана обязанность регистрации в §1 холодного старта, убраны 3 внутренних
  упоминания `NICKS.md` в Протоколе (занятие/реклейм/отказ) — **5 самостоятельных правил**.
- `BOARD.md`: 2 колонки живого статуса (`Статус`, `Текущая задача` как отдельная от находок
  сущность) для 6 ролей — структурно исчезли, слиты в одну содержательную колонку.
- `NICKS.md`: 0 правил снято по существу (реестр не трогаем), только 1 строка-пометка о закрытии.
- **Итог: 8 самостоятельных координационных правил закрыто, 1 новый файл** (`coordination/MIGRATION.md`
  — единственный, как и требовал промт).

## 4. Что не сделано и почему

- Пункты 2 (TTY-пикер сессий), часть п.4 (`/context`, `/branch` сам не вызывал) — подтверждены
  только `--help`/бандл-документацией, не прямым вызовом; отмечено честно, не выдано за «проверено».
- Хук `SessionEnd` (п.7) — подтверждено, что событие существует в этой сборке; **не настраивал**
  сам хук и не переносил на него правило «лок последним действием» — не проверено, успевает ли он
  отработать при КРАХЕ (не только при штатном `exit`/`/clear`). Записано отдельной строкой в
  `coordination/TEMPLATE_BACKLOG.md` (`design-question`, `open`), дальше не развивал.
- `coordination/tools/**` (код `.lock`), `SCHEDULING.md`, `research/**`, `TEMPLATE_MAP.md` — не
  трогал, как и требовали границы промта.
- Agent Teams (п.10) — только зафиксирован как существующий за флагом
  (`CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1`, выключено по умолчанию); использование как замену
  обмену между сессиями не проектировал.
- Отчёта в самом репозитории не создавал (по прямому запрету промта «Отчёта файлом не делай»);
  этот файл — отдельная, вне-репозиторная копия по прямой просьбе владельца.
