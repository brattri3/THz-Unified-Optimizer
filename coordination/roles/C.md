# Роль C — приложения

**Имя сессии:** `c-app` · **префикс коммита:** `[C]`

## Пишу
`attenuator_app/**`, `docs/attenuator_app/**` · `track_viewer/**`, `docs/track_viewer/**`
(зона расширена санкцией владельца 2026-08-12)

## Не трогаю
`data_pool/**` — read-only. ⚠ Зона хрупка к именам/путям — дважды ломалась молча (glob 28.07,
валидатор 04.08), ничего не падало.

## Что открыто и что дальше

1. **СВОДНОЕ ЗАДАНИЕ** (`HANDOFFS.md`, `ОТ ORCH К C` 17.08, вердикт A → `ACTIVITY.md` 24.08,
   ничего не отклонено). Осталось: (3) `C-6` — отвязать побинный фит от полосы показа; (4) `C-2`
   — фраза в `docs/track_viewer/01_FORMATS.md` §6.3. Прочее (знак `θ₀`, `eta0=0`) вне зоны C —
   `QUESTIONS.md`.
2. **⚠ ПЕРВЫМ ДЕЛОМ — `track_viewer/gui.py` не закоммичена (В-32).** +52/−3 от 14.08 в **основном**
   каталоге (не клоне `TUO-C`): копирование панели данных в буфер. Не в git, пропадёт при
   `checkout`. Доделать, закоммитить `[C]`.
3. **Два release gate у владельца** (личный прогон у прибора): `C6_v02_acceptance` (аттенюатор
   v0.3) и track_viewer ТЗ п.5 (Win7). До ответа C3/C4/C5 не начинать. ⚠ `.exe` v0.3 под Python
   3.14 не запустится на Win7.

## Находки, переданные A/B
шумовой пол по предымпульсу завышен на 12.4 дБ (умолчание — метод Нафтали) · `T_time` без
вычитания DC завышает тёмные углы в 121 раз · `argmax(abs(E))` в гашении мимо на 5 шагов.

## Моё
`attenuator_app/HANDOFF_C_20260807.md` · `attenuator_app/STATE.md` ·
`coordination/handoffs/REVIEW_C_fit_malus_2026-08-13.md`

⚠ `fit_malus.py` — **копия** `malus_linearization.py` (A), не импорт. A сверяет при правке
оригинала (`C-1`, 16.08), лок/CI не нужны. Сверка ORCH 17.08: 48/48, `0.000e+00` (→ `ACTIVITY.md`).
