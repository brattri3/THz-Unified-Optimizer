# BOARD — что открыто и что делать дальше

> **Кто сейчас активен — `claude agents` / `claude agents --json`** (см. `coordination/reports/MIGRATION_2026-08-07.md`),
> не здесь. Живой статус убран 2026-08-07: колонка систематически отставала от факта.

> **⚠ Регламент объёма (с 2026-08-08).** Строка роли — **до ~10 строк**: текущий статус, что открыто,
> что делать дальше, куда идти за подробностями. **История находок сюда не пишется** — её место в
> `ACTIVITY.md` (append-only, в холодный старт не входит) и в передачах ролей `HANDOFF_*`.
> Причина жёсткая: этот файл читает каждая роль при каждом старте. К 08.08 он разросся до 62 КБ
> (~21 тыс. токенов на старт) и был сжат до ~10 КБ. Дословный снимок «до» — в `ACTIVITY.md`,
> запись «Снимок BOARD.md перед сжатием» от 2026-08-08.

> **➜ План на понедельник 2026-08-10 — `coordination/reports/PLAN_2026-08-10.md`.**
> Что кому делать, в порядке зависимостей, и чего делать НЕ надо. Роли B, L, D можно
> запускать параллельно; A ждёт таблицу измерений владельца.

| Роль | Статус, что открыто, что дальше | Обновлено |
|---|---|---|
| **A** модели / идентифицируемость / гипотезы | **A19 `microscopy_lattice` закрыта 2026-08-10** (исполнитель A4, лок освобождён). Обработана поставка микрофото владельца, метод по границам вместо FFT. **Полный отчёт — `research/results/a18_microscopy/REPORT_A19_2026-08-10.md`** (12 разделов; §§1–2 писал заход A5, §§3–12 дособрал ORCH после его остановки по лимиту, врезка о происхождении внутри). Краткий свод — `SUMMARY.md`, скрипт `research/two_wgp/a18_microscopy_lattice.py`. Периоды: purewave **24.44±0.19** (было 25.5), specac **24.73±0.22** (24.9), test_grid_33_11 **31.71±0.47** (31.82), test_grid_40_20 **37.18±1.83** (38.8). Диаметры на полувысоте: 10.28 / 9.72 / 10.47 / 20.30 — **specac 14.0 в коде окончательно опровергнут**. **Нерегулярность измерена впервые:** σ_pos/P = 11–19 %, это независимый джиттер, не дрейф намотки (ACF₁ и σ_P/σ_dev внутри i.i.d.-нуль-модели); совпало с разбросом ручных обмеров владельца ⇒ `_P_stat.csv` — реальная структура, а не шум оператора. **Систематика «+0.2 мкм» отвергнута** (p = 0.0002 и 0.0026): дело в конвенции границы (владелец меряет по f ≈ 0.1–0.33, не 0.5), а не в микроскопе. **FFT ошибается в 42 % кадров из-за беспорядка, а не плотности** ⇒ метод по границам применим и к `att-11-16`. ⚠ Две собственные оценки периода расходятся на 0.6–1.6 % — реальная неопределённость больше SEM. **Дальше:** A-1/A-2/A-3 в `QUESTIONS.md` (конвенция границы, порядок перезапуска фитов purewave/40_20, калибровочная шкала); H5 не пересчитывался — ждёт утверждения таблицы владельцем. | 2026-08-10 |
| **B** техническая гигиена | **⚠ Роль переопределена владельцем 2026-08-08:** было «численное ядро + ревьюер эквивалентности», стало **техническая гигиена** — рефакторинг, дубли, инварианты, работоспособность кода. Правила сохранены: нулевая доля в физических выводах, допуск ноль со снимком «до». Режим — **по вызову, не постоянной сессией**. Зона расширена на `unified_optimizer/**` **по разовой санкции владельца на конкретный дефект**. **⏸ Задача 1 приостановлена до 2026-08-10** (слабый ноутбук): починка двух `NameError` продакшна, правка на ветке **`session/B-prod-fix`** (`ddbe0e7`), помечена «НЕ ПРОВЕРЕНО», **в `main` не мержить**; остаток проверок — в `HANDOFFS.md`. **Очередь после неё:** (1) вынос `GEOMETRY` → `research/experiments/geometry.py` — **со сроком**, иначе каждая правка диаметров требует сессии B; (2) `build_experiment(angles_limit, mask_angles)` — снимает второй билдер; (3) машинная проверка инварианта. ⚠ Цифра «копии фильтра 5 → 20» из передачи **не подтвердилась** (их пять); настоящий риск — **два билдера** и зависимость водяной маски от набора углов. Передача: `research/experiments/HANDOFF_B_20260807.md` (числа §3.1 читать с этой поправкой). | 2026-08-08 |
| **C** приложения | **Ник C2, лок занят 12.08 20:38.** **C7_track_viewer СДАН, фаза 2 тоже** — шесть этапов, шесть коммитов на `main`, приёмка **27/27**, комплект Win7 пересобран (106 МБ, приёмка внутри него тоже 27/27). **Фаза 2 (13.08):** пропускание по полосе частот рядом с временны́м + панели дельты (Парсеваль виден на экране: полная полоса схлопывает Δ, узкая в зоне гашения даёт −0.86 дБ); канал `dark` (перекрытый пучок) и три источника шумового пола. **➜ Находка для A/B: прежняя оценка шумового пола по предымпульсу завышена на 12.4 дБ** (`002_bg`: 3.94e-03 против 2.25e-04 по ВЧ-хвосту) — предымпульсный участок содержит низкочастотный дрейф, а не только белый шум; умолчание переведено на метод Нафтали. Пункты приёмки ТЗ §7 1–4 закрыты автотестами; **пункт 5 (запуск на Win7 у прибора) ждёт владельца** — теперь вместе с проверкой панелей дельты и dark. Документация — `docs/track_viewer/`. **Зона C расширена санкцией владельца 12.08 (CHARTER §8): + `track_viewer/**`, `docs/track_viewer/**`** — ORCH, внести в `OWNERSHIP.md` (запрос в `HANDOFFS.md`). **➜ A и B: две находки, `HANDOFFS.md` от 12.08** — `T_time` из `a21_transmission_summary.py` без вычитания DC завышает тёмные углы в 121 раз (опубликованные числа A21 не задеты, риск в переиспользовании); поиск пика по сырому `argmax(abs(E))` в зоне гашения промахивается на 5 шагов сетки. **Аттенюатор:** `C6_v02_acceptance` по-прежнему **open и ждёт владельца**, C3/C4/C5 не начинать; `.exe` v0.3 собран под Python 3.14 и **на Win7 не запустится** — целевая доставка теперь портативный комплект. Win7-комплект аттенюатора и распечатанный чек-лист намеренно остались на старых именах наборов. ➜ всем, кто двигает `data_pool/**`: зона C зависит от имён и расположения файлов, дважды ломалась молча. | 2026-08-12 |
| **D** статья + образовательный слой | **Курс §0–§10 + приложения A/Б завершён (2026-08-08)**, 76 страниц, сборка чистая, проверено дважды независимо. **Ждёт чтения владельца** — это и есть его приоритет «восстановить контроль над физикой». ⚠ Восемь параграфов из одиннадцати написаны одним непрерывным прогоном **без пауз на отзыв** (разовое исключение, закрыто) — замечания собирать по ним первым делом. ⚠ Приложение Б (образцы и геометрия) стоит на числах A16, которые владелец **ещё не утвердил** — вероятно, потребует переиздания. Правило «один параграф → PDF → пауза до отзыва» снова действует по умолчанию. Жанр — самостоятельная учебная литература, регламент `research/paper/course/STYLE.md` (без имён сессий, путей, номеров гипотез и кодовых имён наборов). Следующий ник — **D4**, промт и передача: `research/paper/course/LAUNCH_PROMPT.md`. Ждём от A/B свод чисел для `DRAFT §4.2/§4.7`. | 2026-08-08 |
| **L** литература / метапоиск | **stopped**, лок свободен, следующий чат берёт ник **L3**. **➜ читать первым: `research/literature/HANDOFF_L_20260807.md`.** **Главное для цели №5:** Blanco 1986 = *Infrared Phys.* **26**(6):357–363, DOI `10.1016/0020-0891(86)90058-8` (прежняя ссылка была фантомом); автор объявляет границу применимости `d/p < 0.5`, а наш `D/P = 0.71` — **вне** её ⇒ самостоятельное объяснение загадки `D_eff`, конкурирующее с «недостающей физикой». Разводить это с работой A **до** того, как загадка будет заявлена как главная новизна. Аудит DOI: 34 записи, битый `…884786` → `…838786` (исправлен). Предложение проекту без решения: прогонять Crossref-проверку списка литературы перед отправкой. Очередь: P2-выжимки, P4/P5, 2-й уровень графа цитирований, Kaltenecker на гармоники. Владельцу — 15 закрытых работ в `research/literature/WANTED.md`, первые три блокируют содержательные ответы. | 2026-08-07 |
| **P** отчётные презентации | **stopped**, лок свободен, следующий чат берёт ник **P2**. **➜ читать первым: `research/presentations/HANDOFF_P_20260807.md`.** Зона развёрнута с нуля, первый недельный отчёт сдан (`weekly/2026-08-04/`, 8 слайдов + `SOURCES.md`). ⚠ Его числа **устарели 06.08** после A16 — `fig1` обновлять **только после утверждения диаметров владельцем**, иначе переделывать дважды. **Четыре постоянных решения владельца по оформлению** (светлая тема; 8 ± 2 слайда; минимум выносок на графиках; обязательный слайд «теория и эксперимент в двух шкалах») — в `STYLE_MANIFEST.md`, действуют для всех отчётов. Владелец 04.08 разрешил зоне P строить графики по данным других зон, **но фитить нельзя** — параметры берутся готовыми из артефакта зоны-автора. Очередь: `P2_weekly_next` (не заблокирована, материала много), `P3_deff_deepdive` (ждёт утверждения геометрии). | 2026-08-07 |
| **ORCH** координация | **Ник O8, лок занят реклеймом (2026-08-14, простой O7 — 5484 мин).** Состояние: `main` **ahead origin на 18 коммитов**, все `[C]` (`22b1565`…`82add80`, `track_viewer/**`, +7559 строк) — **пуш ждёт владельца**. Локи: A/B/D/L — FREE, C — отставший heartbeat живой роли (не снят), P — просрочен без следов работы после 10.08. В общих файлах лежит **чужая незакоммиченная работа** (`ACTIVITY`+540, `HANDOFFS`+162, `QUESTIONS`+56, `RESEARCH_LOG`+85, `two_wgp/state.json`) + untracked отчёт A19 и артефакты `a18_microscopy` — не коммитил (CHARTER §5). **`data_pool/specac/` перестроен владельцем 13.08 и закоммичен по его распоряжению** (`5f7b471`): прежние 47 файлов → `exp_data_1/` (переименования, содержимое идентично), рядом `exp_data_2/` — второй угловой прогон по треку `track_viewer`, снят 13.08 17:40–22:20, все 67 файлов заполнены. **Код не задет:** `DataManager` разбирает имя файла, а не путь ⇒ датасет `specac` остался одним, 13 углов (проверено запуском); имена второго прогона под шаблон не подходят и молча пропускаются — **новых датасетов не появилось** (первоначальное опасение ORCH снято проверкой). Карта — `data_pool/specac/README.md`. **Открыто:** второй прогон в общий фит не включён — нужна нормализация имён, задача не заявлена. Прежний свод (2026-08-10, O7): **Мы на РАБОЧЕМ ПК** Синхронизировано с origin: `415b708..7b1528b`, 37 коммитов, дерево чистое; на origin появилась `session/B-prod-fix` (`ddbe0e7`). **➜ задания на сегодня — `coordination/reports/PLAN_2026-08-10.md`.** Учёт роли отстал: сессии 07–09.08 работали без регистрации ников и не переоформляли `.lock` (держал `O3` с 03.08, просрочка 9759 мин) — приведено в соответствие, разбор в `ACTIVITY.md` `[STALE-LOCK]` от 10.08. **⚠ Мы на РАБОЧЕМ ПК:** пути `C:\Users\pop\Antigravity IDE\**` и соседний `../THz-WGP-Analysis/` существуют ⇒ (1) предсказанные 09.08 падения трёх скриптов по «мёртвому профилю» здесь не воспроизводятся (правка нужна для переносимости, не для запуска); (2) отложенный README-снимок по В-6 **исполним сегодня** (текст готов, README в снимке отсутствует). Поправка к отчёту 09.08: скриптов с чужим профилем **три**, а не четыре — `scripts/compile_pdf_specification.py` попал в список ошибочно (там `Path(__file__)`, абсолютны только шрифты Windows). **Новая находка для L (`HANDOFFS.md` 10.08):** 22 PDF первоисточников остались в untracked `research/papers/litrev/pdfs/`, новый `research/literature/litrev/pdfs/` **пуст** — `git mv` не переносит игнорируемые файлы. **Открыто:** (1) доводим задачу B; (2) В-10 — три трека упираются в действия владельца; (3) два PDF в `research/literature/pdfs/` трекаются вопреки политике зоны — решение за L; (4) `research/state.json` главной линии устарел (2026-07-25); (5) старые хэндоффы не перепроверялись с 07.08; (6) В-2 (`docs/`) всё ещё `open`. | 2026-08-10 |

## Легенда владения (полностью — `OWNERSHIP.md`)
A→`research/two_wgp/`+`research/hypotheses/` · B→численное ядро + техническая гигиена ·
C→`attenuator_app/` · D→`research/paper/` · L→`research/literature/` · P→`research/presentations/` ·
ORCH→`coordination/`, `CLAUDE.md`, main `research/state.json`.

## Активные «замки» намерений
См. `coordination/sessions/*.md`. Перед правкой общего файла — проверь их. Каждый файл содержит блок
«🔒 Замок исполнения» (CHARTER §12): статус `IN_PROGRESS`/`FREE`, имя держателя, таймаут 15 минут.
