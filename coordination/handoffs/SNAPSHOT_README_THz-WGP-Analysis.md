# Заготовка README для соседнего снимка `THz-WGP-Analysis/` (решение В-6)

**Зачем этот файл здесь.** Решение владельца В-6 от 2026-08-08: пометить `../THz-WGP-Analysis/`
как снимок-экспорт, положив рядом README с датой. При исполнении 2026-08-09 выяснилось, что
**на этой машине такого каталога нет** (и `../WORKSPACE_MAP.md`, `../THz-Spectroscopy-Python-Manual2`
тоже нет). Каталог либо удалён, либо живёт на второй машине владельца — отсюда решение исполнить
нельзя. Текст готов заранее, чтобы на нужной машине это была операция на одну минуту.

**Как применить** (на машине, где каталог есть) — скопировать блок ниже в
`THz-WGP-Analysis/README.md`:

---

```markdown
# THz-WGP-Analysis — СНИМОК-ЭКСПОРТ, не рабочая копия

**Не править. Источник истины — репозиторий `THz-Unified-Optimizer`** (git, origin на GitHub).

| | |
|---|---|
| Что это | Экспортный снимок части проекта, сделанный вручную |
| Git | **НЕ git-репозиторий.** Изменения здесь никуда не уходят и не отслеживаются |
| Дата фиксации | 2026-07-28 (последнее известное изменение состава) |
| Помечено | 2026-08-09, решение владельца В-6 |

## Состав и судьба частей

- **`code/`** — байт-в-байт копия пакета `unified_optimizer/` (9 файлов) на момент экспорта.
  Правки в репозитории сюда **не распространяются автоматически**; расхождение накапливается
  молча. Считать устаревшим по умолчанию.
- **`litrev/`** — литобзор. **Свёрнут в основной репозиторий 2026-07-28**, живёт там как
  `research/papers/litrev/**` (зона роли L). Здесь — снимок до сворачивания.

## Если нужно что-то отсюда

Сверять с репозиторием, а не копировать обратно. Обратный перенос — только через роль-владельца
соответствующей зоны (`coordination/OWNERSHIP.md` в репозитории).
```

---

**Статус В-6: отложено до машины, где каталог существует.** Если каталога нет ни на одной —
решение закрывается как беспредметное, и из `CLAUDE.md` убирается раздел «Кросс-репо связи».
