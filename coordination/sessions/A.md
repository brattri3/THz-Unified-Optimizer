# Сессия A — метка намерений / замок

> Обновляй при СТАРТЕ, смене задачи, «пульсе» и остановке. Перед правкой ОБЩЕГО файла — проверь
> чужие sessions/*.md на пересечение. Устаревший heartbeat (>~1ч) = сессия считается неактивной.

- **Статус:** stopped — чат A3 штатно закрыт владельцем 2026-08-07. **Передача роли: `research/two_wgp/HANDOFF_A_20260807.md` — читать первым.** Следующий чат под ролью A возьмёт ник `A4`.
- **Специализация (расширена 2026-07-28, санкция владельца):** прямая модель, структурная
  идентифицируемость и проверка гипотез + софт затухания (CHARTER §3)
- **Текущая задача:** `A7_purewave` + `A8_diffraction_zones` + `A9_aperture_scan_protocol` —
  **DONE** (2026-08-04, по запросам владельца). Ранее в этом чате: `A3_refit_series` — DONE
  (HN14 REJECTED). **Расчёты остановлены владельцем 2026-08-04 10:15**, всё записано; пакеты для
  сессий **P** (недельный отчёт) и **D** (учебный слой, 2026-08-05) переданы.
  Следующая — `A5_synthesis`; шаг не начат.
- **⚠ Отзыв внутри этого чата:** запись истории от 08:05 ниже утверждает «HN15 не поддержана» —
  это **отозвано** в 09:30 после уточнения владельцем оптической схемы (угол и участок образца
  перепутаны). Актуальный статус HN15 — **НЕ ПРОВЕРЕНА**. Историческая запись не переписана
  намеренно (append-only), см. поправку в конце списка.
- **План:** A3 — является ли `ε_cross` КОНСТАНТОЙ УСТАНОВКИ/ДЕТЕКТОРА или per-dataset поглотителем
  невязки (паттерн HN10). Мультистартовый фит `M5_eps` на аттенюаторной группе (356att, series1-3)
  + контрольной группе одиночных WGP (specac, test_grid_40_20); тест однородности комплексного
  `ε_cross` (χ² по паре Re/Im, 2 dof на датасет) + перекрёстная сверка с модельно-независимым `η`
  из гармоник A0. Побочно закрывается незакрытый пункт A6 «full-range M5 refit».
  ⚠ **`purewave` ЗАБЛОКИРОВАН** — все 69 файлов измерений нулевого размера (заглушки от 2026-07-27).
- **Файлы, что трогаю (замок):** `research/two_wgp/a3_eps_stability.py` (новый),
  `research/two_wgp/state.json`, `research/results/two_wgp/a3_*`, `research/hypotheses/**`;
  append в `research/logs/RESEARCH_LOG.md`; своя строка в `BOARD.md`/`ACTIVITY.md`.
  Общий код (`fit_lib`/`model_core`/`model_m5`/`t6_hn*`) — ТОЛЬКО чтение.
- **Заявлено до (время):** шаг A3 завершён; лок держу до явной остановки владельцем
- **⚠ Разовый выход за зону (санкция владельца 2026-08-04):** переименование данных аттенюатора
  под подпись `att-11-16` потребовало правок в зонах B, C и замороженном продакшне (39 файлов,
  96 замен). Владелец на прямой вопрос выбрал «починить всё сразу», чтобы проект не остался
  сломанным. Подробности и доказательства неизменности чисел — в `ACTIVITY.md`.
- **⚠ Второй разовый выход за зону (санкция владельца 2026-08-04):** прямая просьба привести имена
  трасс `data_pool/purewave/` к конвенции и обработать измерения. Записи в `data_pool/` —
  только КОПИИ с каноническими именами рядом с оригиналами (прецедент `data_pool/specac/`),
  ни один исходный файл не переименован и не удалён; плюс переписан `purewave/README.md` и
  строка `purewave` в `DATASETS.md`. Скрипты и артефакты — в своей зоне.
- **⚠ Третий разовый выход за зону (прямая просьба владельца 2026-08-06):** разложить дорогу
  файлов-заготовок для нового образца, как было у `purewave`. В `data_pool/test_grid_33_11/`
  создано только ПУСТОЕ (69 файлов нулевого размера + шаблон META + два документа), ни один
  существующий файл не тронут; генератор отказывается перезаписывать непустые файлы.
- **Heartbeat:** 2026-08-07 (закрытие)

## 🔒 Замок исполнения (см. CHARTER §12)
- **Статус:** FREE (освобождён штатно 2026-08-07 при закрытии чата владельцем; `A.lock` удалён, процедура `[STALE-LOCK]` не применялась — это не крах)
- **Держатель (ник):** A3 (chat `06cadc2f-5f09-4afb-9788-b28982710213` — сообщён владельцем
  2026-08-04, внесён в `NICKS.md`; предшественники A1 `a0e3dd33-…`, A2 `b91ac21e-…` — оба
  `retired`)
- **Взят:** 2026-08-03 23:12:55 (`preflight.py A --acquire A3` → код возврата **0**, `.lock`
  создан с нуля: технического лока для роли A ранее не существовало)
- **Heartbeat:** 2026-08-07 (закрытие)
- **Таймаут простоя:** 15 минут

## Сделано (артефакты закоммичены в 7f6efec)
- **A1 `model_2wgp.py`** — двух-WGP Джонс-модель, частотно-комплексная, per-element; 10/10 проверок
  (`--selftest` → `results/two_wgp/a1_validation.json`). Побочно доказана **теорема выравнивания**
  (ось WGP2 ∥ оси детектора ⇒ реальный WGP2 ≡ идеальный анализатор × `t_perp2(ν)`) ⇒ гипотеза
  «загадка D_eff = отсутствующий WGP2» опровергнута. T10 закрыл вопрос владельца об ориентации проводов.
- **A2 `fit_2wgp.py`** — лестница 10 вариантов на 356att (`results/two_wgp/a2_356att_ladder.json`).
  Подтверждение на данных: `dAIC=+0.15`, `D_eff` 4.694→4.694 мкм (без изменений). Рассогласование `φ2`
  НЕ заменяет `ε_cross` (мультистарт: разрыв 110.5 AIC в пользу ε_cross, `φ2`=+1.6°±4.8 незначим).

## История намерений (append)
- [2026-07-27] seed-инициализация метки.
- [2026-07-28] Расширение зоны A зафиксировано по санкции владельца (CHARTER §3 врезка, OWNERSHIP,
  BOARD). Взята задача `A6_angle_symmetry`; хэндофф B «не форкать билдер» переведён в `taken`
  (принят по существу, но A6 не ждёт итерации B — приватный билдер с бит-сверкой на (0,90)).
- [2026-07-28] Сессия A зарегистрирована постфактум: работала до создания каркаса `coordination/`,
  поэтому записи A1/A2 в `RESEARCH_LOG.md` (от 2026-07-25 и 2026-07-27) идут БЕЗ тега `[A]` —
  атрибуция зафиксирована здесь и в ACTIVITY. Далее тег `[A]` проставляется.
- [2026-07-28 17:16, ORCH] Замок снят как устаревший (heartbeat 08:24 > 1ч). Контекст сессии A,
  видимо, сброшен. **A6 не завершена:** скрипты `a6_angle_symmetry.py` (452стр) и `exp_builder.py`
  (192стр) написаны и лежат НЕЗАКОММИЧЕННЫМИ в рабочем дереве, но результата нет (нет json в
  `research/results/two_wgp/`, нет числовой записи в RESEARCH_LOG, `state.json.current_task=A6/pending`).
  Контенции за файлы `research/two_wgp/**` нет — освобождение безопасно. Возобновление A: запустить
  A6, залогировать числа, обновить state.json и HYPOTHESES, закоммитить свою зону с тегом `[A]`.
- [2026-07-29] Замок ПЕРЕЗАНЯТ. Принято к сведению: решение ORCH Р-1 «граница по слою» консолидировано
  (CHARTER §3 / OWNERSHIP) — повторная правка НЕ требуется; педагогический регистр CHARTER §11 принят.
  Вне очереди взят приоритетный вопрос владельца — линеаризация закона Малюса для неидеального
  поляризатора с утечкой (задача `A0_malus_linearization`, зона A: прямая модель + идентифицируемость).
  Затем штатно A6 → A4 → A3 → A5.
- [2026-08-03 23:12, A3] Новый чат под ролью A. Лок занят с нуля (код 0), ник `A3` зарегистрирован
  в `NICKS.md`. Взята `A3_refit_series`. **Находка при старте:** владелец в лаунч-промте указал, что
  в `data_pool/purewave/` лежит новый набор измерений — это НЕ так: все 69 файлов трасс имеют размер
  0 байт (заглушки «дороги» от 2026-07-27 21:21, ни один файл `data_pool/` не менялся с 27.07).
  `t20_weighted_fit.py --dataset purewave` подтверждает машинно (штатный выход «no usable multi-rep
  data yet», код 0). Часть задачи по `purewave` записана как BLOCKED, остальная выполняется.
- [2026-08-04 01:45, A3] Шаг `A3_refit_series` завершён. **HN14 REJECTED**: `ε_cross` не константа
  тракта (χ² однородности `761.6/10 dof`, p=3.8e-157; отвергнуто и внутри каждой из двух групп по
  оптической схеме). Установлена причина — тройное вырождение `D_eff↔η₀↔ε_cross`; устойчиво
  измеряется только модельно-независимая суммарная утечка `η` (×1.1–1.3 внутри группы против ×25–57
  у параметров M5). Побочно закрыт открытый пункт A6 (фит на полном угловом диапазоне). Отозвано
  число `|ε_cross|=0.0345` из A2 (4-й бассейн из 11, +192.5 AIC) — хэндофф к D. Правило A2 о
  мультистарте переформулировано (нужна сетка стартов; 22.2% попаданий в глобальный бассейн).
- [2026-07-31, ORCH/O2] Владелец закрыл чат A2 (и все остальные предметные чаты) командой `exit`.
  Штатная остановка, не крах — лок освобождён без процедуры `[STALE-LOCK]` (CHARTER §12 п.3
  применяется только к зависшим, не к явно закрытым локам). A3 остаётся первой задачей очереди
  для следующего чата под ролью A (возьмёт ник `A3`).
- [2026-08-04 08:05, A3] Вне очереди по прямой просьбе владельца выполнены `A7_purewave` и
  `A8_diffraction_zones`. `purewave` перестал быть BLOCKED: владелец домерил его за два дня,
  имена приведены к конвенции копиями (оригиналы целы). **Главная находка: два дня не сшиваются
  в тени** — при физически одной ориентации проводов (π-периодичность, −95° ≡ +85°) день 1 глубже
  на 5.1–6.6 дБ, модельно-независимая `η` 0.0185 против 0.0429 (×2.3), а `θ₀` совпадает
  ⇒ поменялась утечка, а не угловая шкала. Отсюда усиление вердикта A3: `η` — характеристика
  УСТАНОВКИ образца, а не образца. **HN15 не поддержана**: образец с известным дефектом
  регулярности даёт наименьший дефицит `D_eff` и точнее всех ложится на закон H5 (откл. +0.012).
  A8: заводская зона регулярнее восстановленной по всем безразмерным метрикам, но выборка 3 на 3
  и масштаб кадров плавает — тенденция, не число. Владельцу задан вопрос о единицах в измерении
  дифракции (1.75 мм против ожидаемых 8.03/16.06 мм).
- [2026-08-04 09:30, A3] **ПОПРАВКА к записи 08:05.** Владелец уточнил оптическую схему:
  поляризатор стоит в перетяжке 1.5–2 мм и намеренно смещён так, что тень попадает на ЗАВОДСКИЕ
  зоны, а максимум — на восстановленные. Узкий пучок, смещённый от оси вращения, при повороте
  описывает окружность по апертуре ⇒ **угол и участок образца перепутаны**, `η` и `D_eff`
  измерены по заводской зоне. Вывод «HN15 не поддержана» ОТОЗВАН, статус гипотезы —
  **НЕ ПРОВЕРЕНА**. Период по дифракции уточнён (17.5 мм между ±1, не 1.75): `P = 23.4 ± 0.8` мкм,
  с микрофото совместимо (1.8σ). Написан протокол правильного эксперимента (`A9`).
- [2026-08-04 10:15, A3] Владелец остановил анализ и расчёты; результаты зафиксированы.
  Апертура `purewave` = 35 мм внесена, протокол скана пересчитан под неё (шаг 3 мм → 12 позиций,
  ≈5 ч) + добавлен раздел про симметричную форму `η(x)` как встроенную самопроверку.
  Для сессии **P1** (недельный отчёт) подготовлен самодостаточный пакет
  `research/two_wgp/BRIEF_FOR_P_week_20260728_0804.md` с §5 «чего не должно быть в презентации»
  (отозванные и смещённые числа). Chat id A3 внесён в `NICKS.md` по данным владельца.
  Лок держу, `A5_synthesis` не начата.
- [2026-08-05, A3] По прямому заказу владельца подготовлено и передано **ТЗ на учебный слой
  сессии D** — `research/two_wgp/BRIEF_FOR_D_teaching_ladder.md`: 11 модулей от базовой
  электродинамики до методологии анализа, все числа со ссылками на артефакты, нумерация
  методичек вписана в очередь D4. Сквозная ось курса — «угловой скан на одной частоте
  ограничивает не более 5 вещественных чисел» (A0), из неё выводится тройное вырождение.
  **Заказанный владельцем эффект корреляции в линейном фите в проекте проявляется и уже
  измерен:** cond(XᵀX) 2.14 против 236.3, σ(θ₀) 0.096° против 0.944°, смещение от отбрасывания
  4-й гармоники −2.3…−4.8°. Расчёты НЕ возобновлялись — документ собран из посчитанного ранее.
- [2026-08-06, A3] По прямой просьбе владельца разложена **дорога измерений нового образца**
  `data_pool/test_grid_33_11/` (P = 33, D = 11, **D/P = 0.333** — самый разреженный в проекте).
  69 пустых файлов, маршрутный лист с предполётным чек-listом, машинный план
  `research/two_wgp/plan_test_grid_33_11.json`, генератор
  `research/two_wgp/prepare_road_test_grid_33_11.py` (dry-run по умолчанию, `--status` показывает
  прогресс, перезапись непустых файлов запрещена). Имя взято из зарезервированной строки
  `DATASETS.md`; неоднозначность «D≈33? P≈11?» снята владельцем. План углов и порядок блоков
  построены на уроках A0 (полный диапазон вместо обрезки (0,90)) и A7 (разделение дрейфа и
  физики через π-периодичность). Предсказания зафиксированы ДО измерения. Расчёты не
  возобновлялись.
- [2026-08-06 15:30, A3] **Расчёты возобновлены по прямой просьбе владельца** (остановка от
  08-04 снята для этой задачи). Обработан новый датасет `test_grid_33_11`: нормализация имён
  копиями (102 копии, SHA-256 сходятся), качество, два контроля, гармоники A0, мультистарт M5.
  **Закон H5 прошёл первую экстраполяционную проверку** (предсказано 0.717, измерено 0.694).
  **Уточнён вердикт A3/HN14: вырождение `D_eff↔η₀↔ε_cross` — свойство ПЛОТНОГО режима**
  (здесь corr = −0.429 против ±1.0, `D_eff` устойчив, ±0.015 мкм). Явная утечка `η₀` в
  разреженном режиме не нужна (1.2σ). Отчёт `research/two_wgp/REPORT_test_grid_33_11.md`,
  артефакты `a13_test_grid_33_11.{json,png}`. Хэндоффы к B (геометрия в `fit_lib`), D и P
  (восьмая точка + оговорка к формулировке о вырождении).
- [2026-08-06 18:00, A3] **A14: геометрия `test_grid_33_11` измерена по микрофото** (владелец
  указал их на флешке): `P = 31.75 ± 0.65` вместо 33, `D = 12.55 ± 0.60` вместо 11,
  `D/P = 0.395`. Калибровка перенесена с июльской сессии и проверена дважды (ступени увеличения
  1.5 %, согласие увеличений 0.4 %). Фит перезапущен: `D_eff/D_phys` 0.694 → **0.576**.
  **⚠ Вывод A13 «закон H5 прошёл экстраполяционную проверку» ОТОЗВАН** — отклонение −0.088,
  крайнее отрицательное из восьми образцов. Независимое подтверждение: ТГц-фит предпочёл
  измеренный период номинальному на 7.1 AIC при равном числе параметров. Отчёт обновлён
  (§1 редакция 2), поправки разосланы B/D/P.
- [2026-08-06 21:00, A3] **A14b по замечанию владельца: диаметр был завышен расфокусировкой.**
  `in_focus_mask` отбирает по квантилям внутри кадра ⇒ оставляет половину проволок даже когда
  размыты все. Измерил размытие каждой и привёл к пределу резкости прибора (8.69 px по
  калибровочной гребёнке): **`D = 10.91 ± 0.41`** вместо 12.55; лучшая отдельная проволока
  10.80 мкм подтверждает без всякой модели. Контроль на `purewave`: наклоны +0.195 против +0.211
  ⇒ эффект аппаратный. `D_eff` не изменился; `D_eff/D_phys` 0.576 → **0.663**, отклонение от H5
  −0.045 (внутри разброса). **Открыто (A16):** поправка сдвигает и purewave (10.6 → 9.5),
  сплошной пересчёт перенастроит закон H5 — нужно решение владельца.
- [2026-08-06 23:30, A3] **A16 по поручению владельца: геометрия всех образцов пересчитана
  единообразно, закон H5 перенастроен — и устоял** (`k = 0.855 ± 0.085` против 0.850).
  Вскрыты три проблемы метода: плавающий масштаб кадров, разный способ измерения периода у
  разных образцов, и переоценка поправки регрессией. **Моё вчерашнее число `D = 10.91` (A14b)
  ОТОЗВАНО**, итог 12.60. Итоговая геометрия: purewave 24.72/9.83, specac 24.91/**7.77**,
  test_grid_40_20 38.87/15.99, test_grid_33_11 31.82/12.60; `att-11-16-*` измерить нечем.
  Отчёт `REPORT_A16_geometry_h5.md`, хэндофф B/D/P с окончательными строками GEOMETRY.
- [2026-08-07 00:10, A3] **Владелец закрыл вопрос о диаметре `purewave`: 10.0 мкм ровно**
  (паспорт производителя, решение а не измерение). Наши оценки берут его в вилку
  (9.83 / 10.02 / 10.82), противоречия нет. Период остаётся открытым (24.72 против 26.10,
  ТГц не различают). Хэндофф к B по `GEOMETRY` ПРИОСТАНОВЛЕН, пересчёт H5 отложен —
  владелец утверждает диаметры остальных образцов, жду его результатов.
- [2026-08-07, A3] **Чат закрыт владельцем.** Лок освобождён штатно (`A.lock` удалён, статус
  блока → FREE). Полная передача состояния — **`research/two_wgp/HANDOFF_A_20260807.md`**:
  чего ждём от владельца (утверждение физических диаметров, `purewave` 10.0 закрыт,
  `test_grid_33_11` 11.2 предварительно), что нельзя делать до этого (хэндофф к B по
  `GEOMETRY` на паузе, пересчёт H5 отложен), цепочка трёх отзывов за 6 августа, открытые
  вопросы и методические уроки. Следующий чат роли A берёт ник `A4`.
